# Zephyr RTOS 분석 — 작업 계획

최종 갱신: 2026-08-06

이 문서는 **이미 확정된 사항을 기록하여 재질문을 방지**하고, 남은 작업의 순서와 범위를 고정하기 위한 것입니다. 새 세션에서 작업을 이어갈 때는 이 문서를 먼저 읽습니다.

---

## 1. 확정 사항 — 다시 묻지 않음

| 항목 | 결정 | 확정일 | 상세 |
|---|---|---|---|
| 분석 대상 | Zephyr 4.4.99 (`zephyr/` 디렉터리) | 2026-08-05 | [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §1 |
| 1차 범위 | 커널 코어 (`zephyr/kernel/`) | 2026-08-05 | 00 §2 |
| 빌드 타깃 | `qemu_cortex_a53` **SMP 변형** (AArch64, 2코어) | 2026-08-05 | 00 §4 |
| 보조 타깃 | Cortex-M 계열 (`SWAP_NONATOMIC` 경로 확인 전용) | 2026-08-05 | 00 §4.3 |
| 산출물 형식 | 마크다운 문서 (표·ASCII 도식 사용, Mermaid 미사용) | 2026-08-05 | — |
| 산출물 위치 | `1.zephyr분석/` | — | 루트 `CLAUDE.md` |
| 서술 언어 | 한국어 존댓말. 식별자·코드는 영문 | — | `expert-claude/instruction_and_rule/response_rules.md` |
| 인용 규칙 | 모든 서술에 `파일:행` 근거 명시, 추론에는 확신도(high/medium/low) 표기 | — | 00 §5 |
| 파일 수정 정책 | 명시적 지시 없이 원본 소스 수정 금지. 문서 산출만 수행 | — | `expert-claude/.../basic_rule.md` |
| Python 실행 | `/Users/claunel/Projects/zephyr/.venv/bin/python3` 절대 경로 | — | `expert-claude/.../python_virtual_env.md` |
| Git | 현재 저장소 아님. **사용자 지시 없이 `git init` 금지** | — | `expert-claude/.../git_commit_rule.md` |

### 문서 작성 시 매번 반복하는 절차

1. 대상 소스를 정독한다 (헤더 주석이 아니라 구현까지).
2. 문서를 작성한다. 각 문서 상단에 "타깃 적용" 표로 활성/비활성 경로를 표기한다.
3. 검증 스크립트로 인용 링크와 핵심 인용 행을 확인한다.
4. 본 문서(§2)의 상태를 갱신한다.

검증 스크립트: `scratchpad/verify_refs.py` (세션 임시 디렉터리). 새 문서 작성 시 `ANCHORS` 목록에 핵심 인용을 추가합니다.

---

## 2. 진행 상태

| # | 문서 | 상태 | 비고 |
|---|---|---|---|
| 00 | [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) | ✅ 완료 | 타깃 확정 반영 완료 |
| 01 | [01-커널-코어-자료구조.md](1.zephyr분석/01-커널-코어-자료구조.md) | ✅ 완료 | 타깃 적용표 추가 완료 |
| 02 | [02-스케줄러.md](1.zephyr분석/02-스케줄러.md) | ✅ 완료 | 동일 |
| 03 | [03-스레드-생명주기.md](1.zephyr분석/03-스레드-생명주기.md) | ✅ 완료 | 동일. `__incoherent` 설명 정정 완료 |
| 04 | [04-타임아웃과-시스템-클럭.md](1.zephyr분석/04-타임아웃과-시스템-클럭.md) | ✅ 완료 | 02·03 서술 재확인 완료 (§10). 정정 필요 사항 없음 |
| 05 | [05-SMP와-스핀락.md](1.zephyr분석/05-SMP와-스핀락.md) | ✅ 완료 | — |
| 06 | [06-동기화-객체.md](1.zephyr분석/06-동기화-객체.md) | ✅ 완료 | `k_queue`/`k_stack`/`k_msgq`/`k_mbox`/`k_pipe`는 미포함 (범위 참조) |
| 07 | [07-부팅과-초기화.md](1.zephyr분석/07-부팅과-초기화.md) | ✅ 완료 | 커널 코어 1차 분석(동작 관점) 범위 완료 |

### 2차: 구조 관점 (2026-08-06 착수)

동작이 아니라 **조직·의존·확장 구조**를 보는 직교 축입니다.

| # | 문서 | 상태 | 비고 |
|---|---|---|---|
| 08 | [08-계층과-의존-구조.md](1.zephyr분석/08-계층과-의존-구조.md) | ✅ 완료 | include 그래프 실측 기반 |
| 09 | [09-확장-메커니즘.md](1.zephyr분석/09-확장-메커니즘.md) | ✅ 완료 | 7개 메커니즘을 결합 시점·실패 모드 두 축으로 분류 |

구조 관점 분석도 완료되었습니다. 다음 범위는 사용자 결정이 필요합니다(§4-1 참조).

---

## 3. 작업 순서와 그 근거

주제 순서를 조정하면서 **파일 번호는 읽는 순서와 일치**시켰습니다. 즉 SMP·스핀락이 05, 동기화 객체가 06, 부팅·초기화가 07입니다. (2026-08-06 확정 — 그전까지 이 문서는 주제별 번호 07/05/06을, README는 읽는 순서 번호를 써서 어긋나 있었습니다.)

조정 근거:

- **04(타임아웃)를 먼저**: [02](1.zephyr분석/02-스케줄러.md) §8의 superseded 프로토콜과 [03](1.zephyr분석/03-스레드-생명주기.md) §7.2~7.3의 `-EAGAIN` 재시도 루프는 `timeout_q.h`의 **헤더 주석을 근거로 서술**했고 `timeout.c` 구현은 읽지 않은 상태였습니다. abort/halt 경로의 정확성이 걸린 부분이라 근거를 구현으로 확정해야 했습니다. → 완료, 정정 사항 없음
- **05(SMP·스핀락)를 앞당김**: 타깃이 2코어 SMP로 확정되어 `_sched_spinlock`, IPI, `z_smp_current_get()`이 전부 주 경로가 되었습니다. 기존 문서들은 이들을 이름만 언급하고 넘어갔습니다. → 완료
- 06(동기화 객체)은 04·05 위에 쌓이는 응용 계층이므로 뒤로, 07(부팅)은 상대적으로 독립적이고 A53에서 MMU·EL 전환 때문에 복잡도가 높아 마지막에 둡니다.

---

## 4. 문서별 범위 — 착수 시 재확인 불필요

### 04 타임아웃과 시스템 클럭
- `kernel/timeout.c` 전체: `z_add_timeout()`, `z_try_abort_timeout()`, `sys_clock_announce_locked()`, `elapsed()`, `next_timeout()`
- **in-flight / superseded 상태 머신** — 동일 CPU 대 타 CPU 분기, `-EAGAIN` 계약
- 백엔드 4종 비교: DLIST(타깃), BUCKET, WHEEL, MINHEAP
- tickless 동작과 `z_get_next_timeout_expiry()`
- `k_timer` (`kernel/timer.c`)와의 관계
- **검증 목표**: 02 §8, 03 §7.2~7.3의 서술을 구현 근거로 재확인하고, 어긋나면 정정

### 05 SMP와 스핀락 — 완료
- `kernel/smp/`: `smp.c`, `ipi.c`, `cpu_mask.c`
- `k_spinlock` 구현과 `CONFIG_SPIN_VALIDATE`
- IPI 경로: `flag_ipi()`, `signal_pending_ipi()`, `ipi_mask_create()`, directed 대 broadcast
- `z_smp_current_get()`, `z_smp_cpu_mobile()`
- 보조 CPU 부팅 (`z_smp_init()`, `k_smp_cpu_start()`)

### 06 동기화 객체 — 완료
- `sem.c`, `mutex.c`(우선순위 상속·우선순위 상한), `condvar.c`, `events.c`, `poll.c`
- 공통 기반인 `z_pend_curr()` / `z_sched_wake()` 위에 각각 어떻게 쌓이는지
- `k_queue`, `k_stack`, `k_msgq`, `k_mbox`, `k_pipe`는 **미포함** — 자료구조 운반이 주이고 동기화 원리는 위 다섯과 동일하므로, 필요 시 별도 문서로 다룹니다

### 07 부팅과 초기화 — 완료
- 리셋 벡터 → `z_cstart()` → `SYS_INIT` 레벨 → main 스레드 진입
- `kernel/init.c`, `arch/arm64/core/reset.S`·`reset.c`·`prep_c.c`
- A53 특이사항: EL3/EL2/EL1 강등 사다리, voting lock 기반 주 코어 선출

---

## 4-1. 후속 분석 후보 (미착수, 우선순위 미정)

커널 코어 1차 범위가 완료되었으므로, 다음 단계는 사용자 결정이 필요합니다.

| 후보 | 내용 | 비고 |
|---|---|---|
| 워크 큐 | `kernel/work.c` — `k_work`, delayable, poll 연동 | 커널 코어의 마지막 미분석 영역 |
| 데이터 전달 객체 | `k_queue`, `k_stack`, `k_msgq`, `k_mbox`, `k_pipe` | 06에서 의도적으로 제외 |
| 메모리 관리 | `kheap.c`, `mem_slab.c`, `mempool.c`, `mmu.c` | |
| 디바이스 드라이버 모델 | `device.c`, devicetree, Kconfig 연동 | 초기 질의에서 후보였던 항목 |
| 빌드 시스템 | CMake / Kconfig / DTS / west 결합 | 동일 |
| userspace | `kernel/userspace/`, syscall 생성, 메모리 도메인 | 타깃에서 비활성이라 별도 구성 필요 |

---

## 5. 분석 범위 밖 — 환경 구성 (작업 항목 아님)

빌드·실행 환경 구축은 **소스 분석이 아니므로 작업 계획에 포함하지 않습니다.** 아래는 사용자가 필요하다고 판단할 때 선택하는 사항이며, 하지 않아도 분석은 완결됩니다.

| 항목 | 하면 얻는 것 | 현재 상태 |
|---|---|---|
| Zephyr SDK 설치 후 빌드 | [00](1.zephyr분석/00-분석-개요.md) §4.2의 "기본값" 항목을 생성된 `.config`로 확정 | `aarch64-zephyr-elf-gcc` 미설치 |
| QEMU 실행·GDB | 정적 분석 결과를 실행으로 대조 | 위와 동일 |

이로 인한 분석상의 한계는 [00](1.zephyr분석/00-분석-개요.md) §4.4에 기록되어 있고, 영향받는 서술에는 확신도 medium이 표기되어 있습니다. **한계를 명시한 것으로 분석은 완결된 것으로 간주합니다.**

---

## 6. 미결 사항 (사용자 결정 필요)

| # | 사항 | 배경 | 진행 차단 여부 |
|---|---|---|---|
| 1 | **문서 링크 경로 방식** | `1.zephyr분석/`이 git 저장소 루트로 초기화되었습니다(2026-08-06). 현재 문서의 링크는 **워크스페이스 루트 기준**이라, 저장소 단독 문맥(GitHub 웹 등)에서는 해석되지 않습니다. 문서 간 링크(`1.zephyr분석/00-....md`)와 소스 링크(`zephyr/kernel/sched.c`) 모두 해당합니다 | 아니오 — IDE에서는 정상 동작하므로 작성은 계속 가능 |
| 2 | 분석 문서 라이선스 | 공개 배포 시 필요. 인용 코드는 Zephyr(Apache-2.0) | 아니오 |
| ~~3~~ | ~~검증 스크립트 저장소 포함 여부~~ | **해소됨 (2026-08-06)** — `scripts/verify_refs.py`와 `scripts/include_graph.py`를 저장소에 포함했습니다. 절대 경로를 자기 위치 기준 해석으로 바꿔 다른 워크스페이스에서도 동작합니다 | — |

---

## 7. 독립 검증 이력

`expert-claude/instruction_and_rule/verification_instruction.md`는 산출물 검증을 **Examinator(독립·읽기 전용)**가 수행하도록 요구합니다. 자체 검증만으로는 기준이 자기 주장에 그치기 때문입니다.

### 2026-08-06 — 1차 독립 검증

문서에서 가장 무게가 실린 주장 16개를 측정 가능한 명제로 뽑아 Examinator에 전달했습니다(모델 opus, 도구 Read/Grep/Glob).

| 결과 | 건수 |
|---|---|
| 검증 통과 | 15 |
| **검증 실패** | **1** |
| 검증 불가 | 0 |

**실패 1건 — 08 §6.** "커널 내부 헤더는 두 곳에만 열린다", "`subsys/` 코드가 커널 내부 헤더를 쓰면 빌드가 실패한다"는 서술이 틀렸습니다. `zephyr_library_include_directories()` / `zephyr_include_directories_ifdef()`를 통한 타깃별 opt-in 경로를 누락했습니다. 실제로 `subsys/` 9곳이 요청하며, `subsys/portability/posix/options/semaphore.c:16`은 `<wait_q.h>`를 포함하고 정상 빌드됩니다.

같은 문서 §7이 처음부터 opt-in 24곳을 정확히 기술하고 있었으므로 **§6과 §7이 서로 모순된 상태**였습니다. 정정 완료하고 문서에 정정 이력을 남겼습니다.

**검증자 보고의 오류 1건.** Examinator는 opt-in `CMakeLists.txt`를 72곳으로 보고했으나 실측은 **71곳**입니다(`drivers/` 0, `subsys/` 9는 일치). 검증 결과도 그대로 수용하지 않고 재확인해야 함을 보여주는 사례입니다.

### 남은 검증 공백

- 인용 141건 외 나머지 링크는 **내용이 아니라 존재·범위만** 확인됩니다
- 행 범위 인용(`#L56-L137`)이 실제 그 함수인지는 미검증
- 09 문서의 집계 수치는 재현 스크립트가 없습니다(§12에 명령만 기재)
- 16개 명제 외의 서술은 독립 검증을 거치지 않았습니다

### 스크립트 유지보수 규칙

새 문서를 작성하면 `scripts/verify_refs.py`의 `ANCHORS` 목록에 **핵심 인용을 추가**해야 합니다. 링크 유효성 검사만으로는 "행 번호가 밀렸지만 여전히 파일 범위 안"인 경우를 잡지 못합니다. 상세는 [scripts/README.md](1.zephyr분석/scripts/README.md)를 참조하십시오.

### #1에 대한 선택지

| 안 | 내용 | 장점 | 단점 |
|---|---|---|---|
| A | 문서 간 링크는 저장소 상대(`00-....md`)로, 소스 링크는 상류 GitHub 고정 커밋 URL로 | 저장소 단독으로 완전히 해석됨 | 소스 링크가 로컬 트리가 아닌 웹을 가리킴. 버전 고정 필요 |
| B | 문서 간 링크는 저장소 상대로, 소스 링크는 `../zephyr/...` | 로컬 파일시스템에서 해석됨 | 저장소 밖을 가리켜 GitHub에서는 여전히 깨짐 |
| C | 현행 유지하고 워크스페이스 배치 전제를 README에 명시 | 변경 없음 (이미 적용) | 저장소 단독 열람 시 링크 무효 |

권장: **A**. 다만 소스 링크가 213건이라 일괄 변환이 필요하므로, 사용자 결정 후 스크립트로 처리하겠습니다.
