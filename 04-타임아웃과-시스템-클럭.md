# 타임아웃과 시스템 클럭

분석 대상: [zephyr/kernel/timeout.c](zephyr/kernel/timeout.c), [zephyr/kernel/timeout_list.h](zephyr/kernel/timeout_list.h), [zephyr/kernel/timeout_minheap.h](zephyr/kernel/timeout_minheap.h), [zephyr/kernel/timeout_bucket.h](zephyr/kernel/timeout_bucket.h), [zephyr/kernel/timeout_wheel.h](zephyr/kernel/timeout_wheel.h), [zephyr/kernel/timer.c](zephyr/kernel/timer.c), [zephyr/include/zephyr/drivers/timer/system_timer.h](zephyr/include/zephyr/drivers/timer/system_timer.h)

> **타깃 적용 (`qemu_cortex_a53` SMP)** — 전체 구성은 [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §4 참조
>
> | 본 문서 항목 | 이 타깃에서 |
> |---|---|
> | §8 백엔드 | **DLIST만 활성** (`TIMEOUT_BACKEND_DLIST`). MINHEAP·BUCKET·WHEEL은 대조용 |
> | `_timeout.dticks` | `int64_t` — `TIMEOUT_64BIT=y` |
> | §5 SMP 조기 반환 (`any_cpu_announcing`) | **활성.** 2코어이므로 실제로 도달 |
> | §6 `-EAGAIN` 경로 | **활성.** `IS_ENABLED(CONFIG_SMP)` 조건 충족 |
> | §3 `elapsed()`의 `announce_remaining` 가산 | **활성.** SMP 전용 항 |
> | `SYSTEM_CLOCK_SLOPPY_IDLE` | **비활성** (기본값 없음) → §7 두 번째 행(마감 없음 시 `MAX_WAIT` 그대로)은 이 타깃에서 도달하지 않음 |
> | tickless | **활성.** `ARM_ARCH_TIMER`가 `TICKLESS_CAPABLE`을 select ([Kconfig.arm_arch:10](zephyr/drivers/timer/Kconfig.arm_arch#L10)) → `TICKLESS_KERNEL` 기본 y |
> | 타이머 드라이버 | ARM architected timer, 64비트 사이클 카운터 |
> | `z_add_timeout()`의 coherence 어서션 | **비활성.** `KERNEL_COHERENCE=n` |

---

## 1. 구조 — 프런트엔드와 백엔드의 분리

`timeout.c`는 **백엔드 독립 프런트엔드**이고, 실제 큐 자료구조는 네 개의 헤더 중 하나로 갈아끼웁니다([timeout.c:108-116](zephyr/kernel/timeout.c#L108-L116)).

```c
#if defined(CONFIG_TIMEOUT_BACKEND_MINHEAP)
#include "timeout_minheap.h"
#elif defined(CONFIG_TIMEOUT_BACKEND_WHEEL)
#include "timeout_wheel.h"
#elif defined(CONFIG_TIMEOUT_BACKEND_BUCKET)
#include "timeout_bucket.h"
#else /* CONFIG_TIMEOUT_BACKEND_DLIST */
#include "timeout_list.h"
#endif
```

이 `#include`의 **위치가 설계의 일부**입니다. 공유 상태(`curr_tick`, `announce_remaining`, `inflight_timeout`, `timeout_lock`) 정의 **뒤에** 포함되므로, 백엔드는 그 상태를 직접 참조할 수 있으면서도 파일 밖으로는 노출되지 않습니다([timeout.c:100-107](zephyr/kernel/timeout.c#L100-L107) 주석). 헤더 파일이지만 실질적으로는 "이 번역 단위 전용 구현 조각"입니다.

경계는 이렇게 나뉩니다:

| 계층 | 내용 | 위치 |
|---|---|---|
| 노드별 헬퍼 | `z_init_timeout()`, `z_is_inactive_timeout()` | `kernel/include/timeout_q.h` — 트리 전역에서 필요 (`timer.c`, `work.c`, `poll.c`…) |
| 큐 연산 | `z_timeout_q_insert/remove/remainder/next_expiry/next_gap/advance/pop_due` | 백엔드 헤더 — `timeout.c` 전용 |
| 프런트엔드 | `z_add_timeout()`, `z_try_abort_timeout()`, `sys_clock_announce_locked()`, `next_timeout()` | `timeout.c` |

### 락 규약

파일 상단에 규칙이 못박혀 있습니다([timeout.c:22-27](zephyr/kernel/timeout.c#L22-L27)).

> 타임아웃 코드는 자기 락(`timeout_lock`) 외에 어떤 락도 잡지 않으며, 이 락을 쥔 채 다른 서브시스템을 호출하지 않는다.

이 규약이 지켜지는 것이 눈에 보이는 지점이 두 곳입니다 — 핸들러 호출 전 락 해제(§5), 그리고 `z_time_slice()` 호출이 `k_spin_unlock()` **뒤에** 있는 것([timeout.c:401-405](zephyr/kernel/timeout.c#L401-L405)).

---

## 2. 공유 상태

| 변수 | 타입 | 의미 |
|---|---|---|
| `curr_tick` | `uint64_t` | 부팅 이후 절대 틱. **커널이 인지한** 시각이며 실시간과 다를 수 있음 |
| `timeout_lock` | `k_spinlock` | 이 파일의 유일한 락 |
| `announce_remaining` | `uint32_t` | 현재 announce에서 아직 처리하지 않은 틱 수 |
| `announcing_cpu` | `int` | announce 루프에 들어가 있는 CPU id, 없으면 `-1` |
| `inflight_timeout` | `struct _timeout *` | 핸들러가 **지금 실행 중**인 타임아웃. 하위 비트는 superseded 플래그 |

[timeout.c:19-57](zephyr/kernel/timeout.c#L19-L57)

`announcing_cpu`가 CPU 집합이 아니라 **단일 `int`로 충분한 이유**가 주석에 있습니다([timeout.c:32-39](zephyr/kernel/timeout.c#L32-L39)): §5의 SMP 조기 반환이 "동시에 한 CPU만 루프에 있음"을 보장하기 때문입니다.

---

## 3. 시간 계산 — `elapsed()`와 핵심 불변식

[timeout.c:72-98](zephyr/kernel/timeout.c#L72-L98)

```c
static uint32_t elapsed(void)
{
	if (this_cpu_announcing()) {
		return 0U;
	}
	return sys_clock_elapsed() +
	       (IS_ENABLED(CONFIG_SMP) ? announce_remaining : 0);
}
```

두 분기의 의미가 전혀 다릅니다.

**(a) 이 CPU가 announce 중이면 0.** announce 루프 안은 정확히 틱 경계이며, 콜백에서 새로 등록하는 상대 타임아웃은 "지금 발화 중인 틱"을 기준으로 삼아야 합니다.

**(b) 그 외에는 실제 경과 + `announce_remaining`.** 이쪽이 미묘합니다. 유지해야 할 불변식은:

```
T_real - curr_tick = announce_remaining + sys_clock_elapsed()
```

이유는 다음과 같습니다. 타이머 ISR 진입 시 **드라이버는 내부 baseline을 `(curr_tick_초기 + N) * CYC_PER_TICK`으로 이미 올려버립니다.** 반면 커널의 `curr_tick`은 루프가 처리한 K틱만큼만 전진한 상태입니다. 따라서 `sys_clock_elapsed()`가 알려주는 값에 잔여분 `announce_remaining = N - K`를 더해야 `curr_tick` 기준의 실제 시간차가 나옵니다. 이 보정이 announce 창을 가로질러 `sys_clock_tick_get()`의 **단조성**을 지켜줍니다.

`announce_remaining` 가산이 `IS_ENABLED(CONFIG_SMP)` 조건인 것은, UP에서는 announce 중인 CPU가 곧 이 CPU여서 (a) 분기로 빠지므로 도달할 수 없기 때문입니다. (확신도: high — UP에서 `announcing_cpu != -1`이면 그 값은 반드시 `CPU_ID`)

---

## 4. 타임아웃 등록 — `z_add_timeout()`

[timeout.c:172-233](zephyr/kernel/timeout.c#L172-L233)

```c
if (K_TIMEOUT_EQ(timeout, K_FOREVER)) {
	return 0;
}
__ASSERT(z_is_inactive_timeout(to), "");
to->fn = fn;

K_SPINLOCK(&timeout_lock) {
	if (Z_IS_TIMEOUT_RELATIVE(timeout)) {
		ticks_elapsed = elapsed();
		has_elapsed = true;
		dticks = timeout.ticks + ticks_elapsed +
			 (this_cpu_announcing() ? 0 : 1);      /* ← 반올림 */
		ticks = curr_tick + dticks;
	} else {
		dticks = Z_TICK_ABS(timeout.ticks) - curr_tick;
		dticks = max(1, dticks);
		ticks = timeout.ticks;
	}

	if (z_timeout_q_insert(to, dticks) && !any_cpu_announcing()) {
		if (!has_elapsed) {
			ticks_elapsed = elapsed();
		}
		sys_clock_set_timeout(next_timeout(ticks_elapsed), false);
	}
}
```

주목할 네 가지:

### 4.1 `K_FOREVER`는 큐에 들어가지 않습니다

즉시 0을 반환합니다. `k_sem_take(sem, K_FOREVER)`가 타임아웃 슬롯을 소비하지 않는 이유이며, [03-스레드-생명주기.md](1.zephyr분석/03-스레드-생명주기.md) §5에서 언급한 "`K_FOREVER`이면 무등록"이 여기서 확정됩니다.

### 4.2 `+1` 반올림과 그 예외 — 타임슬라이싱과 직결

```c
dticks = timeout.ticks + ticks_elapsed + (this_cpu_announcing() ? 0 : 1);
```

일반적으로 "지금"은 틱 중간 어딘가이므로, 1틱을 올림해야 **"최소 N틱"** 의미가 보장됩니다. 올림하지 않으면 틱 중간에 요청한 타임아웃이 다음 틱 경계에 발화해 N틱보다 짧아집니다([timeout.c:195-211](zephyr/kernel/timeout.c#L195-L211) 주석).

예외는 **이 CPU가 announce 중일 때**입니다. 그때는 정확히 틱 경계이므로 올림이 불필요하며, 올림하면 **주기 타이머가 매 주기마다 1틱씩 밀립니다.** 주석이 이를 명시합니다 — 주기 타이머는 타이머 ISR에서 스스로를 재등록할 때 이 성질에 의존합니다.

> **[02-스케줄러.md](1.zephyr분석/02-스케줄러.md) §9의 `-1` 보정이 여기서 설명됩니다.**
> `z_time_slice()`는 `sys_clock_announce_locked()`의 **맨 끝, `announcing_cpu = -1`과 언락 이후**에 호출됩니다([timeout.c:397-405](zephyr/kernel/timeout.c#L397-L405)). 따라서 슬라이스 타임아웃을 재무장하는 시점에는 `this_cpu_announcing()`이 이미 **거짓**이고 `+1`이 적용됩니다. `slice_reset()`이 `slice_size - 1`을 넘기는 것은 정확히 이 `+1`을 상쇄하기 위한 것입니다([timeslicing.c:85-95](zephyr/kernel/timeslicing.c#L85-L95)). 타임슬라이싱 주석의 "틱 경계에 있지만 announce 창을 지난 상태"라는 표현이 코드상 어디를 가리키는지가 확인되었습니다. (확신도: high)

### 4.3 절대 타임아웃의 `max(1, dticks)`

이미 지나간 절대 시각을 요청해도 최소 1틱 뒤에 발화합니다. 즉 **`z_add_timeout()`은 콜백을 동기적으로 호출하지 않습니다.**

### 4.4 재무장 조건 — `&& !any_cpu_announcing()`

새 타임아웃이 최선(가장 이른)이 되었더라도, **누군가 announce 중이면 드라이버를 재무장하지 않습니다.** announce 루프가 끝날 때 `sys_clock_set_timeout(next_timeout(0), false)`로 어차피 재무장하므로([timeout.c:399](zephyr/kernel/timeout.c#L399)), 중복 프로그래밍을 피합니다.

---

## 5. announce 루프 — `sys_clock_announce_locked()`

[timeout.c:332-406](zephyr/kernel/timeout.c#L332-L406). 타이머 드라이버 ISR이 락을 잡은 채 호출하며, 이 함수가 락을 놓습니다. 락 없이 쓰는 래퍼 `sys_clock_announce()`도 있습니다([system_timer.h:228-231](zephyr/include/zephyr/drivers/timer/system_timer.h#L228-L231)).

### 5.1 SMP 조기 반환

```c
if (IS_ENABLED(CONFIG_SMP) && any_cpu_announcing()) {
	announce_remaining += ticks;
	k_spin_unlock(&timeout_lock, key);
	return;
}
```

핸들러 호출을 위해 락을 놓으므로, 다른 CPU가 이미 루프를 돌고 있을 수 있습니다. 그때 **경쟁하지 않고 틱만 누적하고 물러납니다.** 주석의 근거가 명확합니다 — 경쟁하면 "순차적이어야 할" 타임아웃들이 병렬 실행되어 애플리케이션을 혼란시킵니다([timeout.c:334-339](zephyr/kernel/timeout.c#L334-L339)).

이것이 §2에서 `announcing_cpu`가 단일 `int`로 충분한 근거입니다.

### 5.2 본 루프

```c
while (announce_remaining > 0) {
	int32_t dt = z_timeout_q_next_gap();
	if ((uint32_t)dt > announce_remaining) {
		dt = (int32_t)announce_remaining;
	}

	z_timeout_q_advance(dt);
	curr_tick += dt;
	announce_remaining -= dt;

	while ((t = z_timeout_q_pop_due()) != NULL) {
		_timeout_func_t handler = t->fn;
		inflight_timeout = t;
		k_spin_unlock(&timeout_lock, key);
		handler(t);
		key = k_spin_lock(&timeout_lock);
		inflight_timeout = NULL;
	}
}
```

- **다음 이벤트까지 한 번에 점프합니다.** 틱을 하나씩 세지 않고 `z_timeout_q_next_gap()`만큼 건너뜁니다. 빈 틱이 아무리 많아도 루프는 이벤트 수만큼만 돕니다.
- **세 줄(`advance` / `curr_tick +=` / `announce_remaining -=`)이 락 아래에서 함께 갱신됩니다.** 주석은 그 이유를 §3의 불변식으로 설명합니다([timeout.c:369-378](zephyr/kernel/timeout.c#L369-L378)) — 핸들러 때문에 락을 놓는 동안에도 다른 CPU가 일관된 `curr_tick + announce_remaining + sys_clock_elapsed() == T_real`을 관측해야 합니다.
- 같은 주석이 **`announce_remaining`이 루프 도중 0이 되어도 무해**함을 밝힙니다. "announce 중"이라는 상태는 `announcing_cpu`가 들고 있기 때문입니다.
- **핸들러 호출 전후로 `inflight_timeout`을 설정/해제**합니다. 락을 놓는 이 구간이 §6 프로토콜이 존재하는 이유 전부입니다.

### 5.3 루프 종료 후

```c
announcing_cpu = -1;
sys_clock_set_timeout(next_timeout(0), false);
k_spin_unlock(&timeout_lock, key);
#ifdef CONFIG_TIMESLICING
	z_time_slice();
#endif
```

`z_time_slice()`가 **언락 뒤**에 있는 것이 §1 락 규약의 실천입니다. 그리고 §4.2에서 본 것처럼, 이 시점에는 `announcing_cpu`가 이미 `-1`이라 슬라이스 재무장에 `+1`이 붙습니다.

### 5.4 백엔드가 루프를 가져가는 경우

타이머 휠 백엔드는 `_TIMEOUT_BACKEND_OWNS_ANNOUNCE`를 정의해 **자기 announce 루프를 제공**합니다([timeout.c:349-356](zephyr/kernel/timeout.c#L349-L356), [timeout_wheel.h:47](zephyr/kernel/timeout_wheel.h#L47)). 이유는 휠의 틱 전진이 "비어 있는 틱을 건너뛰는 비트맵 스캔"이고, 계층 간 sift가 특정 타임아웃에 매이지 않은 시간 구동 이벤트여서 범용 `next_gap`/`advance`/`pop_due` 3연산으로 표현되지 않기 때문입니다.

---

## 6. in-flight / superseded 프로토콜 ★

[02-스케줄러.md](1.zephyr분석/02-스케줄러.md) §8과 [03-스레드-생명주기.md](1.zephyr분석/03-스레드-생명주기.md) §7.2~7.3이 의존하는 핵심 메커니즘입니다.

### 6.1 문제

announce 루프는 핸들러 호출을 위해 **락을 놓습니다**(§5.2). 그 사이에:

- 타임아웃은 이미 큐에서 **빠져 있습니다** (`z_timeout_q_pop_due()`가 제거함) → `z_is_inactive_timeout()`이 참
- 그런데 핸들러는 **아직 실행되지 않았거나 실행 중**입니다

이때 다른 주체가 그 타임아웃을 abort하려 하면, "큐에 없으니 이미 끝났다"고 오판하게 됩니다. 실제로는 곧 핸들러가 돌아 스레드를 깨우게 됩니다.

### 6.2 표현 — 포인터 하위 비트

```c
static struct _timeout *inflight_timeout;
#define INFLIGHT_SUPERSEDED_BIT 1UL

static inline struct _timeout *inflight_ptr(void)
{
	return (struct _timeout *)((uintptr_t)inflight_timeout & ~INFLIGHT_SUPERSEDED_BIT);
}
```
[timeout.c:51-70](zephyr/kernel/timeout.c#L51-L70)

`struct _timeout`은 포인터 정렬이므로 **비트 0이 항상 비어 있습니다.** 그 자리에 "이 in-flight 디스패치는 추월당했다"는 플래그를 얹습니다. 별도 변수 없이 포인터 한 개로 두 정보를 담습니다.

### 6.3 `z_try_abort_timeout()`의 3분기

[timeout.c:235-270](zephyr/kernel/timeout.c#L235-L270)

| 상황 | 조건 | 반환 | 부가 동작 |
|---|---|---|---|
| 아직 큐에 있음 | `!z_is_inactive_timeout(to)` | `0` | 큐에서 제거. 최선이었으면 드라이버 재무장 |
| **다른 CPU에서 핸들러 실행 중** | SMP && `inflight_ptr() == to` && `!this_cpu_announcing()` | `-EAGAIN` | 반환 직전 `arch_spin_relax()` |
| 그 외 (미등록/이미 완료/**같은 CPU** in-flight) | — | `-EINVAL` | — |

그리고 **어느 분기든 상관없이**, `inflight_ptr() == to`이면 superseded 표시를 남깁니다([timeout.c:256-262](zephyr/kernel/timeout.c#L256-L262)).

세 번째 행의 "같은 CPU"가 중요합니다. `this_cpu_announcing()`이 참이면 — 즉 **이 CPU의 announce 루프가 핸들러를 호출했고, 그 핸들러(또는 그것을 선점한 ISR)가 지금 abort를 시도하는 중이면** — `-EAGAIN`을 줄 수 없습니다. 자기 자신을 기다리는 것이므로 영원히 재시도하게 됩니다. 대신 `-EINVAL`을 주되 superseded 표시를 남겨, 핸들러가 스스로 물러나게 합니다.

### 6.4 두 종류의 호출자

`timeout_q.h`의 계약 주석([timeout_q.h:83-101](zephyr/kernel/include/timeout_q.h#L83-L101))과 구현 주석([timeout.c:249-252](zephyr/kernel/timeout.c#L249-L252))이 호출자를 두 부류로 나눕니다.

**(a) free-safety 호출자** — 타임아웃 저장 공간을 해제하려는 쪽. `-EAGAIN`에서 반드시 락을 놓고 재시도해야 합니다.

```c
while (z_try_abort_thread_timeout(thread) == -EAGAIN) {
	k_spin_unlock(&_sched_spinlock, *key);
	*key = k_spin_lock(&_sched_spinlock);
}
```
[sched.c:849-854](zephyr/kernel/sched.c#L849-L854) — 동적 스레드의 UAF 방지

**(b) 그 외 호출자** — superseded 표시에만 의존하고 기다리지 않습니다.

```c
(void)z_try_abort_thread_timeout(thread);
```
[ksched.h:268](zephyr/kernel/include/ksched.h#L268) (`z_unpend_first_thread_locked`), [sched.c:777](zephyr/kernel/sched.c#L777) (`z_unpend_all`)

**`-EAGAIN`에서 락을 놓아야 하는 이유**는 데드락 회피입니다. 비행 중인 핸들러(`z_thread_timeout()`)는 `_sched_spinlock`을 잡으려 합니다([sched.c:476](zephyr/kernel/sched.c#L476)). abort 측이 그 락을 쥔 채 스핀하면 핸들러는 영원히 진행하지 못합니다. (확신도: high — 두 함수의 락 획득 대상이 동일)

### 6.5 핸들러 측 — 물러나기

```c
bool z_timeout_inflight_superseded(const struct _timeout *to)
{
	K_SPINLOCK(&timeout_lock) {
		superseded = inflight_timeout ==
			     (struct _timeout *)((uintptr_t)to | INFLIGHT_SUPERSEDED_BIT);
	}
	return superseded;
}
```
[timeout.c:272-282](zephyr/kernel/timeout.c#L272-L282)

**포인터 일치와 비트 설정을 한 번의 비교로** 확인합니다. 즉 "지금 in-flight인 것이 바로 `to`이고, 그것이 추월당했다"일 때만 참입니다.

두 곳에서 사용합니다:

```c
/* 스레드 타임아웃 */
if (!z_timeout_inflight_superseded(timeout)) {
	z_sched_wake_thread_locked(thread);
}
```
[sched.c:484-486](zephyr/kernel/sched.c#L484-L486)

```c
/* k_timer */
if (!z_is_inactive_timeout(t) || z_timeout_inflight_superseded(t)) {
	k_spin_unlock(&timer_lock, key);
	return;
}
```
[timer.c:86-90](zephyr/kernel/timer.c#L86-L90)

`k_timer` 쪽은 **조건이 두 개**입니다. 주석이 두 경우를 구분합니다([timer.c:78-85](zephyr/kernel/timer.c#L78-L85)):

- `!z_is_inactive_timeout(t)` — 타이머가 **재시작**되었음. `z_add_timeout()`이 다시 큐에 넣었으므로 새 일정이 나중에 발화함
- `z_timeout_inflight_superseded(t)` — 타이머가 **정지**되었음. `expiry_fn`을 부르면 안 됨

`expiry_fn`처럼 **멱등하지 않은 부작용**이 있는 핸들러는 반드시 이 검사를 진입 시점에(자기 락을 잡은 뒤) 해야 한다는 것이 `timeout_q.h:104-108`의 지침입니다.

### 6.6 전체 흐름

```
CPU0: announce 루프                        CPU1: 세마포어 give
─────────────────────────                  ────────────────────────
pop_due() → t 큐에서 제거
inflight_timeout = t
unlock(timeout_lock)
                                           lock(_sched_spinlock)
                                           z_unpend_first_thread_locked()
                                             → z_try_abort_thread_timeout(t)
                                               lock(timeout_lock)
                                               z_is_inactive_timeout(t) 참 (이미 pop됨)
                                               inflight_ptr()==t → -EAGAIN
                                               inflight_mark_superseded()   ★
                                               unlock(timeout_lock)
                                             → 반환값 무시 (b 부류 호출자)
                                             → 스레드를 무조건 ready로
handler(t) = z_thread_timeout()
  lock(_sched_spinlock) ← CPU1이 놓을 때까지 대기
  z_timeout_inflight_superseded(t) 참  ★
  → 아무것도 하지 않고 반환
lock(timeout_lock)
inflight_timeout = NULL
```

깨우는 책임이 **abort한 쪽에 단독으로** 있게 되는 구조입니다. 이 때문에 `z_unpend_all()`이 abort 결과와 무관하게 스레드를 ready로 만드는 것이 옳습니다([sched.c:772-778](zephyr/kernel/sched.c#L772-L778)).

---

## 7. 다음 만료 계산 — `next_timeout()`

[timeout.c:124-170](zephyr/kernel/timeout.c#L124-L170). 드라이버에게 "얼마나 자도 되는가"를 알려주는 함수이며, 네 갈래 모두 이유가 다릅니다.

```c
#define SYS_CLOCK_MAX_WAIT (UINT32_MAX / 2)
```
[system_timer.h:37](zephyr/include/zephyr/drivers/timer/system_timer.h#L37)

`sys_clock_announce()`가 **직전 announce 이후 경과 틱**을 `uint32_t`로 보고하므로, 두 announce 사이 간격이 이 값을 넘으면 카운트가 맞지 않습니다. 부호 없는 틱 공간의 절반으로 잡아 여유를 둡니다.

| 상황 | 반환 | 이유 |
|---|---|---|
| `ticks_elapsed >= MAX_WAIT` | `0` | 예산 소진. 즉시 announce 요구 |
| 대기 없음 + `SYSTEM_CLOCK_SLOPPY_IDLE` | `MAX_WAIT` **그대로** | 드라이버가 **클럭 완전 정지** 신호로 인식하는 값. uptime 오차는 감수 |
| 대기 없음 + sloppy idle 아님 | `MAX_WAIT - ticks_elapsed` | uptime 정확성 유지를 위해 계속 깨어남 |
| `next >= MAX_WAIT` | `MAX_WAIT - 1 - ticks_elapsed` | **1을 빼는 것이 핵심** |
| 그 외 | `next - ticks_elapsed` (음수면 0) | 정상 경로 |

네 번째 행의 `-1`이 미묘합니다([timeout.c:154-161](zephyr/kernel/timeout.c#L154-L161) 주석): 반환값이 정확히 `MAX_WAIT`이면 두 번째 행의 **"마감 없음"과 구별되지 않고**, sloppy idle 환경에서 그 타임아웃이 **통째로 유실**됩니다. 하나 모자라게 반환해 중간 announce를 유발하고, 거기서 다시 평가하게 만듭니다.

또한 주석은 `next`(64비트일 수 있음)를 먼저 상한과 비교함으로써 **이후 산술이 32비트에 머물게** 한다는 부수 효과도 밝힙니다.

---

## 8. 백엔드 4종 비교

| 백엔드 | 자료구조 | 키 | 삽입 | 임의 제거 | 최선 조회 | Kconfig |
|---|---|---|---|---|---|---|
| **DLIST** (기본·타깃) | 정렬된 델타 리스트 | 선행 노드 대비 델타 | O(n) | O(1) | O(1) | `TIMEOUT_BACKEND_DLIST` |
| MINHEAP | 이진 최소 힙 | 절대 만료 틱 | O(log n) | O(log n) | O(1) | `TIMEOUT_BACKEND_MINHEAP` |
| BUCKET | 틱별 버킷 + 오버플로 리스트 | 버킷 인덱스 / 절대 틱 | O(1) / O(n) | O(1) | O(1) 비트맵 | `TIMEOUT_BACKEND_BUCKET` |
| WHEEL | soon/later/distant 3계층 | 계층별 상이 | O(1) / O(n) | O(1) | O(1) 비트맵 | `TIMEOUT_BACKEND_WHEEL` |

DLIST 외 셋은 Kconfig에서 **EXPERIMENTAL**로 표시되어 있으며, MINHEAP과 BUCKET은 `TIMEOUT_64BIT`를 요구합니다([kernel/Kconfig:755-806](zephyr/kernel/Kconfig#L755-L806)).

### 8.1 DLIST — 델타 리스트 (타깃)

[timeout_list.h](zephyr/kernel/timeout_list.h). 각 노드가 **선행 노드 대비 델타**를 들고 있어, 틱 전진 시 **머리 노드 하나만 감소**시키면 전체가 갱신됩니다.

```c
static inline void z_timeout_q_advance(int32_t dt)
{
	struct _timeout *t = z_timeout_q_first();
	if (t != NULL) {
		t->dticks -= dt;
	}
}
```
[timeout_list.h:128-135](zephyr/kernel/timeout_list.h#L128-L135)

삽입은 델타를 누적하며 자리를 찾고, 끼어들 때 후속 노드의 델타를 줄입니다([timeout_list.h:44-60](zephyr/kernel/timeout_list.h#L44-L60)).

**제거 경로가 두 개**로 나뉘는 점이 중요합니다.

| 함수 | 용도 | 델타 처리 |
|---|---|---|
| `z_timeout_q_remove()` | abort 전용 | 제거 노드의 델타를 **후속 노드에 흡수**시킴 |
| `z_timeout_q_pop_due()` | announce 전용 | 머리 노드만, **흡수 없음** |

`pop_due`가 흡수하지 않아도 되는 이유가 주석에 있습니다([timeout_list.h:137-142](zephyr/kernel/timeout_list.h#L137-L142)): `advance()`가 이미 머리의 `dticks`를 0으로 만들었고 `curr_tick`도 그만큼 전진했으므로, 후속 노드의 저장된 델타가 그대로 유효합니다.

### 8.2 MINHEAP — 절대 틱 힙

[timeout_minheap.h](zephyr/kernel/timeout_minheap.h). 절대 만료 틱을 키로 쓰므로 **`advance()`가 아예 no-op**입니다([timeout_minheap.h:118-121](zephyr/kernel/timeout_minheap.h#L118-L121)) — 호출자의 `curr_tick += dt`가 전부입니다. 델타 리스트와 정확히 반대의 트레이드오프입니다.

용량이 `CONFIG_TIMEOUT_HEAP_MAX_ENTRIES`로 **고정**이며, 넘치면 `k_panic()`입니다.

```c
if (unlikely(min_heap_ref_push(&timeout_heap_inst, &to->heap_handle) != 0)) {
	k_panic();
}
```
[timeout_minheap.h:55-62](zephyr/kernel/timeout_minheap.h#L55-L62)

주석은 이를 "항상 설정 오류"로 규정합니다 — 커널 타임아웃을 우아하게 버릴 방법이 없기 때문입니다. 정적 용량 제약이 생기는 것이 이 백엔드의 실질적 비용입니다.

### 8.3 BUCKET — 창(window) + 오버플로

[timeout_bucket.h:11-42](zephyr/kernel/timeout_bucket.h#L11-L42). 향후 `CONFIG_TIMEOUT_BUCKET_LISTS`틱 이내는 **틱당 하나의 버킷 리스트**(O(1)), 그보다 먼 것은 절대 만료 기준 정렬된 **오버플로 리스트**(O(n))입니다.

영리한 부분은 **플래그 없이 두 표현을 구분**하는 것입니다: 버킷 항목은 `dticks`에 버킷 인덱스(`< BUCKET_LISTS`)를, 오버플로 항목은 절대 만료(`>= BUCKET_LISTS`)를 담아 값 범위가 겹치지 않게 했습니다.

창 불변식도 명시되어 있습니다 — 버킷은 `[cursor_base, cursor_base + BUCKET_LISTS)`를 덮고, `cursor_base`는 `BUCKET_LISTS` 정렬을 유지하므로 버킷 인덱스가 곧 `절대만료 & (BUCKET_LISTS - 1)`입니다.

마이그레이션이 **주기적이 아니라 실제 다음 이벤트 기준 온디맨드**라는 점도 중요합니다: 먼 타임아웃 하나만 있는 유휴 시스템이 주기적으로 깨지 않고 곧장 그 시점까지 잡니다.

### 8.4 WHEEL — 계층형 타이머 휠

[timeout_wheel.h:11-36](zephyr/kernel/timeout_wheel.h#L11-L36). soon(틱당 리스트 32개) / later(32틱 밴드 32개) / distant(정렬 델타 리스트) 3계층이며, `NUM_SOON_LISTS`틱마다 later 밴드를 soon으로 sift하고 distant에서 later를 채웁니다.

유일하게 **자체 announce 루프를 소유**합니다(§5.4). 노드의 `flags` 필드로 현재 어느 계층에 있는지 기록해, 제거 시 soon 점유 비트맵을 유지합니다.

---

## 9. `k_timer`와의 접점

[timer.c:72-133](zephyr/kernel/timer.c#L72-L133). `z_timer_expiration_handler()`가 §6.5의 이중 방어를 수행한 뒤, 주기 타이머면 스스로를 재등록합니다.

```c
#ifdef CONFIG_TIMEOUT_64BIT
	next = K_TIMEOUT_ABS_TICKS(k_uptime_ticks() + next.ticks);
#endif
	z_add_timeout(&timer->timeout, z_timer_expiration_handler, next);
```
[timer.c:100-112](zephyr/kernel/timer.c#L100-L112)

주석이 설명하는 기법이 §3의 `elapsed()` 동작에 직접 의존합니다: **커널 타임아웃 핸들러 안에서의 uptime은 실시간이 아니라 "예정된 이벤트 시각"을 반영**하므로(announce 중이면 `elapsed()`가 0), 인터럽트가 늦게 처리되어도 다음 만료가 "원래 실행됐어야 할 시점"부터의 정규 간격으로 계산됩니다. 지연이 누적되지 않는 값싼 보호책이며, **절대 타임아웃을 요구**합니다.

---

## 10. 기존 문서 서술의 재확인

TODO.md §3에 적은 대로, 02·03 문서의 서술은 `timeout_q.h` 헤더 주석에 근거했습니다. `timeout.c` 구현으로 대조한 결과입니다.

| 문서 | 서술 | 판정 |
|---|---|---|
| 02 §8 | "waker는 타임아웃을 abort하면서 superseded 표시를 남기고, 비행 중이던 핸들러는 뒤늦게 실행될 때 이 표시를 보고 물러난다" | ✅ 정확 ([timeout.c:256-262](zephyr/kernel/timeout.c#L256-L262), [sched.c:484-486](zephyr/kernel/sched.c#L484-L486)) |
| 02 §8 | "`z_unpend_all()`은 abort 결과와 무관하게 스레드를 레디로 만든다" | ✅ 정확. §6.4 (b) 부류 호출자 |
| 02 §9 | "`slice_reset()`의 `-1`은 `z_add_timeout()`의 `+1` 반올림 상쇄" | ✅ 정확. §4.2에서 기전까지 확인 |
| 03 §7.2 | "`-EAGAIN` 재시도 루프는 제3의 CPU에서 비행 중인 핸들러를 기다린다" | ✅ 정확. 단 **조건을 보강**: `-EAGAIN`은 `!this_cpu_announcing()`일 때만 나옵니다. 같은 CPU 케이스는 `-EINVAL` + superseded 표시로 처리됩니다 |
| 03 §7.3 | "락을 놓았다 다시 잡는 재시도" | ✅ 정확. 데드락 회피 근거는 §6.4에서 확정 |
| 01 §7 | "스레드는 동시에 두 개의 타임아웃을 가질 수 없다" | ✅ 정확. `__ASSERT(z_is_inactive_timeout(to), "")`가 [timeout.c:184](zephyr/kernel/timeout.c#L184)에서 이를 강제 |

**정정이 필요한 서술은 없었습니다.** 03 §7.2에 대해서는 위 표의 조건 보강 내용을 본 문서 §6.3이 담당하므로, 원문은 그대로 두고 상호 참조로 연결합니다.

---

## 11. 정리

- 타임아웃 서브시스템은 **프런트엔드(정책) / 백엔드(자료구조)** 로 깔끔히 갈라져 있고, 백엔드 헤더를 공유 상태 뒤에 include하는 방식으로 캡슐화와 직접 접근을 동시에 얻습니다.
- 시간의 **단일 진실**은 `curr_tick`이며, announce 창을 가로지르는 동안 `announce_remaining`이 실시간과의 간극을 메웁니다.
- `+1` 반올림은 "최소 N틱" 보장을 위한 것이고, **틱 경계(announce 중)에서만** 면제됩니다. 타임슬라이싱의 `-1`은 이 면제를 받지 못하는 위치에서 호출되기 때문에 필요합니다.
- **superseded 프로토콜**은 "핸들러 호출을 위해 락을 놓아야 한다"는 제약에서 파생된 전부입니다. 포인터 하위 비트 하나로 표현되며, 호출자를 free-safety 부류와 그 외로 나눠 각각 재시도/무시하게 합니다.
- `next_timeout()`의 `MAX_WAIT - 1`처럼, **값 하나가 의미(마감 없음)를 겸하는 API**에서 생기는 함정을 코드가 명시적으로 피해 갑니다.

## 12. 다음 문서

- `_sched_spinlock`과 IPI, 보조 CPU 부팅: `07-SMP와-스핀락.md` (예정)
