# 계층과 의존 구조

앞의 문서 01~07이 **"어떻게 동작하는가"**를 다뤘다면, 본 문서는 **"코드가 어떻게 나뉘고 누가 누구를 아는가"**를 다룹니다. 특정 빌드 구성과 무관한 분석이므로 타깃 적용표를 두지 않습니다.

---

## 1. 방법과 오차 범위

의존 관계는 인상이 아니라 **실제 `#include` 그래프에서 측정**했습니다. 스크립트가 트리 전체의 `.c`/`.h` **8,086개**를 스캔해 각 `#include`를 실제 헤더 위치로 해석하고 계층별로 집계합니다.

C 전처리기가 아니므로 완벽하지 않습니다. 해석 실패분은 버리지 않고 **세어서 보고**했습니다.

| 계층 | 미해결 include |
|---|---|
| `arch` | 60 |
| `kernel` | 119 |
| `lib` | 59 |
| `subsys` | 711 |
| `drivers` | 3,068 |
| `soc` | 1,845 |

`kernel`·`arch` 구간의 미해결 184건을 전수 확인한 결과 **전부 두 종류**였습니다.

1. **빌드 시각 생성 헤더** — `zephyr/offsets.h`(14), `zephyr/syscalls/*_mrsh.c`, `zephyr/zsr.h`
2. **툴체인 제공 헤더** — `stddef.h`, `xtensa/config/core-isa.h` 등

즉 커널·아키텍처 구간의 수치는 **트리 내부 의존을 빠뜨린 것이 아니므로 신뢰할 수 있습니다.** (확신도: high — 147종 전수 확인)

`drivers`·`soc`의 미해결분은 벤더 HAL(`modules/` 소재)과 생성된 devicetree 헤더로 추정되며, 해당 행의 절대값은 하한으로 읽어야 합니다. (확신도: medium — 전수 확인하지 않음)

---

## 2. 계층과 규모

| 계층 | C/H 파일 수 | 성격 |
|---|---|---|
| `include/zephyr` | 2,106 | **공개 API 표면** |
| `drivers` | 4,081 | 하드웨어 드라이버 |
| `subsys` | 1,879 | 서브시스템 (네트워킹, BT, FS…) |
| `soc` | 1,515 | SoC 지원 |
| `arch` | 344 | 아키텍처 포팅 |
| `boards` | 241 | 보드 지원 |
| `lib` | 189 | libc, 유틸리티 |
| **`kernel`** | **78** | **커널 코어** |

커널 코어가 트리 전체에서 **1% 미만**입니다. 01~07 문서가 다룬 것이 이 78개 파일입니다.

---

## 3. 계층 간 의존 실측

행이 포함하는 쪽, 열이 포함되는 쪽입니다.

| ↓포함 \ 대상→ | include/zephyr | arch | kernel | lib | subsys | drivers | soc |
|---|---|---|---|---|---|---|---|
| **arch** | 986 | 171 | **109** | 64 | · | · | 7 |
| **kernel** | 307 | **4** | 147 | 28 | · | · | · |
| **lib** | 325 | 1 | 2 | 218 | 1 | · | · |
| **subsys** | 7,486 | 2 | 27 | 1,806 | 4,438 | 3 | 87 |
| **drivers** | 19,616 | 11 | 1 | 2,004 | 114 | 2,255 | 1,059 |
| **soc** | 2,635 | 31 | 9 | 251 | 6 | 17 | 904 |

여기에 공개 헤더 트리 자체를 별도로 측정한 결과를 더합니다.

| ↓포함 \ 대상→ | include/zephyr | lib(libc) | soc | subsys | arch | **kernel** |
|---|---|---|---|---|---|---|
| **include/zephyr** | 3,265 | 643 | 18 | 18 | 3 | **0** |

`lib` 643건은 전수 확인 결과 **전부 `lib/libc`** — Zephyr의 minimal libc로 해석된 표준 C 헤더이며 계층 위반이 아닙니다.

### 즉시 읽히는 세 가지

**첫째, `include/zephyr`가 모든 계층의 최대 의존처입니다.** drivers는 19,616회, subsys는 7,486회 공개 헤더를 참조합니다. 계층 간 통신이 사실상 **전부 공개 API를 경유**합니다.

**둘째, 공개 헤더는 커널 내부를 전혀 모릅니다.** `include/zephyr → kernel = 0`. 공개 API 표면이 커널 구현에 오염되지 않았다는 뜻이며, 이것이 커널 내부를 자유롭게 바꿀 수 있게 하는 근거입니다.

**셋째, `kernel → arch`가 단 4회입니다.** 다음 절의 주제입니다.

---

## 4. 핵심 발견 — 의존성 역전

상식적으로는 커널이 아키텍처 계층을 호출하니 `kernel → arch` 의존이 클 것 같습니다. 실측은 반대입니다.

```
kernel  ──→  arch          4회
arch    ──→  kernel/include  109회
```

**커널은 아키텍처를 거의 include하지 않고, 아키텍처가 커널 내부 헤더를 대량으로 include합니다.**

이유는 **계약의 소유권**에 있습니다. 커널이 필요로 하는 아키텍처 함수들의 프로토타입이 `arch/`가 아니라 **커널이 소유한 헤더**에 선언되어 있습니다.

```
kernel/include/kernel_arch_interface.h     ← 커널이 소유하는 "요구사항"
        △ 구현 제공
        │
arch/<ARCH>/include/kernel_arch_func.h     ← 아키텍처가 채워 넣는 "구현"
```

커널 코드는 `kernel_arch_interface.h`(자기 것)만 include하면 되고, 실제 아키텍처 헤더를 직접 참조할 일이 거의 없습니다. 반대로 아키텍처 코드는 계약을 이행하기 위해 커널 내부 자료구조를 알아야 하므로 `kernel/include/`를 109회 참조합니다.

**상위 계층이 인터페이스를 소유하고 하위 계층이 구현하는** 전형적인 의존성 역전입니다. 그 결과 새 아키텍처를 추가할 때 커널 코드는 손대지 않습니다. (확신도: high — 두 계약 헤더의 상호 참조 주석과 실측 수치가 일치)

---

## 5. 아키텍처 계약의 이중 구조

계약이 하나가 아니라 **둘로 쪼개져** 있고, 두 파일이 서로를 가리키며 분할 규칙을 명시합니다.

### 공개 계약 — `include/zephyr/arch/arch_interface.h`

> **Internal kernel APIs with public scope**
>
> 공개 커널 API 중 인라인 함수로 구현되어 **아키텍처별 API를 호출해야 하는 것들**의 프로토타입이 여기 있다. 그런 식으로 쓰이지 않는 아키텍처 API는 `kernel/include/kernel_arch_interface.h`로 간다.

### 비공개 계약 — `kernel/include/kernel_arch_interface.h`

> **Internal kernel APIs implemented at the architecture layer.**
>
> 모든 아키텍처별 정의가 여기 있는 것은 아니다. **공개 함수와 매크로가 사용하는 API**는 `include/zephyr/arch/arch_interface.h`에 정의된다.
>
> 여기 프로토타입된 인라인 함수의 구현은 `arch/ARCH/include/kernel_arch_func.h`가 제공할 것으로 기대된다.

분할 기준이 명확합니다.

| | 위치 | 기준 |
|---|---|---|
| 공개 계약 | `include/zephyr/arch/` | **애플리케이션 코드에 인라인 전개되는** 공개 매크로·인라인 함수가 호출함 |
| 비공개 계약 | `kernel/include/` | 커널 내부에서만 호출됨 |

공개 헤더에 있는 인라인 함수가 `arch_*()`를 호출하면, 그 프로토타입도 **애플리케이션이 볼 수 있는 곳**에 있어야 컴파일됩니다. 순수한 설계 취향이 아니라 **인라인 전개가 강제하는 구조**입니다.

비공개 계약이 공개 계약을 `#include`하므로(`kernel_arch_interface.h`가 `<zephyr/arch/arch_interface.h>`를 포함), 커널은 두 계약을 모두 보고 애플리케이션은 공개 계약만 봅니다.

이는 [01-커널-코어-자료구조.md](1.zephyr분석/01-커널-코어-자료구조.md) §1에서 본 `kernel_structs.h`의 계층 규칙과 같은 계열의 장치입니다 — **어느 헤더가 무엇에 의존해도 되는지를 파일 주석에 명문화**하는 방식입니다.

---

## 6. 경계를 강제하는 것 — 빌드 시스템

계층 규칙이 관례에 그치지 않고 **컴파일 단계에서 강제**됩니다.

### 전역 include 경로는 공개 트리뿐

```cmake
zephyr_include_directories(
  include                                  # ← include/zephyr/**
  ${PROJECT_BINARY_DIR}/include/generated
  ${USERINCLUDE}
  ${STDINCLUDE}
)
```
[CMakeLists.txt:128-133](zephyr/CMakeLists.txt#L128-L133)

모든 코드가 공유하는 include 경로에 **`kernel/include`도 `arch/*/include`도 없습니다.**

### 내부 헤더는 타깃별로 요청해야 열림

기본 소유자 둘은 무조건 부여받습니다.

```cmake
# kernel/CMakeLists.txt:188-191  — 커널 타깃에 PRIVATE
target_include_directories(kernel PRIVATE
  ${ZEPHYR_BASE}/kernel/include
  ${ARCH_DIR}/${ARCH}/include
  )
```

```cmake
# arch/CMakeLists.txt:8-11  — arch 서브트리에 디렉터리 스코프
include_directories(
  ${ZEPHYR_BASE}/kernel/include
  ${ARCH_DIR}/${ARCH}/include
  )
```
[kernel/CMakeLists.txt:188-191](zephyr/kernel/CMakeLists.txt#L188-L191), [arch/CMakeLists.txt:8-11](zephyr/arch/CMakeLists.txt#L8-L11)

그 외 컴포넌트는 **자기 `CMakeLists.txt`에서 명시적으로 요청**해야 합니다. 두 가지 형태가 쓰입니다.

```cmake
# subsys/portability/posix/options/CMakeLists.txt:187-188  — 라이브러리 범위
zephyr_library_include_directories(
  ${ZEPHYR_BASE}/kernel/include
```

```cmake
# subsys/tracing/CMakeLists.txt:65-67  — Kconfig 조건부
zephyr_include_directories_ifdef(
  CONFIG_TRACING
  ${ZEPHYR_BASE}/kernel/include
```
[posix/options/CMakeLists.txt:187](zephyr/subsys/portability/posix/options/CMakeLists.txt#L187), [tracing/CMakeLists.txt:65](zephyr/subsys/tracing/CMakeLists.txt#L65)

따라서 **"기계적으로 불가능"이 아니라 "요청하지 않으면 닫혀 있음"**입니다. 요청하지 않은 코드가 `#include <ksched.h>`를 쓰면 헤더를 찾지 못해 빌드가 실패하지만, 요청한 컴포넌트에서는 정상 동작합니다.

§3 행렬의 두 행이 서로 다른 이유로 낮습니다.

| | 값 | 이유 |
|---|---|---|
| `drivers → kernel` | 1 | `drivers/` 하위 `CMakeLists.txt` 중 이 경로를 요청하는 것이 **0개** — 실제로 닫혀 있음 |
| `subsys → kernel` | 27 | 요청한 곳이 **9개** 있음 — 닫혀 있어서가 아니라 필요한 곳이 적어서 |

예컨대 `subsys/portability/posix/options/semaphore.c:16`은 커널 내부 헤더 `<wait_q.h>`를 포함하며 정상 빌드됩니다. 해당 `CMakeLists.txt`가 경로를 요청했기 때문입니다.

> **정정 이력**: 이 절은 처음에 "내부 헤더는 두 곳에만 열린다", "`subsys/`의 코드가 커널 내부 헤더를 쓰면 빌드가 실패한다"고 서술했습니다. 독립 검증(2026-08-06)에서 `zephyr_library_include_directories()` 경로를 누락한 오류로 확인되어 위와 같이 고쳤습니다. §7이 처음부터 opt-in 24곳을 정확히 기술하고 있었으므로, 같은 문서 안에서 §6과 §7이 모순된 상태였습니다. (`drivers → kernel = 1`은 §1의 해석기 한계에 따른 오해석으로 보입니다 — 요청한 `CMakeLists.txt`가 0개이므로 실제 의존은 없어야 합니다.)

`arch/CMakeLists.txt`가 `-D__ZEPHYR_SUPERVISOR__`를 함께 정의하는 것도 같은 맥락으로, 아키텍처 코드가 특권 문맥임을 컴파일 시각에 표시합니다.

---

## 7. 경계를 넘는 24곳

닫힌 문에는 **명시적 opt-in**이 있습니다. 내부 헤더가 필요한 컴포넌트는 자기 `CMakeLists.txt`에 경로를 직접 추가합니다.

전체 71곳 중 **47곳이 `tests/`·`samples/`**이고, 프로덕션 코드는 **24곳**입니다.

| 부류 | 대상 | 왜 내부가 필요한가 |
|---|---|---|
| 소유자 | `kernel`, `arch` | 계약의 양쪽 당사자 |
| **커널 관찰자** | `subsys/tracing`, `subsys/tracing/ctf`, `subsys/debug/coredump` | 스레드 상태·스케줄링 이벤트를 들여다봐야 함 |
| **커널 에뮬레이터** | `subsys/portability/posix/options`, `cmsis_rtos_v1`, `cmsis_rtos_v2` | 다른 RTOS API를 Zephyr 원시 연산 위에 재구현 |
| **커널 확장** | `subsys/mem_mgmt/demand_paging/{backing_store,eviction}` | 커널 페이징 훅을 구현 |
| 저수준 유틸 | `lib/os`, `lib/utils` | `p4wq` 등이 스케줄러 내부를 사용 |
| 플랫폼 | `soc/nxp/*`(8), `soc/ti/lm3s6965`, `soc/native/inf_clock`, `boards/native/*`(2) | 시뮬레이션·멀티코어 부팅 등 |
| 그 외 | `subsys/net/ip` | — |

**부류가 일관됩니다.** 커널을 관찰하거나, 흉내 내거나, 확장하는 컴포넌트들입니다. 이들은 "커널 API를 쓰는 사용자"가 아니라 **커널과 같은 층위에서 협력하는 코드**입니다.

`subsys/net/ip`만 이 분류에 잘 맞지 않아, 역사적 잔재이거나 성능상의 이유일 가능성이 있습니다. (확신도: low — 실제 사용처를 확인하지 않음)

**opt-in의 가장 큰 가치는 가시성**입니다. 어떤 컴포넌트가 커널 내부에 결합되어 있는지가 `CMakeLists.txt` 한 줄로 드러나며, `grep` 한 번으로 전수 파악됩니다.

---

## 8. 숨은 계층 — 빌드 시각 생성 헤더

§1의 미해결 include가 드러낸 것은 **소스 트리에 존재하지 않는 계층**입니다.

| 생성물 | 내용 | 생성 시점 |
|---|---|---|
| `zephyr/offsets.h` | 구조체 멤버 오프셋 (어셈블리용) | `gen_offset_header.py` |
| `zephyr/syscalls/*_mrsh.c` | 시스템 콜 마샬링 함수 | syscall 생성기 |
| `autoconf.h` | Kconfig 결과 | Kconfig |
| `devicetree_generated.h` | DTS 결과 | `gen_defines.py` |

`offsets.h`의 생성 경로가 특히 흥미롭습니다. `zephyr_constants_library()`가 **C 파일 하나를 오브젝트로 컴파일한 뒤 그 오브젝트에서 상수를 추출**해 헤더를 만듭니다([extensions.cmake:1994-2008](zephyr/cmake/modules/extensions.cmake#L1994-L2008)). 그리고 그 라이브러리에도 `kernel/include`가 붙습니다 — 오프셋을 뽑으려면 커널 구조체를 봐야 하기 때문입니다.

어셈블리 코드가 `struct k_thread`의 필드 위치를 알아야 하는데 C 헤더를 파싱할 수 없으므로, **컴파일러에게 계산을 시켜 결과만 받아오는** 우회로입니다. [01-커널-코어-자료구조.md](1.zephyr분석/01-커널-코어-자료구조.md) §2에서 본 "ARC의 오프셋 인코딩 제약 때문에 필드 순서가 고정된다"는 서술이 이 계층과 맞물립니다.

---

## 9. 나머지 계층의 방향성

**`drivers ↔ soc`는 사실상 단방향**입니다 — `drivers → soc` 1,059 대 `soc → drivers` 17. 드라이버가 SoC 정의를 참조하지, 그 반대는 드뭅니다.

**`subsys`는 내부 응집이 높습니다** — `subsys → subsys` 4,438로 자기 계층 참조가 공개 헤더(7,486) 다음으로 큽니다. 네트워킹·블루투스처럼 큰 서브시스템 내부의 모듈 간 참조입니다.

**`drivers → subsys`가 114로 낮습니다.** 드라이버가 서브시스템에 의존하지 않는다는 뜻이며, 드라이버를 서브시스템과 독립적으로 재사용할 수 있게 하는 구조입니다.

**순환은 발견되지 않았습니다** — `kernel ↔ arch`가 양방향으로 보이지만, §4에서 본 대로 실제로는 "계약 소유"와 "구현 제공"의 관계이며 커널 쪽 4건은 `kernel_arch_func.h` 참조입니다. (확신도: medium — 파일 단위 순환은 검사하지 않았고 계층 단위로만 판단)

---

## 10. 정리

```
        ┌─────────────────────────────────────────────┐
        │          include/zephyr  (공개 API)          │  ← 모든 계층의 최대 의존처
        │          kernel 의존 = 0                     │     커널 내부를 모름
        └─────────────────────────────────────────────┘
              ▲          ▲          ▲          ▲
              │          │          │          │
        ┌─────┴───┐ ┌────┴───┐ ┌────┴───┐ ┌────┴────┐
        │ drivers │ │ subsys │ │  lib   │ │   soc   │
        └─────────┘ └────┬───┘ └────────┘ └─────────┘
                         ┊ opt-in 24곳 (CMakeLists 명시)
                         ▼
        ┌─────────────────────────────────────────────┐
        │  kernel/include  (커널 내부 계약)             │
        │  ── kernel_arch_interface.h ──┐              │
        └───────────────────────────────┼─────────────┘
                                        │ 구현 요구
                                        ▼
        ┌─────────────────────────────────────────────┐
        │  arch/<ARCH>/include/kernel_arch_func.h      │
        │  (커널 내부를 109회 참조)                     │
        └─────────────────────────────────────────────┘
```

- **공개 API가 유일한 공용 통로**입니다. 계층 간 통신이 거의 전부 `include/zephyr`를 경유하고, 그 트리는 커널 내부를 전혀 모릅니다.
- **의존성이 역전되어 있습니다.** 커널이 아키텍처 계약을 소유하고 아키텍처가 구현합니다. 그래서 `kernel → arch`는 4회에 그칩니다.
- **계약이 둘로 쪼개진 것은 인라인 전개 때문**입니다. 애플리케이션에 인라인되는 코드가 호출하는 `arch_*`는 공개 헤더에 있어야만 합니다.
- **경계는 빌드 시스템이 관리합니다.** 내부 헤더는 전역 include 경로에 없어 **기본이 닫힘**이고, 필요한 타깃이 자기 `CMakeLists.txt`에서 요청해야 열립니다. 요청하지 않은 코드에서는 컴파일 오류가 되지만, 넘을 수 없는 벽은 아닙니다.
- **예외는 숨겨지지 않고 선언됩니다.** 24곳의 opt-in이 `CMakeLists.txt`에 남아, 커널에 결합된 컴포넌트가 무엇인지 한눈에 드러납니다.
- **생성 헤더가 보이지 않는 계층을 이룹니다.** 어셈블리가 C 구조체를 알아야 하는 문제를 "컴파일러에게 계산시켜 결과를 받는" 방식으로 우회합니다.

## 11. 다음 문서

- 이 구조 위에서 확장이 실제로 어떻게 이뤄지는가: `09-확장-메커니즘.md` (예정)

## 부록: 재현 방법

본 문서의 수치는 저장소에 포함된 스크립트로 재현할 수 있습니다.

```sh
python3 scripts/include_graph.py     # §3의 의존 행렬, §1의 미해결 집계
```

사용법과 해석기의 한계는 [scripts/README.md](1.zephyr분석/scripts/README.md)에 있습니다. §2의 파일 수는 `find <dir> -name "*.c" -o -name "*.h" | wc -l`로 셉니다.
