# 확장 메커니즘

[08-계층과-의존-구조.md](1.zephyr분석/08-계층과-의존-구조.md)가 **"코드가 어떻게 나뉘는가"**였다면, 본 문서는 **"그 경계에 어떻게 끼어들 수 있는가"**를 다룹니다. 특정 빌드 구성과 무관하므로 타깃 적용표를 두지 않습니다.

---

## 1. 개관

Zephyr가 쓰는 확장 장치는 일곱 가지이며, **결합 시점**과 **구현하지 않았을 때의 결과**로 성격이 갈립니다.

| # | 메커니즘 | 결합 시점 | 규모 | 미구현 시 |
|---|---|---|---|---|
| 2 | `arch_*` 계약 | 컴파일·링크 | 선언 **120개** | **링크 오류** |
| 3 | `__weak` 심볼 | 링크 | 정의 **98개 파일** | 기본 동작으로 대체 |
| 4 | Kconfig 백엔드 교체 | 컴파일 | 스케줄러 3·타임아웃 4·대기큐 2종 | 해당 없음 (택일) |
| 5 | 플랫폼 훅 | 컴파일·링크 | 훅 **8개** | 끄면 비용 0 / 켜면 링크 오류 |
| 6 | iterable section | **링크** | 순회 **241곳** | 항목이 없을 뿐 |
| 7 | 디바이스 모델 | 빌드(DTS)·링크 | `DEVICE_API` **2,012 파일** | 인스턴스 0개 |
| 8 | 시스템 콜 생성 | 빌드(코드 생성) | `__syscall` **673개** | 해당 없음 |
| 9 | LLEXT | **런타임** | `EXPORT_SYMBOL` 68건 | 심볼 해석 실패 |

측정 방법은 §11에 적었습니다.

---

## 2. 계약 기반 확장 — `arch_*`

새 아키텍처를 얹는 통로입니다. 커널이 필요한 함수 목록을 선언하고 아키텍처가 채웁니다.

| 계약 | 위치 | 선언 수 |
|---|---|---|
| 공개 | `include/zephyr/arch/arch_interface.h` | 75 |
| 비공개 | `kernel/include/kernel_arch_interface.h` | 45 |

구현은 `arch/<ARCH>/include/kernel_arch_func.h`와 `arch/<ARCH>/core/*.c`가 제공합니다. 계약이 **왜 둘로 나뉘는지**(인라인 전개 때문)와 그로 인한 의존성 역전은 [08](1.zephyr분석/08-계층과-의존-구조.md) §4~5에서 다뤘습니다.

**특징은 "빠뜨리면 반드시 터진다"**는 것입니다. 커널이 호출하는데 아키텍처가 구현하지 않으면 링크 단계에서 미정의 심볼로 실패합니다. 새 포팅을 할 때 무엇이 남았는지를 링커가 알려주는 구조입니다.

---

## 3. `__weak` 심볼 — 기본값을 제공하는 확장

정의를 미리 제공해 두고, 필요하면 덮어쓰게 하는 방식입니다.

| 계층 | `__weak` 정의를 담은 파일 |
|---|---|
| `subsys` | 30 |
| `arch` | 28 |
| `drivers` | 24 |
| `kernel` | 8 |
| `lib` | 8 |

커널의 대표 사례입니다.

| 심볼 | 기본 동작 | 덮어쓰는 쪽 |
|---|---|---|
| `arch_spin_relax()` | `arch_nop()` ([idle.c:97](zephyr/kernel/idle.c#L97)) | ARM64·Xtensa·RISC-V |
| `k_sys_fatal_error_handler()` | 정책 기본값 ([fatal.c:37](zephyr/kernel/fatal.c#L37)) | 애플리케이션 |
| `z_early_rand_get()` | LCG 의사난수 ([init.c:497](zephyr/kernel/init.c#L497)) | 엔트로피 드라이버 보유 플랫폼 |
| `main()` | `arch_nop()` 후 0 반환 ([main_weak.c:24](zephyr/kernel/main_weak.c#L24)) | 애플리케이션 |

`arch_spin_relax()`가 좋은 예입니다. 기본은 단순 `nop`이지만 ARM64는 FPU 플러시 데드락을 피하는 코드로 덮어씁니다([05-SMP와-스핀락.md](1.zephyr분석/05-SMP와-스핀락.md) §9). **아키텍처가 필요할 때만 개입**하고, 아니면 커널이 준 기본값이 그대로 쓰입니다.

### `main_weak.c` — 메커니즘의 함정이 파일 하나를 낳은 사례

weak `main()`이 왜 **자기만의 파일**에 격리되어 있는지가 주석에 있습니다([main_weak.c:8-20](zephyr/kernel/main_weak.c#L8-L20)).

> 링커는 weak 함수가 **그 심볼을 호출하는 오브젝트 안에 있는지 여부에 따라 다르게 취급**할 수 있다.
>
> 예를 들어 armlink에서는 weak 심볼이 그것을 참조하는 오브젝트 안에 있으면 weak 심볼이 쓰인다. 그 결과 weak와 strong 심볼이 **모두 사용되어 다중 정의**가 된다.
>
> GNU ld에게는 weak 심볼이 같은 오브젝트에 있든 없든 상관없다. GNU ld는 항상 strong 버전에 링크한다.
>
> weak `main` 심볼을 독립 파일에 두면 여러 링커에서 올바르게 취급되는 것이 보장된다.

**weak 심볼의 동작이 링커 구현에 따라 다르다**는 것이 핵심입니다. Zephyr가 GNU ld와 armlink를 모두 지원해야 하므로, 이식성을 위해 파일을 분리했습니다. 확장 메커니즘 자체의 이식성 문제가 파일 배치를 결정한 드문 사례입니다.

---

## 4. Kconfig 백엔드 교체 — 알고리즘을 통째로 바꾸는 확장

같은 인터페이스에 **여러 구현**을 두고 빌드 구성으로 택일합니다. 앞선 문서들이 다룬 세 사례입니다.

| 대상 | 백엔드 | 문서 |
|---|---|---|
| 레디 큐 | SIMPLE / SCALABLE / MULTIQ | [02](1.zephyr분석/02-스케줄러.md) §3 |
| 타임아웃 큐 | DLIST / BUCKET / WHEEL / MINHEAP | [04](1.zephyr분석/04-타임아웃과-시스템-클럭.md) §8 |
| 대기 큐 | SIMPLE / SCALABLE | [01](1.zephyr분석/01-커널-코어-자료구조.md) §6 |

교체 **방식**은 둘로 갈립니다.

**(a) 매크로 디스패치** — 인터페이스 이름을 구현 함수에 매핑합니다.

```c
#if defined(CONFIG_SCHED_SIMPLE)
#define _priq_run_add		z_priq_simple_add
#elif defined(CONFIG_SCHED_SCALABLE)
#define _priq_run_add		z_priq_rb_add
#elif defined(CONFIG_SCHED_MULTIQ)
#define _priq_run_add		z_priq_mq_add
#endif
```
[priority_q.h:13-46](zephyr/kernel/include/priority_q.h#L13-L46)

**(b) 헤더 교체** — 구현 파일 자체를 갈아끼웁니다.

```c
#if defined(CONFIG_TIMEOUT_BACKEND_MINHEAP)
#include "timeout_minheap.h"
#elif defined(CONFIG_TIMEOUT_BACKEND_WHEEL)
#include "timeout_wheel.h"
...
#else /* CONFIG_TIMEOUT_BACKEND_DLIST */
#include "timeout_list.h"
#endif
```
[timeout.c:108-116](zephyr/kernel/timeout.c#L108-L116)

(b)가 더 강력합니다. 백엔드가 프런트엔드의 공유 상태(`curr_tick` 등)에 직접 접근할 수 있고, 심지어 **announce 루프 자체를 가져갈** 수도 있습니다(`_TIMEOUT_BACKEND_OWNS_ANNOUNCE`, [04](1.zephyr분석/04-타임아웃과-시스템-클럭.md) §5.4). 대신 그 대가로 헤더가 특정 번역 단위 전용이 됩니다.

**이 메커니즘의 성격은 다른 여섯과 다릅니다** — 외부가 끼어드는 통로가 아니라, **커널 스스로가 자기 구현을 교체 가능하게 만든** 장치입니다. 그래서 "미구현" 상태가 존재하지 않습니다.

---

## 5. 플랫폼 훅 — Kconfig 게이트 방식

SoC와 보드가 부팅 경로에 개입하는 지점이며, 총 8개입니다.

| 훅 | 시점 |
|---|---|
| `soc_early_reset_hook()` | 데이터 RAM 초기화 전, 리셋 벡터 초입 |
| `soc_reset_hook()` | 리셋 벡터 |
| `soc_prep_hook()` | 리셋 벡터 이후 |
| `soc_early_init_hook()` / `board_early_init_hook()` | 커널·디바이스 초기화 전 |
| `soc_late_init_hook()` / `board_late_init_hook()` | `POST_KERNEL` 이후 |
| `soc_per_core_init_hook()` | `arch_kernel_init()` 내부 |

호출 위치는 [07-부팅과-초기화.md](1.zephyr분석/07-부팅과-초기화.md) §3~4에 나옵니다.

주목할 점은 **`__weak`을 쓰지 않는다**는 것입니다.

```c
#if defined(CONFIG_SOC_EARLY_RESET_HOOK) || defined(__DOXYGEN__)
void soc_early_reset_hook(void);
#else
#define soc_early_reset_hook() do { } while (0)
#endif
```
[hooks.h:22-34](zephyr/include/zephyr/platform/hooks.h#L22-L34)

Kconfig가 켜져 있으면 **함수 선언**, 꺼져 있으면 **빈 매크로**입니다.

### 왜 `__weak`이 아닌가

| | `__weak` 방식 | Kconfig 게이트 방식 |
|---|---|---|
| 미사용 시 비용 | 빈 함수 호출 (링커가 제거 못 할 수도) | **완전히 0** — 호출 자체가 사라짐 |
| 켰는데 구현 안 함 | 조용히 no-op | **링크 오류** |
| 구현 여부 파악 | 링크 결과를 봐야 함 | **Kconfig에 드러남** |

부팅 초기 훅은 **"켰다면 반드시 구현되어야 하는"** 성격입니다. 조용히 아무 일도 안 하면 진단이 어렵습니다. 반대로 `arch_spin_relax()`처럼 **"없어도 정상 동작하는"** 것은 weak이 적합합니다.

**같은 "선택적 구현"이라도 실패 모드에 따라 다른 메커니즘을 고른다**는 것이 이 대비의 요점입니다.

---

## 6. iterable section — 등록과 발견의 분리

가장 널리 쓰이는 장치입니다. **순회 지점만 241곳**입니다.

```c
#define STRUCT_SECTION_ITERABLE(struct_type, varname) \
	TYPE_SECTION_ITERABLE(struct struct_type, varname, struct_type, varname)
```
[iterable_sections.h:244](zephyr/include/zephyr/sys/iterable_sections.h#L244)

원리는 단순합니다. 같은 타입의 인스턴스를 **전용 링커 섹션**에 모으고, 링커가 만든 시작·끝 심볼 사이를 배열처럼 순회합니다.

```
[여러 파일에 흩어진 정의]           [링커]              [순회 코드]
  STRUCT_SECTION_ITERABLE(...)  ──→  섹션에 수집  ──→  STRUCT_SECTION_FOREACH
```

**핵심 가치는 커널이 등록자를 모른다는 점**입니다. 새 항목을 추가할 때 목록이 있는 파일을 고칠 필요가 없습니다. 링크에 포함되기만 하면 자동으로 발견됩니다.

사용처 일부:

| 타입 | 용도 | 관련 문서 |
|---|---|---|
| `init_entry` | `SYS_INIT` / 디바이스 초기화 | [07](1.zephyr분석/07-부팅과-초기화.md) §5 |
| `_static_thread_data` | 정적 스레드 | [07](1.zephyr분석/07-부팅과-초기화.md) §7.4 |
| `device` | 디바이스 인스턴스 | §7 |
| `k_obj_core_desc` | 오브젝트 코어 | — |
| `net_buf_pool`, `net_if` | 네트워킹 | — |
| `zbus_observer`, `zbus_channel_observation_mask` | zbus | — |
| `llext_const_symbol` | LLEXT 심볼 테이블 | §9 |
| `k_msgq`, `k_mem_slab`, `k_heap` | 커널 오브젝트 목록 | — |

`init_entry`는 여기에 **정렬**까지 얹은 사례입니다 — 섹션 이름에 레벨·우선순위를 박아 넣어 링커가 순서까지 결정하게 합니다([07](1.zephyr분석/07-부팅과-초기화.md) §5.2).

---

## 7. 디바이스 모델 — 가장 큰 확장 표면

`DEVICE_API`를 쓰는 파일이 **2,012개**로, 트리에서 가장 넓은 확장 지점입니다.

```c
#define DEVICE_API(_class, _name) const STRUCT_SECTION_ITERABLE(Z_DEVICE_API_TYPE(_class), _name)
```
[device.h:1371](zephyr/include/zephyr/device.h#L1371)

**메커니즘이 합성됩니다** — 디바이스 API는 §6의 iterable section 위에 세워져 있습니다. 별도 장치가 아니라 기존 장치의 특수화입니다.

여기에 devicetree가 결합해 **인스턴스 개수까지 빌드 시각에 결정**됩니다.

```
DTS (보드가 기술)  →  DT_INST_FOREACH_STATUS_OKAY  →  드라이버 인스턴스 N개 생성
```

`DT_INST_FOREACH_STATUS_OKAY`를 쓰는 드라이버가 **1,884개**입니다. 드라이버 코드는 "이 컴패터블에 해당하는 노드마다 인스턴스를 만들라"고만 쓰고, 실제 개수와 주소·IRQ는 보드의 DTS가 정합니다.

세 단계가 겹쳐 있는 구조입니다.

| 무엇을 | 누가 | 언제 |
|---|---|---|
| API 형태(함수 포인터 집합) | 서브시스템 | 컴파일 |
| 구현 | 드라이버 | 컴파일 |
| 인스턴스 개수·자원 | 보드 DTS | 빌드 |

---

## 8. 시스템 콜 — 코드 생성 기반 확장

커널 API를 사용자 모드에 노출하는 통로입니다.

| 항목 | 수 |
|---|---|
| `__syscall` 선언 (공개 헤더) | **673** |
| `z_impl_*` 구현 | 542 |
| `z_vrfy_*` 검증 래퍼 | 526 |

작성자는 `__syscall`로 선언하고 `z_impl_`(실제 구현)과 `z_vrfy_`(권한 검사)를 쓰기만 하면, **마샬링 코드는 생성기가 만듭니다**. 앞선 문서들에서 반복해 본 아래 패턴이 그것입니다.

```c
#include <zephyr/syscalls/k_sem_take_mrsh.c>
```

이 파일들은 소스 트리에 없고 빌드 시각에 생성됩니다 — [08](1.zephyr분석/08-계층과-의존-구조.md) §8의 "숨은 계층"입니다. 실제로 08의 미해결 include 조사에서 `zephyr/syscalls/*_mrsh.c`가 대량으로 잡힌 이유입니다.

`z_impl_`과 `z_vrfy_`의 개수 차이(542 대 526)는 **일부 API가 검증 래퍼 없이 커널 전용**임을 시사합니다. (확신도: medium — 개수 차이로부터의 추론이며 개별 확인은 하지 않음)

---

## 9. LLEXT — 유일한 런타임 확장

앞의 여섯 가지는 모두 빌드·링크 시각에 결합됩니다. `subsys/llext/`만 **런타임에** ELF 오브젝트를 적재합니다.

커널이 확장 모듈에 노출하는 심볼은 `EXPORT_SYMBOL`로 명시되며 **68건**입니다. 이 매크로 역시 §6의 iterable section(`llext_const_symbol`)으로 구현되어 있습니다 — 또 하나의 합성 사례입니다.

앞선 문서에서 마주친 사례가 있습니다. `z_timeout_remaining()`과 `z_spin_lock_valid()` 등이 `EXPORT_SYMBOL`로 내보내집니다([timeout.c:296](zephyr/kernel/timeout.c#L296), [spinlock_validate.c:38](zephyr/kernel/spinlock_validate.c#L38)). 커널 내부 함수인데도 확장 모듈이 쓸 수 있도록 열어둔 것입니다.

**노출 목록이 명시적**이라는 점이 중요합니다. 68개만 열려 있으므로, 확장 모듈이 커널의 임의 내부에 접근할 수 없습니다. [08](1.zephyr분석/08-계층과-의존-구조.md) §7의 opt-in과 같은 철학입니다 — **기본 차단, 명시적 개방.**

---

## 10. 메커니즘 선택의 규칙성

일곱 가지를 두 축으로 놓으면 선택 기준이 드러납니다.

```
                    미구현 시 조용히 진행 ────────────→ 미구현 시 실패
                              │                              │
  런타임 결합                 │                          LLEXT
                              │                              │
  링크 결합            __weak │  iterable section        arch_* 계약
                              │                              │
  컴파일 결합                 │  Kconfig 백엔드 교체      플랫폼 훅
                              │                          (Kconfig 게이트)
```

읽어낼 수 있는 규칙:

1. **"없어도 정상"이면 weak, "켰으면 반드시 있어야" 하면 Kconfig 게이트.** 같은 선택적 구현이라도 실패 모드가 다르면 다른 장치를 씁니다(§5).
2. **"목록"이 필요하면 iterable section.** 커널이 등록자를 몰라도 되게 만드는 것이 목적이며, 그래서 가장 널리 쓰입니다(241곳).
3. **"반드시 채워야 하는 구멍"이면 계약.** 링커가 미완성을 알려주는 것이 이 방식의 값어치입니다.
4. **"내부 구현의 교체"는 Kconfig.** 외부 확장이 아니라 커널이 스스로에게 준 유연성입니다.
5. **메커니즘은 합성됩니다.** 디바이스 모델과 LLEXT 심볼 테이블이 모두 iterable section 위에 있습니다. 새 장치를 만들기보다 기존 장치를 특수화합니다.

---

## 11. 정리

- **확장 지점은 대부분 빌드·링크 시각에 닫힙니다.** 런타임 결합은 LLEXT 하나뿐이며, 나머지는 전부 정적입니다. 런타임 디스패치 비용을 지불하지 않겠다는 임베디드 RTOS의 선택입니다.
- **링커가 프레임워크 역할을 합니다.** 목록 수집(iterable section), 순서 결정(`init_entry` 섹션 이름 정렬), 기본값 대체(`__weak`), 미완성 검출(계약)이 모두 링커의 일입니다. 런타임 등록 테이블이 거의 없습니다.
- **실패 모드가 메커니즘을 고릅니다.** `__weak`과 Kconfig 게이트의 대비가 그 원칙을 가장 선명하게 보여줍니다.
- **개방은 명시적입니다.** LLEXT의 `EXPORT_SYMBOL` 68건, 내부 헤더 opt-in 24곳([08](1.zephyr분석/08-계층과-의존-구조.md) §7) 모두 "기본 차단, 필요한 것만 선언"입니다.
- **메커니즘 자체의 이식성도 설계 대상입니다.** weak 심볼의 링커별 동작 차이 때문에 파일이 하나 분리된 `main_weak.c`가 그 증거입니다.

## 12. 측정 방법

| 수치 | 명령 |
|---|---|
| `arch_*` 선언 수 | `grep -cE "^[a-zA-Z].*\barch_[a-z_0-9]+\(" include/zephyr/arch/arch_interface.h` |
| `__weak` 파일 수 | `grep -rc "__weak" <dir> --include="*.c" \| grep -v ':0' \| wc -l` |
| iterable section 순회 | `grep -rn "STRUCT_SECTION_FOREACH" kernel subsys --include="*.c" \| wc -l` |
| `DEVICE_API` 파일 | `grep -rl "DEVICE_API(" drivers/ include/ \| wc -l` |
| `__syscall` 선언 | `grep -rh "^__syscall" include/zephyr/ --include="*.h" \| wc -l` |
| `EXPORT_SYMBOL` | `grep -rh "EXPORT_SYMBOL(" kernel/ lib/ subsys/ --include="*.c" \| wc -l` |

수치는 분석 시점(2026-08-06) 작업 트리 기준입니다.
