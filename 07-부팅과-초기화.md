# 부팅과 초기화

분석 대상: [zephyr/arch/arm64/core/reset.S](zephyr/arch/arm64/core/reset.S), [zephyr/arch/arm64/core/reset.c](zephyr/arch/arm64/core/reset.c), [zephyr/arch/arm64/core/prep_c.c](zephyr/arch/arm64/core/prep_c.c), [zephyr/kernel/init.c](zephyr/kernel/init.c), [zephyr/include/zephyr/init.h](zephyr/include/zephyr/init.h)

> **타깃 적용 (`qemu_cortex_a53` SMP)** — 전체 구성은 [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §4 참조
>
> | 본 문서 항목 | 이 타깃에서 |
> |---|---|
> | §2.1 voting lock | **활성.** `MP_MAX_NUM_CPUS=2 > 1` |
> | §2.3 EL 강등 사다리 | **활성.** `ARMV8_A_NS=y` (보드 defconfig) |
> | §2.4 EL2 스킵 경로 | 조건부. QEMU가 진입시키는 EL과 secure 상태에 따라 갈림 |
> | §4 `arch_smp_init()` | **활성.** GIC SGI0을 IPI로 연결 (`05-SMP와-스핀락.md` §6.4) |
> | §7.1 `ready_q.cache` 프라이밍 | **비활성.** `#ifndef CONFIG_SMP` |
> | §7.3 `z_smp_init()` + `INIT_LEVEL_SMP` | **활성.** `SMP_BOOT_DELAY` 기본 n |
> | main 스레드 우선순위 | **0** (선점형). `PREEMPT_ENABLED`가 켜져 있으므로 ([kernel/Kconfig:73](zephyr/kernel/Kconfig#L73)) |
> | `z_mem_manage_init()` (`CONFIG_MMU`) | ARM64 MMU 사용 시 활성 |
> | `CONFIG_BOOTARGS` | 기본 n → `main(void)` 형태로 호출 |

---

## 1. 전체 경로

```
[리셋]
   │
   ▼
__reset / __start                          reset.S:110-194
   │  DAIFSet #0xf — 모든 예외 마스킹
   │  voting lock — 주 코어 선출 (다중 코어)
   │  x24 = 스택, x25 = 진입점
   ▼
__reset_prep_c                             reset.S:40-100
   │  현재 EL별 최소 설정, 스택 확보
   ▼
z_arm64_el_highest_init                    reset.c:37-46
   │
   ▼
switch_el:  ┌─ EL3 → z_arm64_el3_init → eret ─┐
            │                                  │  (같은 레이블로 복귀)
            ├─ EL2 → z_arm64_el2_init → eret ─┤
            │                                  │
            └─ EL1 → z_arm64_el1_init ────────┘
   │
   ▼  ret x25
z_prep_c                                   prep_c.c:34-52
   │  tpidrro_el0 ← &_kernel.cpus[0]
   │  .bss 제로, .data 복사, MMU, 인터럽트 컨트롤러
   ▼
z_cstart                                   init.c:541-622
   │  EARLY → arch_kernel_init → 더미 스레드
   │  → PRE_KERNEL_1 → arch_smp_init → PRE_KERNEL_2
   ▼
prepare_multithreading + switch_to_main_thread
   │  main 스레드 생성 후 더미 스레드에서 컨텍스트 스위치
   ▼
bg_thread_main  (= main 스레드)            init.c:274-361
   │  POST_KERNEL → APPLICATION → 정적 스레드 시작
   │  → z_smp_init() → INIT_LEVEL_SMP
   ▼
main()
```

---

## 2. ARM64 리셋 경로

### 2.1 리셋 벡터와 주 코어 선출

[reset.S:110-194](zephyr/arch/arm64/core/reset.S#L110-L194)

첫 명령은 **마스킹 가능한 예외 전부(D/A/I/F)를 막는 것**입니다. (`CONFIG_WAIT_AT_RESET_VECTOR`가 켜지면 그 앞에 `wfe` 대기 루프가 붙지만 기본값은 n입니다.)

```asm
msr	DAIFSet, #0xf
```
[reset.S:123](zephyr/arch/arm64/core/reset.S#L123)

파일 주석이 진입 시점의 상태를 밝힙니다([reset.S:102-107](zephyr/arch/arm64/core/reset.S#L102-L107)): 프로세서는 특권 레벨의 thread mode이고, **`SP_EL0`도 `SP_ELx`도 SRAM의 유효한 영역을 가리키지 않습니다.** 스택이 없는 상태입니다.

다중 코어에서는 모든 코어가 **동시에** 이 벡터로 진입하므로, 누가 주 코어인지 정해야 합니다. 그 방법이 **voting lock**입니다([reset.S:125-191](zephyr/arch/arm64/core/reset.S#L125-L191)).

```asm
/* signal our desire to vote */
mov	w5, #1
strb	w5, [x4, x2]          /* voting[logic_id] = 1 */
ldr	x3, [x0, #BOOT_PARAM_MPID_OFFSET]
cmn	x3, #1
beq	1f                     /* 아직 아무도 안 정해졌으면 후보 등록 */

/* some core already won, release */
strb	wzr, [x4, x2]
b	secondary_core

1:	str	x1, [x0, #BOOT_PARAM_MPID_OFFSET]   /* 자기 MPID를 제안 */
	strb	wzr, [x4, x2]
	dmb	ish

	/* then wait until every core else is done voting */
2:	ldrb	w3, [x4, x5]
	tst	w3, #255
	bne	2b
```

주석이 알고리즘 출처를 명시합니다 — Linux 커널의 [vlocks](https://kernel.org/doc/html/next/arch/arm/vlocks.html) 문서입니다([reset.S:127-133](zephyr/arch/arm64/core/reset.S#L127-L133)). **메모리 시스템에 최소한의 요구만 하면서** 정확히 하나의 승자를 뽑는 것이 목적입니다. 이 시점에는 캐시도 MMU도 아직 설정되지 않아, 원자적 read-modify-write에 의존할 수 없기 때문입니다. (확신도: medium — 주석이 "reasonable but minimal requirements on the memory system"이라고만 밝히고 원자 연산 불가를 명시하지는 않음)

투표에 쓰는 id는 devicetree의 `cpu_node_list`로 정해지는 **정적 "logic id"**이며, 주석이 이것이 최종 `arch_curr_cpu()->id`와 **다르다**고 못박습니다([reset.S:136-139](zephyr/arch/arm64/core/reset.S#L136-L139)).

승패에 따라 두 레지스터가 갈립니다.

| | `x24` (스택) | `x25` (진입점) |
|---|---|---|
| 주 코어 | `z_interrupt_stacks + SIZEOF` | `z_prep_c` |
| 보조 코어 | `boot_params.sp` | `z_arm64_secondary_prep_c` |

[reset.S:186-194](zephyr/arch/arm64/core/reset.S#L186-L194). 이후 경로는 **완전히 공유**되며, 맨 끝의 `ret x25` 한 줄에서만 갈립니다([reset.S:257](zephyr/arch/arm64/core/reset.S#L257)).

### 2.2 `__reset_prep_c` — C 호출을 위한 최소 준비

[reset.S:40-100](zephyr/arch/arm64/core/reset.S#L40-L100). 함수 주석의 제약이 엄격합니다([reset.S:17-22](zephyr/arch/arm64/core/reset.S#L17-L22)):

> 스택을 아직 쓸 수 없음. `x23`, `x24`, `x25`는 반드시 보존할 것.

`switch_el` 매크로로 현재 EL을 판별해 EL3/EL2/EL1 각각에 대해 **정렬 검사 비활성화**(`SCTLR_A_BIT` 클리어)와 스택 설정을 수행합니다. EL1 경로에만 주석이 하나 더 붙어 있습니다([reset.S:85](zephyr/arch/arm64/core/reset.S#L85)):

```asm
/* Set SP_EL1. We cannot use sp_el1 at EL1 */
msr     SPSel, #1
mov     sp, x24
```

EL1에서는 `sp_el1` 레지스터에 직접 쓸 수 없어, `SPSel`을 바꿔 `sp`가 `SP_EL1`을 가리키게 한 뒤 대입합니다.

끝으로 `fp = NULL`을 설정합니다([reset.S:97-98](zephyr/arch/arm64/core/reset.S#L97-L98)) — 스택 되감기(unwind)가 여기서 멈추도록 하는 종료 표식입니다.

### 2.3 EL 강등 사다리

[reset.S:220-257](zephyr/arch/arm64/core/reset.S#L220-L257). 이 구조가 이 파일에서 가장 독특한 부분입니다.

```asm
switch_el:
	switch_el x0, 3f, 2f, 1f
3:
	bl	z_arm64_el3_init
	adr	x0, switch_el          /* ← 돌아올 주소가 자기 자신 */
	bl	z_arm64_el3_get_next_el
	eret
2:
	bl	z_arm64_el2_init
	mov_imm	x0, (SPSR_DAIF_MASK | SPSR_MODE_EL1T)
	msr	spsr_el2, x0
	adr	x0, 1f
	msr	elr_el2, x0
	eret
1:
	bl	z_arm64_el1_init
	msr	SPSel, #1
	msr	DAIFClr, #(DAIFCLR_ABT_BIT)
	ret	x25
```

EL3 분기가 `eret`의 복귀 주소로 **`switch_el` 레이블 자신**을 지정합니다. 즉 EL2로 내려온 뒤 **같은 코드를 다시 실행**하고, 이번에는 EL2 분기를 타게 됩니다. EL 판별을 한 곳에만 두고 재사용하는 구조입니다.

EL2 분기는 목적지가 EL1로 확정되어 있으므로 복귀 주소를 레이블 `1:`로 직접 지정합니다.

EL1에 도착하면 `SP_ELx` 사용으로 전환하고 **SError만 먼저 풀어줍니다**(`DAIFClr, #DAIFCLR_ABT_BIT`). IRQ/FIQ는 계속 마스킹된 채로, 커널이 스케줄러를 준비할 때까지 유지됩니다.

### 2.4 EL2를 건너뛰는 경우

[reset.c:56-70](zephyr/arch/arm64/core/reset.c#L56-L70)

```c
static inline enum el3_next_el el3_get_next_el(void)
{
	if (!is_el_implemented(2)) {
		return EL3_TO_EL1_NO_EL2;
	} else if (is_in_secure_state() && !is_el2_sec_supported()) {
		/*
		 * Is considered an illegal return "[..] a return to EL2 when EL3 is
		 * implemented and the value of the SCR_EL3.NS bit is 0 if
		 * ARMv8.4-SecEL2 is not implemented" (D1.11.2 from ARM DDI 0487E.a)
		 */
		return EL3_TO_EL1_SKIP_EL2;
	} else {
		return EL3_TO_EL2;
	}
}
```

아키텍처 매뉴얼의 조항 번호까지 인용하며, secure 상태에서 Secure EL2가 없으면 EL2로의 복귀가 **아키텍처적으로 불법**임을 설명합니다.

그런데 EL2를 건너뛰어도 **EL2 설정 자체는 필요합니다.** 그래서 EL3가 대신 수행합니다([reset.c:143-149](zephyr/arch/arm64/core/reset.c#L143-L149)).

```c
if (el3_get_next_el() == EL3_TO_EL1_SKIP_EL2) {
	/*
	 * handle EL2 init in EL3, as it still needs to be done,
	 * but we are going to be skipping EL2.
	 */
	z_arm64_el2_init();
}
```

`z_arm64_el2_init()`이 `_el3_init()`과 별도 함수로 분리되어 있는 이유가 여기에 있습니다 — **두 곳에서 호출**되어야 하기 때문입니다.

### 2.5 각 EL 초기화의 성격

| 함수 | 주요 작업 |
|---|---|
| `z_arm64_el_highest_init()` | `CNTFRQ_EL0`에 시스템 클럭 주파수 기록 ([reset.c:37-46](zephyr/arch/arm64/core/reset.c#L37-L46)) |
| `z_arm64_el3_init()` | 벡터 테이블, 트랩 해제(CPTR), `SCR_EL3`(AArch64/HVC/SMC), GICv3 SRE ([reset.c:72-150](zephyr/arch/arm64/core/reset.c#L72-L150)) |
| `z_arm64_el2_init()` | i-cache·SP 정렬 검사, `HCR_EL2`(예외 라우팅 해제), 가상 타이머 오프셋 0 ([reset.c:153-257](zephyr/arch/arm64/core/reset.c#L153-L257)) |
| `z_arm64_el1_init()` | 벡터 테이블, FP/SIMD 트랩 해제, **d-cache 활성화**, 타이머 CVAL 최대값 ([reset.c:262-318](zephyr/arch/arm64/core/reset.c#L262-L318)) |

모든 `*_plat_init()` 훅이 `__weak`으로 제공되어 SoC가 필요한 단계에만 개입할 수 있습니다([reset.c:17-35](zephyr/arch/arm64/core/reset.c#L17-L35)).

타이머 CVAL을 최대값으로 미리 써 두는 이유가 EL3 쪽에 주석으로 남아 있습니다([reset.c:119-123](zephyr/arch/arm64/core/reset.c#L119-L123)) — 타이머를 켜는 순간 **즉시 인터럽트가 발생하는 것을 막기** 위해서입니다.

---

## 3. `z_prep_c` — C 세계 진입

[prep_c.c:34-52](zephyr/arch/arm64/core/prep_c.c#L34-L52)

```c
FUNC_NORETURN void z_prep_c(void)
{
	soc_prep_hook();

	/* Initialize tpidrro_el0 with our struct _cpu instance address */
	write_tpidrro_el0((uintptr_t)&_kernel.cpus[0]);

	arch_bss_zero();
	arch_data_copy();
#ifdef CONFIG_ARM64_SAFE_EXCEPTION_STACK
	/* After bss clean, _kernel.cpus is in bss section */
	z_arm64_safe_exception_stack_init();
#endif
	z_arm64_mm_init(true);
	z_arm64_interrupt_init();

	z_cstart();
}
```

**순서에 의미가 있습니다.** (`z_arm64_safe_exception_stack_init()`은 `CONFIG_ARM64_SAFE_EXCEPTION_STACK` 아래에 있고 이 옵션은 `ARM_MPU`를 요구하므로, MMU 기반인 본 타깃에서는 컴파일되지 않습니다. 순서 규칙 자체는 유효합니다.)

`tpidrro_el0`에 CPU 구조체 주소를 심는 것이 `arch_bss_zero()`보다 **먼저**입니다. 이 레지스터가 `arch_curr_cpu()`의 구현이며, 이후 모든 per-CPU 접근의 기반이 됩니다. 반면 `z_arm64_safe_exception_stack_init()`은 **bss 제로화 뒤**여야 합니다 — 주석이 그 이유를 밝힙니다: `_kernel.cpus`가 bss 섹션에 있으므로, 먼저 초기화하면 지워집니다.

파일 헤더 주석도 같은 제약을 명시합니다([prep_c.c:13-14](zephyr/arch/arm64/core/prep_c.c#L13-L14)): "이 모듈에서 스택은 쓸 수 있으나, **전역 data/bss는 초기화 전까지 쓸 수 없다.**"

보조 코어는 `z_arm64_secondary_prep_c()`로 진입해 `arch_secondary_cpu_init()`만 호출합니다([prep_c.c:55-62](zephyr/arch/arm64/core/prep_c.c#L55-L62)). bss 제로화나 data 복사를 **반복하지 않습니다** — 주 코어가 이미 했기 때문입니다.

---

## 4. `z_cstart` — 커널 초기화

[init.c:541-622](zephyr/kernel/init.c#L541-L622)

| 순서 | 동작 | 비고 |
|---|---|---|
| 1 | `gcov_static_init()` | 커버리지 훅 |
| 2 | `z_sys_init_run_level(EARLY)` | **아키텍처 초기화보다 먼저** |
| 3 | `arch_kernel_init()` | ARM64에서는 `soc_per_core_init_hook()` 뿐 ([kernel_arch_func.h:33-36](zephyr/arch/arm64/include/kernel_arch_func.h#L33-L36)) |
| 4 | `z_dummy_thread_init(&_thread_dummy)` | 첫 스위치의 출발 컨텍스트 |
| 5 | `z_device_state_init()` | 정적 디바이스 상태 |
| 6 | `soc_early_init_hook()` / `board_early_init_hook()` | |
| 7 | `k_kernel_init_pre_entry` 순회 | 커널 pre-init 훅 |
| 8 | `z_sys_init_run_level(PRE_KERNEL_1)` | 기본 하드웨어 초기화 |
| 9 | `arch_smp_init()` | SMP. IPI 연결 |
| 10 | `z_sys_init_run_level(PRE_KERNEL_2)` | |
| 11 | 스택 카나리 설정 | `z_early_rand_get()` |
| 12 | `switch_to_main_thread(prepare_multithreading())` | **돌아오지 않음** |

`EARLY` 레벨이 `arch_kernel_init()`보다 앞에 오는 이유가 `init.h`에 설명되어 있습니다([init.h:30-34](zephyr/include/zephyr/init.h#L30-L34)) — 아키텍처 코드를 확장·구현하는 SoC가 **아키텍처 초기화 이전에** 필요한 드라이버나 시스템 서비스를 준비할 수 있게 하기 위해서입니다.

커널 pre/post-init 훅은 **pay-per-use** 구조입니다([init.c:564-568](zephyr/kernel/init.c#L564-L568)): 링크된 서브시스템만 항목을 갖고, 순회는 섹션 경계만 참조하므로 쓰지 않으면 비용이 0입니다.

---

## 5. `SYS_INIT` 레벨 체계

### 5.1 여섯 단계

[init.h:30-42](zephyr/include/zephyr/init.h#L30-L42), [init.c:125-134](zephyr/kernel/init.c#L125-L134)

| 레벨 | 실행 시점 | 커널 사용 가능 여부 |
|---|---|---|
| `EARLY` | C 진입 직후 | ✗ |
| `PRE_KERNEL_1` | 인터럽트 스택 위에서 | ✗ |
| `PRE_KERNEL_2` | 위와 동일 | ✗ |
| `POST_KERNEL` | main 스레드 문맥 | ✓ |
| `APPLICATION` | `main()` 직전 | ✓ |
| `SMP` | 보조 CPU 기동 후 | ✓ |

앞의 세 레벨은 **`z_cstart()`가**, 뒤의 세 레벨은 **main 스레드가** 실행합니다. 즉 `POST_KERNEL`부터는 이미 스레드 문맥이며, 이것이 "커널 프리미티브를 쓸 수 있다"의 실질적 의미입니다.

### 5.2 정렬은 링커가 합니다

```c
#define Z_INIT_ENTRY_SECTION(level, prio, sub_prio)                      \
	__attribute__((__section__(                                      \
		".z_init_" #level "_P_" STRINGIFY(prio) "_SUB_" STRINGIFY(sub_prio)"_")))
```
[init.h:111-113](zephyr/include/zephyr/init.h#L111-L113)

각 항목이 **레벨·우선순위·서브우선순위가 이름에 박힌 고유 섹션**에 들어갑니다. 다만 **레벨 순서와 우선순위 순서는 결정 방식이 다릅니다.**

```
/* 레벨 순서 — 링커 스크립트의 명시적 나열 */
CREATE_OBJ_LEVEL(init, EARLY)
CREATE_OBJ_LEVEL(init, PRE_KERNEL_1)
CREATE_OBJ_LEVEL(init, PRE_KERNEL_2)
CREATE_OBJ_LEVEL(init, POST_KERNEL)
CREATE_OBJ_LEVEL(init, APPLICATION)
CREATE_OBJ_LEVEL(init, SMP)
```
[common-rom-kernel-devices.ld:14-19](zephyr/include/zephyr/linker/common-rom/common-rom-kernel-devices.ld#L14-L19)

```c
/* 레벨 내부의 우선순위 순서 — 이름 정렬 */
#define CREATE_OBJ_LEVEL(object, level)				\
		PLACE_SYMBOL_HERE(__##object##_##level##_start);\
		KEEP(*(SORT(.z_##object##_##level##_P_?_*)));	\
		KEEP(*(SORT(.z_##object##_##level##_P_??_*)));	\
		KEEP(*(SORT(.z_##object##_##level##_P_???_*)));
```
[linker-defs.h:70-74](zephyr/include/zephyr/linker/linker-defs.h#L70-L74)

**레벨 순서는 이름 정렬로 나올 수 없습니다** — 사전순이면 `APPLICATION`이 `EARLY`보다 앞서기 때문입니다. 그래서 링커 스크립트가 순서를 직접 나열합니다.

이름 정렬(`SORT()`)이 적용되는 것은 **레벨 안의 우선순위**뿐이며, 그마저도 `_P_?_` / `_P_??_` / `_P_???_`로 **자릿수별 세 번 나눠** 수집합니다. 문자열 정렬에서 `"10"`이 `"9"`보다 앞서는 문제를 자릿수 분리로 피하는 방식입니다.

런타임 정렬 코드는 어느 쪽에도 없습니다.

주석이 이를 확인합니다([init.c:209-211](zephyr/kernel/init.c#L209-L211)):

> 링커 스크립트가 init entry 객체를 **호출되어야 하는 순서대로** 메모리에 배치하며, 심볼이 한 레벨이 끝나고 다음이 시작하는 지점을 표시한다.

### 5.3 실행 루프

```c
static const struct init_entry *levels[] = {
	__init_EARLY_start, __init_PRE_KERNEL_1_start, __init_PRE_KERNEL_2_start,
	__init_POST_KERNEL_start, __init_APPLICATION_start,
#ifdef CONFIG_SMP
	__init_SMP_start,
#endif
	__init_end,          /* End marker */
};

for (entry = levels[level]; entry < levels[level+1]; entry++) { ... }
```
[init.c:217-244](zephyr/kernel/init.c#L217-L244)

**한 레벨의 끝은 다음 레벨의 시작**이며, 마지막을 위해 `__init_end`를 종료 마커로 덧붙였습니다. `levels[level+1]` 접근이 항상 유효한 이유입니다.

### 5.4 디바이스와 `SYS_INIT`의 통합

```c
struct init_entry {
	int (*init_fn)(void);      /* SYS_INIT이면 이쪽 */
	const struct device *dev;  /* 디바이스면 이쪽, 아니면 NULL */
};
```
[init.h:66-77](zephyr/include/zephyr/init.h#L66-L77)

**같은 테이블에 두 종류가 섞여** 우선순위 순으로 실행됩니다([init.c:236-242](zephyr/kernel/init.c#L236-L242)). 디바이스는 `DEVICE_FLAG_INIT_DEFERRED`가 붙어 있으면 여기서 건너뛰고 나중에 명시적으로 초기화됩니다.

---

## 6. 인터럽트 스택의 이중 용도

```c
/*
 * storage space for the interrupt stack
 *
 * Note: This area is used as the system stack during kernel initialization,
 * since the kernel hasn't yet set up its own stack areas. The dual purposing
 * of this area is safe since interrupts are disabled until the kernel context
 * switches to the init thread.
 */
K_KERNEL_STACK_ARRAY_DEFINE(z_interrupt_stacks, CONFIG_MP_MAX_NUM_CPUS,
			    CONFIG_ISR_STACK_SIZE);
```
[init.c:141-150](zephyr/kernel/init.c#L141-L150)

부팅 초기에는 스레드 스택이 없으므로 **인터럽트 스택을 시스템 스택으로 빌려 씁니다.** §2.1에서 `x24`에 실린 값이 바로 이것입니다.

안전한 근거가 주석에 명시되어 있습니다 — **init 스레드로 컨텍스트 스위치할 때까지 인터럽트가 비활성**이므로, 두 용도가 겹칠 수 없습니다. §2.3에서 EL1 도착 시 SError만 풀고 IRQ는 계속 막아둔 것이 이 불변식을 지탱합니다.

---

## 7. main 스레드로의 전환

### 7.1 `prepare_multithreading()`

[init.c:443-475](zephyr/kernel/init.c#L443-L475)

```c
z_sched_init();

#ifndef CONFIG_SMP
	_kernel.ready_q.cache = &z_main_thread;
#endif

stack_ptr = z_setup_new_thread(&z_main_thread, z_main_stack, ...,
			       bg_thread_main, ..., CONFIG_MAIN_THREAD_PRIORITY,
			       K_ESSENTIAL, "main");
z_mark_thread_as_not_sleeping(&z_main_thread);
z_ready_thread(&z_main_thread);
z_init_cpu(0);
```

UP에서 캐시를 미리 채우는 이유가 주석에 세 가지로 정리되어 있습니다([init.c:452-460](zephyr/kernel/init.c#L452-L460)):

1. 캐시는 절대 NULL일 수 없다
2. main 스레드가 가장 먼저 실행될 스레드다
3. **다른 스레드는 아직 초기화되지 않아 우선순위 필드에 쓰레기값이 들어 있고**, 그대로 두면 캐시 적재 알고리즘이 오동작한다

3번이 핵심입니다. `_kernel`이 전부 0으로 시작한다는 사실([init.c:438-439](zephyr/kernel/init.c#L438-L439))만으로는 부족하다는 뜻입니다. SMP에는 캐시 자체가 없으므로 이 문제가 없습니다(`02-스케줄러.md` §6).

main 스레드는 `K_ESSENTIAL`로 생성됩니다 — 이 스레드가 죽으면 시스템 오류입니다.

### 7.2 `switch_to_main_thread()`

[init.c:477-492](zephyr/kernel/init.c#L477-L492)

```c
#ifdef CONFIG_ARCH_HAS_CUSTOM_SWAP_TO_MAIN
	arch_switch_to_main_thread(&z_main_thread, stack_ptr, bg_thread_main);
#else
	/*
	 * Context switch to main task (entry function is _main()): the
	 * current fake thread is not on a wait queue or ready queue, so it
	 * will never be rescheduled in.
	 */
	z_swap_unlocked();
#endif
```

**평범한 컨텍스트 스위치 한 번**으로 부팅이 완료됩니다. 특별한 점은 출발지가 §4의 더미 스레드라는 것뿐이며, 주석대로 그것은 **어떤 큐에도 없으므로 다시 스케줄되지 않습니다.** 더미 스레드의 저장 상태는 그대로 버려집니다(`03-스레드-생명주기.md` §10).

### 7.3 `bg_thread_main()` — main 스레드의 실체

[init.c:274-361](zephyr/kernel/init.c#L274-L361)

| 순서 | 동작 |
|---|---|
| 1 | `z_mem_manage_init()` (MMU) |
| 2 | **`z_sys_post_kernel = true`** |
| 3 | `k_kernel_init_post_entry` 순회 |
| 4 | `z_sys_init_run_level(POST_KERNEL)` |
| 5 | `soc_late_init_hook()` / `board_late_init_hook()` |
| 6 | `z_sys_init_run_level(APPLICATION)` |
| 7 | `z_init_static_threads()` |
| 8 | `z_smp_init()` + `z_sys_init_run_level(SMP)` |
| 9 | **`main()`** |
| 10 | `z_thread_essential_clear(&z_main_thread)` |

2번의 전역 변수가 `k_is_pre_kernel()`의 유일한 근거입니다([kernel.h:876-878](zephyr/include/zephyr/kernel.h#L876-L878)). 커널 API들이 "지금 부팅 중인가"를 판단하는 기준입니다.

3번의 커널 post-init 훅이 `POST_KERNEL` 디바이스 초기화보다 **앞선** 이유도 주석에 있습니다([init.c:294-297](zephyr/kernel/init.c#L294-L297)) — 그 레벨에서 초기화되는 드라이버가 시스템 워크 큐에 제출하는 등 커널 서비스를 쓸 수 있어야 하기 때문입니다.

10번이 흥미롭습니다.

```c
/* Mark non-essential since main() has no more work to do */
z_thread_essential_clear(&z_main_thread);
```

`main()`이 반환한 뒤 essential 표시를 **뗍니다.** 그러지 않으면 main 스레드의 종료가 시스템 치명 오류로 처리됩니다(`03-스레드-생명주기.md` §7.1). 즉 Zephyr에서 **`main()`의 반환은 정상**이며, 다른 스레드들이 계속 실행됩니다.

### 7.4 정적 스레드 시작과 스케줄러 락

[init.c:92-111](zephyr/kernel/init.c#L92-L111)

```c
k_sched_lock();
STRUCT_SECTION_FOREACH(_static_thread_data, thread_data) {
	k_timeout_t init_delay = Z_THREAD_INIT_DELAY(thread_data);

	if (!K_TIMEOUT_EQ(init_delay, K_FOREVER)) {
		thread_schedule_new(thread_data->init_thread, init_delay);
	}
}
k_sched_unlock();
```

주석의 논리가 정확합니다:

> 비-레거시 정적 스레드는 즉시 또는 지정된 지연 후 시작될 수 있다. **스케줄러가 잠겨 있어도 틱은 계속 전달되고 처리되므로**, 전부 시작될 때까지 실행되지 않도록 sched lock을 잡는다.

`k_sched_lock()`은 **선점만** 막을 뿐 타임아웃 처리를 막지 않습니다(`02-스케줄러.md` §10). 락이 없으면 목록 앞쪽 스레드가 뒤쪽이 등록되기도 전에 실행되어, 시작 순서가 정의되지 않습니다.

---

## 8. 보조 CPU

보조 CPU는 **두 번** 다른 경로를 탑니다.

**부팅 시**: `z_smp_init()`이 `arch_cpu_start()`로 코어를 깨우면, 그 코어는 §2.1의 리셋 벡터에서 `secondary_core` 분기를 타고 `z_arm64_secondary_prep_c()`로 진입합니다. 이후 `smp_init_top()`의 두 단계 핸드셰이크로 주 코어와 동기화합니다(`05-SMP와-스핀락.md` §8).

**실행 중**: `k_smp_cpu_start()` / `k_smp_cpu_resume()`으로 개별 기동이 가능합니다.

`z_smp_init()`이 `bg_thread_main()` 안, 즉 **`APPLICATION` 레벨 이후**에 호출된다는 점이 중요합니다([init.c:326-331](zephyr/kernel/init.c#L326-L331)). 보조 CPU가 돌기 시작하는 시점에는 이미 모든 디바이스와 정적 스레드가 준비되어 있습니다. `INIT_LEVEL_SMP`는 그보다도 뒤이므로, 이 레벨의 초기화 함수는 **전 CPU가 살아 있는 상태**를 전제할 수 있습니다.

`CONFIG_SMP_BOOT_DELAY`를 켜면 이 자동 기동을 건너뛰고 애플리케이션이 직접 시점을 정합니다([smp/Kconfig:23-30](zephyr/kernel/smp/Kconfig#L23-L30)).

---

## 9. 정리

- **부팅은 "실행 환경을 한 단계씩 넓혀가는" 과정**입니다. 스택 없음 → 스택 있음(인터럽트 스택 차용) → bss/data 유효 → 커널 자료구조 유효 → 스레드 문맥. 각 단계에서 쓸 수 있는 것이 명확히 정해져 있고, 코드가 그 경계를 주석으로 표시합니다.
- **EL 강등이 같은 코드를 반복 실행하는 방식**으로 구현되어 있습니다. `eret`의 복귀 주소를 판별 지점 자신으로 지정해, EL별 분기 코드를 한 벌만 유지합니다.
- **초기화 순서는 링커가 결정합니다.** 레벨 순서는 링커 스크립트가 직접 나열하고, 레벨 안의 우선순위만 섹션 이름 정렬로 결정됩니다. 어느 쪽에도 런타임 정렬이 없으며, 디바이스와 `SYS_INIT`이 같은 테이블에서 섞여 실행됩니다.
- **부팅의 종착점은 평범한 컨텍스트 스위치 한 번**입니다. 더미 스레드에서 main 스레드로 넘어가면 그 뒤는 일반적인 스케줄링일 뿐이며, 나머지 초기화(`POST_KERNEL` 이후)는 main 스레드가 **일반 스레드로서** 수행합니다.
- **`main()`의 반환은 정상 종료**입니다. 반환 직후 essential 표시를 떼는 한 줄이 그것을 보장합니다.

---

## 부록: 커널 코어 분석 마무리

본 문서로 [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §2에서 정한 **커널 코어 1차 분석 범위가 완료**되었습니다.

| # | 문서 | 다루는 축 |
|---|---|---|
| 01 | [커널 코어 자료구조](1.zephyr분석/01-커널-코어-자료구조.md) | 무엇이 있는가 |
| 02 | [스케줄러](1.zephyr분석/02-스케줄러.md) | 누가 언제 실행되는가 |
| 03 | [스레드 생명주기](1.zephyr분석/03-스레드-생명주기.md) | 스레드가 어떻게 나고 죽는가 |
| 04 | [타임아웃과 시스템 클럭](1.zephyr분석/04-타임아웃과-시스템-클럭.md) | 시간이 어떻게 흐르는가 |
| 05 | [SMP와 스핀락](1.zephyr분석/05-SMP와-스핀락.md) | 여러 CPU가 어떻게 협조하는가 |
| 06 | [동기화 객체](1.zephyr분석/06-동기화-객체.md) | 스레드끼리 어떻게 주고받는가 |
| 07 | 본 문서 | 이 모든 것이 어떻게 시작되는가 |

되풀이해 관찰된 설계 특징:

- **비트 하나까지 아낍니다.** `preempt` 유니온(01 §4.2), 포인터 하위 비트 재활용(04 §6.2, 05 §3.1), `thread->events`의 이중 용도(06 §6.4).
- **원자성의 경계가 명시적입니다.** wake 작업 전체를 한 번의 락 획득 안에서 끝내야 한다는 규칙이 스케줄러·타임아웃·뮤텍스에서 반복됩니다.
- **UP과 SMP는 같은 API 뒤의 다른 구현입니다.** 코드를 읽을 때 어느 쪽을 보고 있는지 먼저 확정해야 합니다.
- **일부 규약은 코드가 아니라 주석에 있습니다.** 뮤텍스 중첩 해제 순서(06 §4.5), `signal_pending_ipi()`의 호출 위치(05 §6.2), 타임아웃 락 규약(04 §1)이 그렇습니다.

후속으로 다룰 만한 영역은 [TODO.md](1.zephyr분석/TODO.md)에 기록되어 있습니다.
