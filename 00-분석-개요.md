# Zephyr RTOS 소스 분석 — 개요

## 1. 분석 대상

| 항목 | 값 | 근거 |
|---|---|---|
| 프로젝트 | Zephyr RTOS | `zephyr/README.rst` |
| 버전 | 4.4.99 (4.4 릴리스 이후 개발 브랜치) | `zephyr/VERSION` |
| SDK 버전 | 1.0.1 | `zephyr/SDK_VERSION` |
| 워크스페이스 | west 기반 (`.west/`, `modules/`, `bootloader/`, `tools/`) | 디렉터리 구성 |
| 분석 타깃 | `qemu_cortex_a53` SMP (ARM Cortex-A53 / AArch64, 2코어) | §4 참조 |

`PATCHLEVEL = 99`는 Zephyr에서 "릴리스 태그가 아닌 개발 중 스냅샷"을 의미하는 관례값입니다. 따라서 본 분석 내용은 4.4 릴리스와 5.0 사이의 `main` 계열 코드에 해당하며, 정식 릴리스 문서와 세부 구현이 다를 수 있습니다. (확신도: high — VERSION 파일 직접 확인)

## 2. 분석 범위

1차 분석 범위는 **커널 코어**입니다. 대상 디렉터리는 `zephyr/kernel/` 및 그 내부 헤더 `zephyr/kernel/include/`, 그리고 커널이 노출하는 공개 헤더 `zephyr/include/zephyr/kernel_structs.h`, `zephyr/include/zephyr/kernel/thread.h` 입니다.

범위에 포함하지 않은 영역 (후속 분석 대상):

- 아키텍처 포팅 계층 (`zephyr/arch/`) — 단, §4의 타깃 구성 확정을 위해 `arch/arm64/`의 Kconfig는 참조했습니다
- 디바이스 드라이버 및 디바이스트리 (`zephyr/drivers/`, `zephyr/dts/`)
- 서브시스템 (`zephyr/subsys/` — 네트워킹, 블루투스, 파일시스템 등)
- 빌드 시스템 (CMake / Kconfig / west 연동)

## 3. 문서 구성

작업 순서·범위·확정 사항은 [TODO.md](1.zephyr분석/TODO.md)에서 관리합니다. 작성 순서는 04 → 07 → 05 → 06으로 조정되었으며, 근거는 TODO.md §3에 있습니다.

| 순서 | 문서 | 내용 | 상태 |
|---|---|---|---|
| — | `00-분석-개요.md` | 본 문서. 범위, 타깃, 인용 규칙 | 완료 |
| 1 | `01-커널-코어-자료구조.md` | `z_kernel`, `_cpu`, `k_thread`, wait_q, timeout 등 핵심 자료구조 | 완료 |
| 2 | `02-스케줄러.md` | 우선순위 체계, 레디 큐 백엔드, 스레드 선택 로직, 컨텍스트 스위치 | 완료 |
| 3 | `03-스레드-생명주기.md` | 생성 → 실행 → 대기 → 종료의 상태 전이와 API 경로 | 완료 |
| 4 | `04-타임아웃과-시스템-클럭.md` | `timeout.c`의 in-flight/superseded 상태 머신, 백엔드 4종, tickless | 완료 |
| 5 | `05-SMP와-스핀락.md` | `k_spinlock`, 검증 프레임워크, IPI 경로, 보조 CPU 부팅 | 완료 |
| 6 | `06-동기화-객체.md` | semaphore, mutex(우선순위 상속), condvar, event, poll | 완료 |
| 7 | `07-부팅과-초기화.md` | `z_cstart()` → `SYS_INIT` 레벨 → main 진입 | 다음 |

## 4. 분석 타깃

**주 타깃: `qemu_cortex_a53` (SMP 변형) — ARM Cortex-A53 / AArch64**
**보조 타깃: Cortex-M 계열 (`CONFIG_SWAP_NONATOMIC` 경로 확인용)**

### 4.1 선정 근거

1. **`CONFIG_USE_SWITCH`가 무조건 켜집니다.** `ARM64`가 이를 직접 select하므로([arch/Kconfig:57-58](zephyr/arch/Kconfig#L57-L58)), 신형 컨텍스트 스위치 경로(`arch_switch()`, `z_get_next_switch_handle()`)가 전부 실코드입니다.
2. **UP과 SMP를 같은 보드에서 대조할 수 있습니다.** `qemu_cortex_a53_defconfig`(UP)와 `qemu_cortex_a53_qemu_cortex_a53_smp_defconfig`(SMP, 2코어)가 함께 제공됩니다. 스케줄러는 두 모드에서 동작이 크게 갈리므로(`02-스케줄러.md` §11), 하드웨어를 고정한 채 대조하는 것이 분석에 유리합니다.
3. **QEMU 보드입니다.** 실물 하드웨어 없이 빌드·실행·GDB 디버깅이 가능합니다.
4. **directed IPI를 지원합니다.** `ARM64`가 `ARCH_HAS_DIRECTED_IPIS`를 select하므로([arch/Kconfig:60](zephyr/arch/Kconfig#L60)), `z_thread_halt()`이 broadcast가 아닌 directed 분기를 탑니다([sched.c:308](zephyr/kernel/sched.c#L308)).

### 4.2 확정된 커널 구성 (qemu_cortex_a53 / SMP)

`select` 문으로 강제되는 항목은 **확정**, Kconfig 기본값에 의존하는 항목은 **기본값**으로 구분했습니다.

| 옵션 | 값 | 유형 | 근거 |
|---|---|---|---|
| `ARM64` / `64BIT` | y | 확정 | [arch/Kconfig:50-53](zephyr/arch/Kconfig#L50-L53) |
| `USE_SWITCH` | y | 확정 | [arch/Kconfig:57](zephyr/arch/Kconfig#L57) |
| `ARCH_HAS_DIRECTED_IPIS` | y | 확정 | [arch/Kconfig:60](zephyr/arch/Kconfig#L60) |
| `SMP` | y | 보드 defconfig | `qemu_cortex_a53_qemu_cortex_a53_smp_defconfig` |
| `MP_MAX_NUM_CPUS` | 2 | 보드 defconfig | 동일 |
| `SCHED_IPI_SUPPORTED` | y | 확정 (SMP 조건부) | [arm64/core/Kconfig:11](zephyr/arch/arm64/core/Kconfig#L11) |
| `TIMEOUT_64BIT` | y | 보드 defconfig | 동일 (기본값도 y, [kernel/Kconfig:746](zephyr/kernel/Kconfig#L746)) |
| `SCHED_SIMPLE` | y | 기본값 | [kernel/Kconfig:258](zephyr/kernel/Kconfig#L258) |
| `WAITQ_SIMPLE` | y | 기본값 | [kernel/Kconfig:310](zephyr/kernel/Kconfig#L310) |
| `TIMEOUT_BACKEND_DLIST` | y | 기본값 | [kernel/Kconfig:757](zephyr/kernel/Kconfig#L757) |
| `NUM_COOP_PRIORITIES` | 16 | 기본값 | [kernel/Kconfig:30](zephyr/kernel/Kconfig#L30) |
| `NUM_PREEMPT_PRIORITIES` | 15 | 기본값 | [kernel/Kconfig:55](zephyr/kernel/Kconfig#L55) |
| `TIMESLICING` | y | 기본값 | [kernel/Kconfig:552](zephyr/kernel/Kconfig#L552) |

따라서 우선순위 공간은 **`K_HIGHEST_THREAD_PRIO = -16` ~ `K_LOWEST_THREAD_PRIO = K_IDLE_PRIO = 15`**, 애플리케이션 사용 범위는 `-16` ~ `14`, `K_NUM_THREAD_PRIO = 32`입니다.

### 4.3 이 타깃에서 **비활성**인 경로

문서를 읽을 때 "이 코드는 A53에서 실행되지 않는다"고 알아두어야 할 항목입니다.

| 옵션 | 값 | 비활성 사유 | 영향받는 코드 |
|---|---|---|---|
| `SWAP_NONATOMIC` | n | `CPU_CORTEX_M` 전용 select ([arm/core/Kconfig:30](zephyr/arch/arm/core/Kconfig#L30)) | `pending_current` 전체, `should_preempt()` 4번 조건 |
| `ARCH_HAS_CUSTOM_CURRENT_IMPL` | n | RISC-V 전용 select ([riscv/Kconfig:52](zephyr/arch/riscv/Kconfig#L52)) | `_current` 재정의 |
| `KERNEL_COHERENCE` | n | `CACHE_CAN_SAY_MEM_COHERENCE` 미충족 (Intel ADSP SoC 전용) | `sys_cache_is_mem_coherent()` 어서션, `__incoherent` |
| `SCHED_CPU_MASK` | n | 기본값 없음 (SoC가 별도 지정해야 함) | `*_mask_best()` 변형 전부, `SCHED_CPU_MASK_PIN_ONLY` |
| `SCHED_DEADLINE` | n | 기본값 없음 | `z_sched_prio_cmp()`의 데드라인 비교 |
| `NUM_METAIRQ_PRIORITIES` | 0 | 기본값 ([kernel/Kconfig:97](zephyr/kernel/Kconfig#L97)) | MetaIRQ 선점 로직 전체 |
| `THREAD_ABORT_NEED_CLEANUP` | n | `THREAD_STACK_MEM_MAPPED`만 select하며 기본 n ([kernel/Kconfig:164-168](zephyr/kernel/Kconfig#L164-L168)) | 지연 정리 (`03-스레드-생명주기.md` §9) |
| `USERSPACE` | n | 기본값 없음 | syscall 검증 계층(`z_vrfy_*`), 메모리 도메인 |

### 4.4 검증 한계 (escalation 아님, 기록용)

위 표는 **Kconfig의 `select`/`default` 선언과 보드 defconfig에서 도출**한 것이며, 실제 빌드로 생성한 `.config`로 확인한 것은 아닙니다. 이 환경에는 Zephyr SDK(`aarch64-zephyr-elf-gcc`)가 설치되어 있지 않아 빌드를 수행할 수 없었습니다.

- **확정** 항목은 `select` 문에 의한 강제이므로 빌드 없이도 확실합니다. (확신도: high)
- **기본값** 항목은 상위 SoC/보드 Kconfig가 덮어쓸 여지가 이론상 남아 있습니다. `soc/arm/qemu_cortex_a53/`와 `boards/qemu/cortex_a53/`의 defconfig를 확인한 결과 해당 항목을 덮어쓰는 곳은 없었으나, 전 트리 역추적은 하지 않았습니다. (확신도: medium-high)

SDK를 설치하시면 `west build -b qemu_cortex_a53/qemu_cortex_a53/smp samples/hello_world` 후 `build/zephyr/.config`로 전 항목을 확정할 수 있습니다. 필요하시면 그 시점에 표를 갱신하겠습니다.

## 5. 인용 규칙

- 모든 서술은 `파일:행` 형식으로 근거를 명시합니다.
- 행 번호는 분석 시점(2026-08-05)의 작업 트리 기준입니다. 소스가 갱신되면 어긋날 수 있습니다.
- 코드에서 직접 확인한 사실과 추론을 구분하며, 추론에는 확신도를 표기합니다.

## 6. 분석 중 발견한 특이사항

### 6.1 정의가 없는 함수 선언 — `z_sched_start()`

`z_sched_start()`는 [ksched.h:96](zephyr/kernel/include/ksched.h#L96)에 선언되어 있으나, Zephyr 저장소 전체(`*.c`, `*.h`)에서 정의도 호출도 발견되지 않았습니다.

```c
void z_sched_start(struct k_thread *thread);
```

스레드 시작 경로가 `k_thread_start()` → `k_wakeup()`으로 통합되면서([kernel.h:1397](zephyr/include/zephyr/kernel.h#L1397)) 남은 잔여 선언으로 보입니다. 기능상 영향은 없으나 정리 대상 후보입니다. (확신도: high — 저장소 전체 grep 결과 선언 1건 외 없음. 단, 저장소 외부 코드가 참조할 가능성은 배제하지 못함)

### 6.2 `k_thread_start()`의 현재 위상

`k_thread_start()`는 별도 구현 없이 `k_wakeup()`을 호출하는 인라인 래퍼이며, 헤더 주석이 스스로를 "legacy API for compatibility"로 명시합니다([kernel.h:1391-1400](zephyr/include/zephyr/kernel.h#L1391-L1400)). 현재 스레드는 생성 시 `_THREAD_SLEEPING` 상태로 초기화되므로([thread.c:898](zephyr/kernel/thread.c#L898)), 시작은 "잠든 스레드를 깨우는 것"과 동일한 연산입니다.
