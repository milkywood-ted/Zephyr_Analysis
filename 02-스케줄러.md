# 스케줄러

분석 대상: [zephyr/kernel/sched.c](zephyr/kernel/sched.c), [zephyr/kernel/scheduler.c](zephyr/kernel/scheduler.c), [zephyr/kernel/include/priority_q.h](zephyr/kernel/include/priority_q.h), [zephyr/kernel/include/run_q.h](zephyr/kernel/include/run_q.h), [zephyr/kernel/include/kswap.h](zephyr/kernel/include/kswap.h), [zephyr/kernel/timeslicing.c](zephyr/kernel/timeslicing.c)

> **타깃 적용 (`qemu_cortex_a53` SMP)** — 전체 구성은 [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §4 참조
>
> | 본 문서 항목 | 이 타깃에서 |
> |---|---|
> | §3 레디 큐 백엔드 | **SIMPLE만 활성.** SCALABLE·MULTIQ는 대조용 서술 |
> | §3.4 CPU 마스크 변형 | **비활성.** `SCHED_CPU_MASK=n` |
> | §2 데드라인 비교 | **비활성.** `SCHED_DEADLINE=n` → `z_sched_prio_cmp()`는 `prio` 비교만 |
> | §2·§6 MetaIRQ | **비활성.** `NUM_METAIRQ_PRIORITIES=0` → `thread_is_metairq()`가 항상 0 |
> | §5 `next_up()` | **SMP 분기 활성.** UP 분기는 UP 변형 보드에서만 |
> | §6 `update_cache()` | **SMP 동작(`swap_ok` 대입).** 캐시 재계산은 UP 변형에서만 |
> | §6 `should_preempt()` 4번 조건 | **비활성.** `SWAP_NONATOMIC=n` |
> | §7 컨텍스트 스위치 | **전부 활성.** `USE_SWITCH=y`가 ARM64에서 강제 |
> | §7.3 `switch_handle` 스핀 | **활성.** SMP 2코어 |
> | §9 `pending_current` 처리 | **비활성.** `SWAP_NONATOMIC=n` |
> | §10 `LOCK_SCHED_SPINLOCK` | **실제 스핀락.** `MP_MAX_NUM_CPUS=2 ≠ 1` |
> | `z_thread_halt()`의 IPI | **directed IPI.** `ARCH_HAS_DIRECTED_IPIS=y` → `arch_sched_directed_ipi()` |
>
> 즉 이 타깃은 **§5~§7의 SMP 경로와 §7의 스위치 경로를 전부 실코드로 다루는** 반면, MetaIRQ·데드라인·CPU 마스크·`SWAP_NONATOMIC` 관련 서술은 컴파일되지 않습니다.

## 1. 파일 분담

커널 4.4 계열에서 스케줄러는 세 파일로 나뉘어 있습니다.

| 파일 | 역할 |
|---|---|
| `sched.c` | 스케줄링 **메커니즘**. 스레드 선택, 레디/대기 큐 이동, 컨텍스트 스위치 진입, halt 처리 |
| `scheduler.c` | 스케줄러 **공개 API**. `k_sched_lock/unlock()`, `k_reschedule()`, 대기 큐 순회 |
| `timeslicing.c` | 타임슬라이스 전용 로직 |

`sched.c`의 내부 함수 중 `thread.c`가 필요로 하는 것들은 `ALIAS_OF`로 `z_sched_*` 이름을 덧붙여 노출합니다([sched.c:225](zephyr/kernel/sched.c#L225), [:246](zephyr/kernel/sched.c#L246), [:422-423](zephyr/kernel/sched.c#L422-L423)). 예를 들어 `ready_thread()`는 `z_sched_ready_locked()`라는 이름으로도 호출됩니다. 심볼 중복 없이 내부 함수를 계층 간에 공유하는 방식입니다.

## 2. 우선순위 체계

[kernel.h:92-100](zephyr/include/zephyr/kernel.h#L92-L100)

```c
#define K_HIGHEST_THREAD_PRIO (-CONFIG_NUM_COOP_PRIORITIES)
#define K_LOWEST_THREAD_PRIO CONFIG_NUM_PREEMPT_PRIORITIES
#define K_IDLE_PRIO K_LOWEST_THREAD_PRIO
#define K_HIGHEST_APPLICATION_THREAD_PRIO (K_HIGHEST_THREAD_PRIO)
#define K_LOWEST_APPLICATION_THREAD_PRIO (K_LOWEST_THREAD_PRIO - 1)
```

**숫자가 작을수록 높은 우선순위**이며, 음수는 협조적(cooperative), 0 이상은 선점형(preemptible)입니다.

```
  -NUM_COOP           -1  0            NUM_PREEMPT-1  NUM_PREEMPT
    │                  │  │                   │            │
    ├── 협조적 (coop) ──┤  ├── 선점형 (preempt) ┤            └─ K_IDLE_PRIO
    │                                          │
    └─ MetaIRQ는 이 구간의 최상위 몇 개를 점유 ──┘
```

- 아이들 스레드만 `K_IDLE_PRIO`를 쓸 수 있습니다. `Z_VALID_PRIO()`가 엔트리 포인트가 `idle`인지까지 확인합니다([ksched.h:22-27](zephyr/kernel/include/ksched.h#L22-L27)).
- **MetaIRQ**는 협조적 영역 최상단 `CONFIG_NUM_METAIRQ_PRIORITIES`개입니다([kthread.h:72-81](zephyr/kernel/include/kthread.h#L72-L81)). 협조적 스레드조차 선점할 수 있는 특수 등급으로, ISR을 스레드 문맥으로 끌어내리기 위한 장치입니다.

우선순위 비교는 전용 함수를 거칩니다([priority_q.h:81-112](zephyr/kernel/include/priority_q.h#L81-L112)).

```c
static ALWAYS_INLINE int32_t z_sched_prio_cmp(struct k_thread *thread_1, struct k_thread *thread_2)
{
	int32_t b1 = thread_1->base.prio, b2 = thread_2->base.prio;
	if (b1 != b2) return b2 - b1;
#ifdef CONFIG_SCHED_DEADLINE
	uint32_t d1 = thread_1->base.prio_deadline, d2 = thread_2->base.prio_deadline;
	if (d1 != d2) return (int32_t)(d2 - d1);
#endif
	return 0;
}
```

`CONFIG_SCHED_DEADLINE`이 켜지면 **같은 우선순위 내에서 데드라인이 이른 쪽이 우선**합니다. 데드라인 비교가 현재 시각을 참조하지 않고 성립하는 이유는 주석에 있습니다: 모든 데드라인이 32비트 모듈러 공간의 "같은 절반" 안에 있다는 것이 API 규약이므로, 2의 보수 뺄셈만으로 대소가 결정됩니다([priority_q.h:91-98](zephyr/kernel/include/priority_q.h#L91-L98)). 부호 없는 뺄셈 후 부호 있는 타입으로 캐스팅하는 것에 대해 주석은 "완벽하지 않지만 오버플로 UB를 구현 정의 동작으로 낮춘다"고 솔직하게 인정합니다.

## 3. 레디 큐 백엔드 3종

Kconfig choice `SCHED_ALGORITHM`, 기본값 `SCHED_SIMPLE`([kernel/Kconfig:256-306](zephyr/kernel/Kconfig#L256-L306)).

| 백엔드 | 자료구조 | 삽입 | 최선 스레드 조회 | 특징 |
|---|---|---|---|---|
| `SCHED_SIMPLE` | 정렬된 이중 연결 리스트 | O(N) | O(1) | 코드 크기 최소. 런너블 스레드 3개 내외 권장 |
| `SCHED_SCALABLE` | 레드블랙 트리 | O(log N) | O(log N) | 상수 계수 큼, 코드 +2KB. 수천 스레드까지 확장 |
| `SCHED_MULTIQ` | 우선순위별 리스트 배열 + 비트맵 | O(1) | O(1) | RAM 다수 소모. `SCHED_DEADLINE`과 배타 |

세 구현 모두 `priority_q.h`에 인라인으로 있고, `_priq_run_add` / `_priq_run_remove` / `_priq_run_best` / `_priq_run_yield` 매크로로 선택됩니다([priority_q.h:13-46](zephyr/kernel/include/priority_q.h#L13-L46)).

### 3.1 SIMPLE — 정렬 삽입

```c
static ALWAYS_INLINE void z_priq_simple_add(sys_dlist_t *pq, struct k_thread *thread)
{
	struct k_thread *t;
	SYS_DLIST_FOR_EACH_CONTAINER(pq, t, base.qnode_dlist) {
		if (z_sched_prio_cmp(thread, t) > 0) {
			sys_dlist_insert(&t->base.qnode_dlist, &thread->base.qnode_dlist);
			return;
		}
	}
	sys_dlist_append(pq, &thread->base.qnode_dlist);
}
```
[priority_q.h:114-126](zephyr/kernel/include/priority_q.h#L114-L126)

리스트가 항상 정렬 상태이므로 `_best`는 헤드를 읽기만 합니다. 삽입이 O(N)인 대신 조회가 상수 시간입니다.

`z_priq_simple_yield()`가 흥미롭습니다([priority_q.h:135-164](zephyr/kernel/include/priority_q.h#L135-L164)). **단, 본문 전체가 `#ifndef CONFIG_SMP`이므로 본 분석 타깃에서는 빈 함수로 컴파일됩니다** — 아래 설명은 UP 변형에 해당합니다(rb·mq의 yield도 동일). 양보하는 스레드를 리스트에서 빼고 **같은 우선순위 그룹의 끝**에 다시 넣는데, 검색을 리스트 처음이 아니라 **원래 자기 다음 위치부터** 시작합니다. 현재 스레드가 헤드가 아닐 수도 있기 때문이며(주석 [:146-150](zephyr/kernel/include/priority_q.h#L146-L150)), 불필요한 앞부분 순회를 건너뜁니다.

### 3.2 SCALABLE — 레드블랙 트리와 `order_key`

트리는 중복 키를 허용하지 않으므로, 같은 우선순위 스레드를 구분할 타이브레이커가 필요합니다. 그것이 `base.order_key`입니다([priority_q.h:207-228](zephyr/kernel/include/priority_q.h#L207-L228)).

```c
thread->base.order_key = pq->next_order_key;
++pq->next_order_key;

if (!pq->next_order_key) {   /* 랩어라운드 */
	RB_FOR_EACH_CONTAINER(&pq->tree, t, base.qnode_rb) {
		t->base.order_key = pq->next_order_key;
		++pq->next_order_key;
	}
}
```

삽입할 때마다 증가하는 일련번호로 FIFO 순서를 보장하고, 32비트가 한 바퀴 돌면 전체를 재번호 매김합니다. 주석은 이 재번호가 **지연 시간 글리치**가 될 수 있음을 인정하면서도, "큐가 완전히 비지 않은 채 매우 오래 돌고 스레드가 매우 많은 시스템"에서만 발생한다고 평가합니다([priority_q.h:214-219](zephyr/kernel/include/priority_q.h#L214-L219)). 큐가 완전히 비면 `next_order_key`가 0으로 리셋되므로([:230-237](zephyr/kernel/include/priority_q.h#L230-L237)) 실제 도달은 드뭅니다.

### 3.3 MULTIQ — 비트맵 스캔

우선순위별 리스트 배열과 점유 비트맵을 함께 둡니다([kernel_structs.h:122-128](zephyr/include/zephyr/kernel_structs.h#L122-L128)).

```c
static ALWAYS_INLINE unsigned int z_priq_mq_best_queue_index(struct _priq_mq *pq)
{
	unsigned int i = 0;
	do {
		if (likely(pq->bitmask[i]))
			return i * NBITS + TRAILING_ZEROS(pq->bitmask[i]);
		i++;
	} while (i < PRIQ_BITMAP_SIZE);
	return K_NUM_THREAD_PRIO - 1;
}
```
[priority_q.h:291-303](zephyr/kernel/include/priority_q.h#L291-L303)

`TRAILING_ZEROS`(하드웨어 CTZ 명령)로 최상위 비어있지 않은 우선순위를 즉시 찾습니다. UP에서는 여기에 더해 `cached_queue_index`로 스캔조차 생략합니다([priority_q.h:356-362](zephyr/kernel/include/priority_q.h#L356-L362)) — 캐시는 삽입 시 갱신, 제거 시 큐가 비었을 때만 재계산합니다.

### 3.4 CPU 마스크 변형

`CONFIG_SCHED_CPU_MASK`가 켜지면 `_best`가 `*_mask_best` 변형으로 바뀝니다. 최선 스레드가 현재 CPU에서 실행 불가일 수 있으므로 **조건에 맞을 때까지 순회**해야 하며, 그만큼 최악 비용이 올라갑니다([priority_q.h:178-192](zephyr/kernel/include/priority_q.h#L178-L192), [:373-401](zephyr/kernel/include/priority_q.h#L373-L401)). MULTIQ 마스크 변형은 O(1)을 잃고 이중 루프가 됩니다.

## 4. 큐 접근 래퍼 — `run_q.h`

레디 큐가 전역인지 CPU별인지를 `thread_runq()`가 흡수합니다([run_q.h:17-49](zephyr/kernel/include/run_q.h#L17-L49)). `CONFIG_SCHED_CPU_MASK_PIN_ONLY`일 때는 스레드의 `cpu_mask`에서 CTZ로 CPU를 뽑아 해당 CPU의 큐를 반환합니다. 어서션이 있음에도 `MIN()`으로 한 번 더 클램프하는데, 그 이유가 주석에 있습니다: **정적 분석 도구가 어서션을 인지하지 못해 배열 범위 초과로 오탐하는 것을 막기 위해서**입니다([run_q.h:30-33](zephyr/kernel/include/run_q.h#L30-L33)).

SMP의 핵심 불변식은 `should_queue_thread()`에 있습니다([run_q.h:81-84](zephyr/kernel/include/run_q.h#L81-L84)).

```c
static inline bool should_queue_thread(struct k_thread *thread)
{
	return !IS_ENABLED(CONFIG_SMP) || (thread != _current);
}
```

> **SMP에서 실행 중인 스레드는 레디 큐에 들어 있지 않다.**

반대로 UP에서는 실행 중인 스레드도 큐에 남아 있습니다. 이유는 [sched.c:75-80](zephyr/kernel/sched.c#L75-L80) 주석에 있습니다 — 그렇지 않으면 모든 아키텍처의 컨텍스트 스위치 어셈블리가 스레드를 큐에 되돌리는 책임을 져야 하기 때문입니다.

따라서 `queue_thread()`/`dequeue_thread()`는 **상태 비트 설정과 실제 큐 조작을 분리**합니다([run_q.h:86-106](zephyr/kernel/include/run_q.h#L86-L106)). SMP에서 현재 스레드에 `queue_thread()`를 호출하면 `_THREAD_QUEUED` 비트만 서고 `swap_ok = true`가 되며, 이는 "양보 의사"로 해석됩니다.

## 5. 스레드 선택 — `next_up()`

[sched.c:56-137](zephyr/kernel/sched.c#L56-L137). 스케줄러의 심장입니다.

### UP 경로

```c
struct k_thread *thread = runq_best();
thread = metairq_preempt_recover(thread);
return (thread != NULL) ? thread : _current_cpu->idle_thread;
```

큐의 최선 스레드, 없으면 아이들. 끝입니다. 현재 스레드가 큐에 남아 있으므로 비교가 필요 없습니다.

### SMP 경로

현재 스레드가 큐 밖에 있으므로, `runq_best()`가 준 것은 "**현재 스레드가 아닌 것 중 최선**"입니다. 따라서 직접 비교해야 합니다.

```c
bool queued = z_is_thread_queued(_current);
bool active = z_is_thread_ready(_current);

if (thread == NULL) thread = _current_cpu->idle_thread;

if (active) {
	int32_t cmp = z_sched_prio_cmp(_current, thread);
	/* 동점은 양보 의사가 있을 때만 교체 */
	if ((cmp > 0) || ((cmp == 0) && !_current_cpu->swap_ok))
		thread = _current;
	if (!should_preempt(thread, _current_cpu->swap_ok))
		thread = _current;
}

if (thread != _current) {
	update_metairq_preempt(thread);
	if (active && !queued && !z_is_idle_thread_object(_current)
	    && metairq_current_requeue_allowed())
		queue_thread(_current);      /* 물러나는 현재 스레드를 큐로 */
}
if (z_is_thread_queued(thread)) dequeue_thread(thread);  /* 새 현재 스레드를 큐 밖으로 */
_current_cpu->swap_ok = false;
```

동점 처리 규칙이 중요합니다: **우선순위가 같으면 `swap_ok`가 서 있을 때만 교체**합니다. 즉 명시적으로 양보하지 않는 한 같은 우선순위끼리는 서로를 밀어내지 않으며, 이것이 라운드로빈이 타임슬라이스나 `k_yield()` 없이는 일어나지 않는 이유입니다.

또한 SMP 경로 진입 직후, **현재 스레드 자신이 halt 요청을 받았는지** 먼저 확인합니다([sched.c:58-68](zephyr/kernel/sched.c#L58-L68)). 다른 CPU가 IPI로 "너 멈춰"라고 요청한 것을 여기서 이행합니다.

## 6. 캐시 갱신과 선점 판정

### `update_cache()`

[sched.c:146-172](zephyr/kernel/sched.c#L146-L172). UP과 SMP가 **완전히 다른 일**을 합니다.

- **UP**: `next_up()`을 실제로 호출해 `_kernel.ready_q.cache`를 갱신합니다. 선점이 허용되지 않으면 캐시에 현재 스레드를 넣어, 이후 `need_swap()`이 포인터 비교만으로 "스위치 불필요"를 판정하게 합니다.
- **SMP**: `_current_cpu->swap_ok = preempt_ok` 대입 한 줄이 전부입니다. 실제 선택은 스위치 시점에 `next_up()`이 다시 합니다.

### `should_preempt()`

[kthread.h:224-259](zephyr/kernel/include/kthread.h#L224-L259). 선점 허용 조건을 순서대로 확인합니다.

1. `preempt_ok != 0` — 소프트웨어가 명시적으로 허용 (예: `k_yield()`)
2. 현재 스레드가 이미 실행 불가 상태 (pend/suspend/dummy)
3. 현재 스레드가 선점 가능하거나(`thread_is_preemptible`), **새 스레드가 MetaIRQ**
4. `CONFIG_SWAP_NONATOMIC` 엣지 케이스 — 새 스레드에 활성 타임아웃이 있으면 허용

4번은 ARM에서 인터럽트 핸들러 안에서 스레드가 pend된 뒤 "동기적" 스왑이 컨텍스트 스위치를 시작하기 전에 발생하는 상황을 위한 것이며, 원자적 스왑이 가능한 플랫폼에서는 도달하지 않는다고 주석에 명시되어 있습니다([kthread.h:248-252](zephyr/kernel/include/kthread.h#L248-L252)).

3번의 "MetaIRQ면 무조건 허용"이 MetaIRQ의 본질입니다 — **협조적 스레드도 선점**합니다.

## 7. 컨텍스트 스위치 경로

두 가지 경로가 있으며, 진입 지점이 다릅니다.

### 7.1 자발적 스위치 — `z_swap()` → `do_swap()`

[kswap.h:80-177](zephyr/kernel/include/kswap.h#L80-L177). 스레드가 스스로 블록하거나 양보할 때.

순서:

1. `old_thread->swap_retval = -EAGAIN` — 기본 반환값을 미리 실패로 설정. 깨우는 쪽이 덮어쓰지 않으면 타임아웃으로 귀결됩니다.
2. 호출자의 락을 놓고 `_sched_spinlock`을 잡습니다. **인터럽트 락은 절대 놓지 않습니다**([kswap.h:96-99](zephyr/kernel/include/kswap.h#L96-L99)).
3. 다음 스레드 결정 — SMP는 `z_swap_next_thread()`, UP은 `_kernel.ready_q.cache`.
4. 교체가 필요하면 `z_sched_switch_spin()` → `arch_switch()`.

### 7.2 인터럽트 복귀 스위치 — `z_get_next_switch_handle()`

[sched.c:687-761](zephyr/kernel/sched.c#L687-L761). 인터럽트가 끝날 때 아키텍처 코드가 호출합니다.

SMP 경로에서 스핀락 안의 순서가 특히 민감합니다:

```c
new_thread = next_up();
z_sched_usage_switch(new_thread);
if (old_thread != new_thread) {
	z_sched_switch_spin(new_thread);
	arch_cohere_stacks(old_thread, interrupted, new_thread);
	...
	if (z_is_thread_queued(old_thread))
		runq_add(old_thread);            /* 여기서야 큐에 복귀 */
}
old_thread->switch_handle = interrupted;
ret = new_thread->switch_handle;
new_thread->switch_handle = NULL;        /* 실행 중 스레드는 반드시 NULL */
signal_pending_ipi();
```

### 7.3 `switch_handle`과 SMP 레이스

`z_sched_switch_spin()`의 주석이 이 설계에서 가장 미묘한 부분을 설명합니다([kswap.h:37-54](zephyr/kernel/include/kswap.h#L37-L54)).

> 스레드가 "실행 중이 아님"을 자기 문맥에서 표시해야 하는데, 그 순간에도 명백히 아직 실행 중입니다.

스레드 레코드는 `arch_switch()`가 상태 저장을 마치기 **전에** 이미 큐에 올라가 다른 CPU에 보입니다. 그래서 다른 CPU는 진입 전에 `switch_handle`이 non-NULL이 될 때까지 스핀해야 합니다.

- `switch_handle == NULL` ⟺ 그 스레드는 현재 어딘가에서 실행 중이다
- `switch_handle != NULL` ⟺ 상태 저장이 끝났으니 인수해도 된다

락으로는 해결할 수 없는 문제입니다 — 스위치되어 들어온 스레드는 자기가 스위치되었다는 사실 자체를 모르므로 락을 놓을 수 없기 때문입니다.

SMP에서 `do_swap()`이 `runq_add(old_thread)`를 **스위치 도달이 확정된 뒤로** 미루는 이유도 같은 맥락입니다(호출과 `arch_switch()` 사이에는 IPI 발송·`switch_handle` 처리·락 해제가 끼어 있습니다. 소스가 보장하는 것은 인접이 아니라 "유한 시간 내 스위치 도달"입니다)([kswap.h:141-151](zephyr/kernel/include/kswap.h#L141-L151)): 현재 스레드가 유한 시간 내 스위치에 도달하는 것이 확정되기 전에 큐에 올리면, 여러 CPU가 서로의 스레드를 고르고 영원히 기다리는 **순환 데드락**이 발생합니다.

## 8. 블록과 깨우기

### 펜드 — `z_pend_curr()`

[sched.c:491-522](zephyr/kernel/sched.c#L491-L522)

```c
if (arch_is_in_isr()) {
	__ASSERT(false, "blocking pend from ISR context");
	k_panic();
}
```

가장 먼저 ISR 문맥 여부를 검사하고 **모든 빌드에서** 패닉시킵니다. 주석은 이것이 안전하게 복구할 수 없는 프로그래밍 오류임을 밝힙니다 — 인터럽트당한 아무 스레드나 재우게 되고, `CONFIG_SWAP_NONATOMIC`에서는 스케줄러 자체를 손상시킵니다. 부작용을 남기지 않도록 **검사를 함수 맨 앞에 배치**했다는 점까지 주석에 명시되어 있습니다([sched.c:494-499](zephyr/kernel/sched.c#L494-L499)).

이어지는 "락 스왑"도 특징적입니다: 호출자의 락을 놓기 전에 `_sched_spinlock`을 먼저 잡아, 컨텍스트 스위치에 도달할 때까지 스케줄러 락과 인터럽트 마스크를 유지합니다([sched.c:509-521](zephyr/kernel/sched.c#L509-L521)).

### 깨우기 — `z_sched_wake()`

[ksched.h:299-316](zephyr/kernel/include/ksched.h#L299-L316)

```c
LOCK_SCHED_SPINLOCK {
	thread = z_unpend_first_thread_locked(wait_q);
	if (thread != NULL) {
		z_thread_return_value_set_with_data(thread, swap_retval, swap_data);
		z_sched_ready_locked(thread);
		ret = true;
	}
}
```

**언펜드 → 반환값 설정 → 레디 전환**을 반드시 **하나의 락 획득 안에서** 끝내야 합니다. 이유가 [ksched.h:240-255](zephyr/kernel/include/ksched.h#L240-L255)에 상세히 적혀 있습니다: 중간에 락을 놓으면, 다른 CPU에서 진행 중인 타임아웃 핸들러가 먼저 스레드를 레디로 만들어 버리고, 그 스레드가 실행되어 **낡은 `swap_retval`을 보게** 됩니다.

주석은 이 문제를 과거에 "dticks 취소 검사"로 막았으나, 지금은 **모든 wake 작업을 스케줄러 락 아래에서 원자적으로 수행하는 것**이 해법이라고 기술합니다(커밋 `1b8c7a3` 이전/이후 언급). 락 범위 자체를 불변식으로 삼은 사례입니다.

### 타임아웃 경합 — superseded 표시

같은 문제의 반대편이 `z_thread_timeout()`입니다([sched.c:471-488](zephyr/kernel/sched.c#L471-L488)).

```c
if (!z_timeout_inflight_superseded(timeout)) {
	z_sched_wake_thread_locked(thread);
}
```

다른 CPU의 waker가 스레드를 깨워 이미 다른 곳에 다시 pend시켰을 수 있습니다. waker는 타임아웃을 abort하면서 "superseded" 표시를 남기고, **비행 중이던 핸들러는 뒤늦게 실행될 때 이 표시를 보고 물러납니다**. 그렇지 않으면 스레드를 엉뚱한 대기에서 깨우게 됩니다.

이 프로토콜 때문에 `z_unpend_all()`은 타임아웃 abort 결과와 **무관하게** 스레드를 레디로 만듭니다([sched.c:772-778](zephyr/kernel/sched.c#L772-L778)) — 비행 중인 핸들러는 어차피 물러날 것이므로, 깨우는 책임은 전적으로 abort한 쪽에 있습니다.

## 9. 타임슬라이싱

[timeslicing.c](zephyr/kernel/timeslicing.c). CPU별 `_timeout` 하나와 만료 플래그로 구현됩니다([timeslicing.c:14-15](zephyr/kernel/timeslicing.c#L14-L15)).

슬라이스를 받을 자격은 `z_time_slice_size()`가 판정합니다([timeslicing.c:40-60](zephyr/kernel/timeslicing.c#L40-L60)):

- 실행 불가 상태이거나 아이들 스레드면 0 (슬라이싱 없음)
- `CONFIG_TIMESLICE_PER_THREAD`로 스레드가 자기 슬라이스를 정했으면 그 값 우선
- 그 외에는 **선점 가능하고** 우선순위가 `slice_max_prio`보다 높지 않을 때만 전역 슬라이스 적용

만료 처리 `z_time_slice()`([timeslicing.c:147-205](zephyr/kernel/timeslicing.c#L147-L205))에서 두 가지가 눈에 띕니다.

**첫째, 재무장 위치의 조건 분기.** 스위치가 일어나지 않으면 디스패치 경로가 재무장해 주지 않으므로 **여기서 직접 재무장**해야 합니다. 그런데 그 판정 조건이 **UP과 SMP에서 다릅니다**([timeslicing.c:191-201](zephyr/kernel/timeslicing.c#L191-L201)).

```c
#ifdef CONFIG_SMP
		struct k_thread *next = runq_best();

		if (next == NULL || z_is_idle_thread_object(next)) {
			slice_reset(slice_size);
		}
#else
		if (_kernel.ready_q.cache == curr) {
			slice_reset(slice_size);
		}
#endif
```

| 모드 | 재무장 조건 | 의미 |
|---|---|---|
| UP | `ready_q.cache == curr` | 회전 후에도 현재 스레드가 여전히 최선 |
| **SMP (타깃)** | `next == NULL \|\| z_is_idle_thread_object(next)` | **레디 큐가 비었거나 아이들뿐** |

SMP 조건은 "같은 우선순위의 경쟁자가 없다"가 아니라 **"큐에 실행할 것이 아무것도 없다"**입니다. 게다가 SMP에서는 `move_current_to_end_of_prio_q()`의 **회전 자체가 no-op**입니다 — `runq_yield()`가 부르는 `z_priq_*_yield()`의 본문이 전부 `#ifndef CONFIG_SMP`이고(§3.1), `update_cache()`도 `swap_ok` 대입만 하기 때문입니다(§6).

그 결과 **큐에 자기보다 낮은 우선순위의 런너블 스레드만 있는 경우** 틈이 생깁니다. `next_up()`이 현재 스레드를 유지해 스위치가 없는데([sched.c:101-111](zephyr/kernel/sched.c#L101-L111)) SMP 재무장 조건도 거짓이라, 어느 쪽도 재무장하지 않고 다음 틱에 `slice_expired`가 선 채로 `z_time_slice()`가 다시 들어옵니다. (확신도: high — 두 분기의 조건식과 `next_up()`의 유지 조건에서 직접 도출)

**둘째, `slice_reset()`의 -1 보정**([timeslicing.c:85-95](zephyr/kernel/timeslicing.c#L85-L95)). 슬라이서가 막 발화해서 호출된 경우, 틱 경계에 있지만 announce 창을 지난 상태입니다. `z_add_timeout()`의 "+1" 반올림을 상쇄하기 위해 1을 빼야 정확히 `slice_size` 틱에 착지합니다.

`CONFIG_SWAP_NONATOMIC`용 `pending_current` 처리도 있습니다([timeslicing.c:17-24](zephyr/kernel/timeslicing.c#L17-L24), [:152-159](zephyr/kernel/timeslicing.c#L152-L159)): 스레드가 스스로 pend한 뒤 컨텍스트 스위치 전에 타이머 인터럽트가 들어와 그 스레드를 타임슬라이스로 밀어내려는 상황을 무시합니다. 이 변수는 `timeslicing.c`가 소유하고 `sched.c`와 `sleep.c`가 `extern`으로 참조합니다([sched.c:30-33](zephyr/kernel/sched.c#L30-L33), [sleep.c:29-35](zephyr/kernel/sleep.c#L29-L35)).

## 10. 스케줄러 락

`_sched_spinlock` 하나가 스케줄러 상태 전체를 보호합니다([sched.c:35](zephyr/kernel/sched.c#L35)).

UP에서는 스핀락이 실질적으로 무의미하므로 매크로 자체가 사라집니다([ksched.h:41-45](zephyr/kernel/include/ksched.h#L41-L45)):

```c
#if (CONFIG_MP_MAX_NUM_CPUS == 1)
#define LOCK_SCHED_SPINLOCK
#else
#define LOCK_SCHED_SPINLOCK   K_SPINLOCK(&_sched_spinlock)
#endif
```

`k_sched_lock()`은 이와 별개로 **스레드 단위 선점 금지**입니다([scheduler.c:26-38](zephyr/kernel/scheduler.c#L26-L38)) — `sched_locked` 카운터를 감소시켜 `preempt` 값을 임계 이상으로 올립니다(§2, `01-커널-코어-자료구조.md` 4.2 참조). 잠금 해제 시에는 카운터를 복구하고 곧바로 재스케줄합니다([scheduler.c:40-50](zephyr/kernel/scheduler.c#L40-L50)).

`z_thread_halt()` 계열 함수들은 "락을 잡은 채 호출하고, 함수가 락을 놓는다"는 규약을 씁니다. `sched.c:347-350`의 경고가 이 규약의 위험성을 보여줍니다:

> 스케줄러 락이 해제되었습니다. 여기에 로직을 넣지 마십시오. 락을 다시 잡더라도 레이스/데드락이 되기 쉽습니다.

## 11. 정리

| 축 | UP | SMP |
|---|---|---|
| 실행 중 스레드의 큐 존재 | 큐 안에 남음 | 큐 밖 |
| 다음 스레드 결정 | `ready_q.cache` 사전 계산 | 스위치 시점에 `next_up()` |
| `update_cache()` | 실제 재계산 | `swap_ok` 대입만 |
| 선점 판정 시점 | 큐 조작 시 | 스위치 시 |
| 필요한 동기화 | 인터럽트 마스크 | `_sched_spinlock` + `switch_handle` 스핀 + IPI |

두 모드가 같은 함수 이름을 공유하지만 내부 동작은 상당히 다릅니다. 스케줄러 코드를 읽을 때는 **먼저 UP/SMP 중 어느 쪽을 보고 있는지 확정**하는 것이 필요합니다.
