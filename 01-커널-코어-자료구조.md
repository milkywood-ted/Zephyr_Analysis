# 커널 코어 자료구조

분석 대상: [zephyr/include/zephyr/kernel_structs.h](zephyr/include/zephyr/kernel_structs.h), [zephyr/include/zephyr/kernel/thread.h](zephyr/include/zephyr/kernel/thread.h)

> **타깃 적용 (`qemu_cortex_a53` SMP)** — 전체 구성은 [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §4 참조
>
> | 본 문서 항목 | 이 타깃에서 |
> |---|---|
> | `_ready_q.cache` (§5) | **없음.** `CONFIG_SMP=y`이므로 필드 자체가 컴파일되지 않음 |
> | `_ready_q.runq` (§5) | `sys_dlist_t` — `SCHED_SIMPLE` |
> | `_cpu.ready_q` (§2) | **없음.** `SCHED_CPU_MASK_PIN_ONLY=n` → 레디 큐는 `_kernel.ready_q` 전역 1개 |
> | `_wait_q_t` (§6) | `sys_dlist_t` — `WAITQ_SIMPLE` |
> | `_timeout` (§7) | `sys_dnode_t node` + `int64_t dticks` — `TIMEOUT_BACKEND_DLIST` + `TIMEOUT_64BIT` |
> | `_thread_base.cpu_mask` (§4.4) | **없음.** `SCHED_CPU_MASK=n` |
> | `_thread_base.prio_deadline` | **없음.** `SCHED_DEADLINE=n` |
> | `_thread_base.order_key` | **없음.** SCALABLE 백엔드 미사용 |
> | `_thread_base.is_idle` / `cpu` / `global_lock_count` | **있음.** `CONFIG_SMP=y` |
> | `k_thread.halt_queue` | **있음.** `CONFIG_SMP=y` |
> | `k_thread.switch_handle` / `swap_retval` | **있음.** `USE_SWITCH=y` (ARM64 강제) |
> | 우선순위 공간 | `-16` ~ `15`, `K_NUM_THREAD_PRIO = 32` |

## 1. 헤더 계층 규칙

`kernel_structs.h` 최상단에 설계 규칙이 명시되어 있습니다([kernel_structs.h:11-17](zephyr/include/zephyr/kernel_structs.h#L11-L17)).

1. `kernel_structs.h`는 `kernel.h`에 직접·간접으로 의존해서는 안 된다.
2. `kernel.h`는 `kernel_structs.h`를 포함해야 한다.

즉 **최소한의 구조체 정의만 담는 하위 계층**으로 설계되어, 아키텍처 코드나 어셈블리 오프셋 생성기가 `kernel.h` 전체를 끌어오지 않고도 스레드/CPU 구조를 참조할 수 있습니다. 실제로 파일 전체가 `#if !defined(_ASMLANGUAGE)`로 감싸여 어셈블리에서 매크로만 취하도록 되어 있습니다([kernel_structs.h:23](zephyr/include/zephyr/kernel_structs.h#L23), [:91](zephyr/include/zephyr/kernel_structs.h#L91)).

## 2. 전역 커널 상태 — `struct z_kernel`

[kernel_structs.h:208-238](zephyr/include/zephyr/kernel_structs.h#L208-L238)

```c
struct z_kernel {
	struct _cpu cpus[CONFIG_MP_MAX_NUM_CPUS];
#ifdef CONFIG_PM
	int32_t idle;
#endif
#ifndef CONFIG_SCHED_CPU_MASK_PIN_ONLY
	struct _ready_q ready_q;
#endif
	...
};
extern struct z_kernel _kernel;
```

전역 인스턴스는 `_kernel` 하나뿐입니다([kernel_structs.h:242](zephyr/include/zephyr/kernel_structs.h#L242)).

주목할 점:

- **레디 큐의 위치가 Kconfig에 따라 이동합니다.** 기본은 `_kernel.ready_q` 하나를 전 CPU가 공유하지만, `CONFIG_SCHED_CPU_MASK_PIN_ONLY`가 켜지면 레디 큐가 `_cpu` 안으로 내려가 CPU별로 하나씩 존재합니다([kernel_structs.h:160-162](zephyr/include/zephyr/kernel_structs.h#L160-L162), [:219-221](zephyr/include/zephyr/kernel_structs.h#L219-L221)). 이 분기를 흡수하는 것이 [run_q.h](zephyr/kernel/include/run_q.h)의 `thread_runq()` / `curr_cpu_runq()`입니다.
- **필드 순서에 어셈블리 제약이 있습니다.** `ready_q`를 작은 필드들 뒤에 두라는 주석이 달려 있으며, 이유는 ARC 등 일부 아키텍처의 어셈블리가 오프셋 인코딩 범위에 제한이 있기 때문입니다([kernel_structs.h:215-218](zephyr/include/zephyr/kernel_structs.h#L215-L218)). 구조체 필드 순서가 성능이 아니라 **명령어 인코딩** 때문에 고정되어 있는 사례입니다.

## 3. CPU별 상태 — `struct _cpu`

[kernel_structs.h:147-204](zephyr/include/zephyr/kernel_structs.h#L147-L204)

핵심 필드:

| 필드 | 의미 |
|---|---|
| `nested` | 인터럽트 중첩 깊이 |
| `irq_stack` | 인터럽트 스택 베이스 |
| `current` | 이 CPU에서 현재 실행 중인 스레드 |
| `idle_thread` | 이 CPU 전용 아이들 스레드 (CPU마다 1개) |
| `id` | CPU 번호 |
| `swap_ok` | SMP 전용. "지금 컨텍스트 스위치해도 되는가" 플래그 |
| `metairq_preempted` | 현재 MetaIRQ에 의해 선점당한 협조적 스레드 |
| `arch` | 아키텍처별 확장 (`struct _cpu_arch`) |

`swap_ok`가 스레드가 아니라 **CPU에 달려 있는 이유**가 주석에 설명되어 있습니다([sched.c:164-169](zephyr/kernel/sched.c#L164-L169)): 스레드가 어떤 이유로든 선점되면 스케줄러는 어차피 같은 결정을 내리므로, 스레드별로 추적할 필요가 없다는 논리입니다.

### `_current` 매크로

```c
#ifdef CONFIG_SMP
#define _current_cpu ({ __ASSERT_NO_MSG(!z_smp_cpu_mobile()); arch_curr_cpu(); })
#define _current z_smp_current_get()
#else
#define _current_cpu (&_kernel.cpus[0])
#define _current _kernel.cpus[0].current
#endif
```

[kernel_structs.h:246-274](zephyr/include/zephyr/kernel_structs.h#L246-L274)

- UP에서는 단순한 전역 접근이지만, SMP에서는 함수 호출입니다.
- `_current_cpu`에는 `z_smp_cpu_mobile()` 어서션이 걸려 있습니다. 선점 가능한 문맥에서 "현재 CPU"를 잡는 것은 그 자체로 레이스이므로, 디버그 빌드에서 잡아냅니다.
- `CONFIG_ARCH_HAS_CUSTOM_CURRENT_IMPL`이 켜지면 `_current`가 `arch_current_thread()`로 재정의됩니다. 전용 레지스터나 TLS에 현재 스레드를 두는 아키텍처를 위한 훅입니다.

## 4. 스레드 — `struct k_thread` / `struct _thread_base`

[thread.h:46-134](zephyr/include/zephyr/kernel/thread.h#L46-L134) (`_thread_base`), [thread.h:259-380](zephyr/include/zephyr/kernel/thread.h#L259-L380) (`k_thread`)

`k_thread`는 `_thread_base base`를 **첫 멤버**로 두고 시작합니다. 스케줄러가 다루는 최소 정보를 `_thread_base`에 모아 두었기 때문에, 실제 스택이 없는 "더미 스레드"도 이 구조로 큐에 넣을 수 있습니다(헤더 주석: "can be used for creating 'dummy' threads").

### 4.1 큐 노드 유니온

```c
union {
	sys_dnode_t qnode_dlist;
	struct rbnode qnode_rb;
};
```

[thread.h:49-52](zephyr/include/zephyr/kernel/thread.h#L49-L52)

연결 리스트 백엔드와 레드블랙 트리 백엔드가 **같은 메모리를 공유**합니다. 스레드는 동시에 레디 큐와 대기 큐 양쪽에 들어가지 않으므로 노드 하나로 충분합니다.

### 4.2 `preempt` 유니온 — 선점 가능성을 한 번의 비교로

`_thread_base`에서 가장 기교적인 부분입니다([thread.h:62-87](zephyr/include/zephyr/kernel/thread.h#L62-L87)).

```c
union {
	struct {
#ifdef CONFIG_BIG_ENDIAN
		uint8_t sched_locked;
		int8_t prio;
#else
		int8_t prio;
		uint8_t sched_locked;
#endif
	};
	uint16_t preempt;
};
```

설계 의도:

- `prio`가 음수면 협조적(coop) 스레드입니다. `int8_t`를 부호 없이 보면 `0x80`~`0xff` 범위입니다.
- `sched_locked`는 `k_sched_lock()` 시 **감소**합니다(`--_current->base.sched_locked`, [scheduler.c:34](zephyr/kernel/scheduler.c#L34)). 따라서 잠긴 동안 값은 `0xff`~`0x01`입니다.
- 두 바이트를 이어 붙인 `preempt`가 `0x0080` 이상이면 선점 불가입니다.

```c
#define _NON_PREEMPT_THRESHOLD 0x0080U
#define _PREEMPT_THRESHOLD (_NON_PREEMPT_THRESHOLD - 1U)
```
[kernel_structs.h:86-89](zephyr/include/zephyr/kernel_structs.h#L86-L89)

```c
static inline int thread_is_preemptible(const struct k_thread *thread)
{
	return thread->base.preempt <= _PREEMPT_THRESHOLD;
}
```
[kthread.h:65-69](zephyr/kernel/include/kthread.h#L65-L69)

결과적으로 "**협조적 스레드인가**" 와 "**스케줄러가 잠겨 있는가**" 라는 두 조건이 **16비트 비교 한 번**으로 판정됩니다. 엔디안별 필드 순서 분기가 있는 이유도 이 바이트 결합이 성립해야 하기 때문입니다. (확신도: high — 주석과 임계값 정의가 논리를 그대로 설명)

### 4.3 스레드 상태 비트

[kernel_structs.h:55-76](zephyr/include/zephyr/kernel_structs.h#L55-L76). `thread_state`는 `uint8_t` 하나이며 비트 8개를 모두 사용합니다.

| 비트 | 매크로 | 의미 |
|---|---|---|
| 0 | `_THREAD_DUMMY` | 실제 스레드가 아님 (부트/스위치 보조용) |
| 1 | `_THREAD_PENDING` | 객체 대기 중 |
| 2 | `_THREAD_SLEEPING` | 슬립 중 |
| 3 | `_THREAD_DEAD` | 종료됨 |
| 4 | `_THREAD_SUSPENDED` | 서스펜드됨 |
| 5 | `_THREAD_ABORTING` | 어보트 진행 중 |
| 6 | `_THREAD_SUSPENDING` | 서스펜드 진행 중 |
| 7 | `_THREAD_QUEUED` | 레디 큐에 등록됨 |

핵심 파생 판정 두 가지:

```c
static inline bool z_is_thread_prevented_from_running(const struct k_thread *thread)
{
	uint8_t state = thread->base.thread_state;
	return (state & (_THREAD_PENDING | _THREAD_SLEEPING | _THREAD_DEAD |
			 _THREAD_DUMMY | _THREAD_SUSPENDED)) != 0U;
}
static inline bool z_is_thread_ready(const struct k_thread *thread)
{
	return !z_is_thread_prevented_from_running(thread);
}
```
[kthread.h:117-133](zephyr/kernel/include/kthread.h#L117-L133)

주의할 점은 **`_THREAD_QUEUED`가 "ready"의 정의에 들어가지 않는다**는 것입니다. "실행 가능(ready)"과 "레디 큐에 들어 있음(queued)"은 별개 개념이며, SMP에서 실행 중인 스레드는 ready이면서 queued가 아닙니다([sched.c:89-93](zephyr/kernel/sched.c#L89-L93) 주석). 이 구분이 스케줄러 코드 전반의 조건문을 이해하는 열쇠입니다.

`_THREAD_ABORTING`/`_THREAD_SUSPENDING`은 SMP에서 "다른 CPU에서 실행 중인 스레드를 멈추라고 요청했고, 아직 멈추지 않았다"는 중간 상태입니다. 다른 CPU가 이 비트를 **락 없이 스핀하며 관찰**하므로, 해제 시 메모리 배리어가 필요합니다([sched.c:48-54](zephyr/kernel/sched.c#L48-L54)).

### 4.4 조건부 필드

`k_thread`는 대부분의 멤버가 `#ifdef`로 감싸여 있습니다. 예: `CONFIG_THREAD_NAME`(이름 배열), `CONFIG_USERSPACE`(메모리 도메인, 스택 오브젝트, syscall 프레임), `CONFIG_SCHED_THREAD_USAGE`(사이클 통계), `CONFIG_SMP`(`halt_queue`). 임베디드 타깃에서 스레드 하나당 RAM을 최소화하기 위한 구조이며, **구조체 크기가 빌드 구성에 따라 크게 달라진다**는 뜻이기도 합니다.

`arch` 멤버는 반드시 마지막에 위치해야 한다는 주석이 붙어 있습니다([thread.h:378-379](zephyr/include/zephyr/kernel/thread.h#L378-L379)).

## 5. 레디 큐 — `struct _ready_q`

[kernel_structs.h:130-143](zephyr/include/zephyr/kernel_structs.h#L130-L143)

```c
struct _ready_q {
#ifndef CONFIG_SMP
	struct k_thread *cache;   /* 항상 다음 실행 스레드. NULL일 수 없음 */
#endif
#if defined(CONFIG_SCHED_SIMPLE)
	sys_dlist_t runq;
#elif defined(CONFIG_SCHED_SCALABLE)
	struct _priq_rb runq;
#elif defined(CONFIG_SCHED_MULTIQ)
	struct _priq_mq runq;
#endif
};
```

- `cache`는 **UP 전용**입니다. 다음에 실행할 스레드를 미리 계산해 두어, 컨텍스트 스위치 판단을 포인터 비교 한 번으로 끝냅니다. SMP에서는 다른 CPU가 언제든 큐를 바꿀 수 있어 캐시가 성립하지 않으므로 필드 자체가 사라집니다([sched.c:82-87](zephyr/kernel/sched.c#L82-L87) 주석).
- 세 가지 백엔드의 트레이드오프는 헤더 주석 [kernel_structs.h:93-115](zephyr/include/zephyr/kernel_structs.h#L93-L115)에 정리되어 있으며, 상세는 `02-스케줄러.md`에서 다룹니다.

## 6. 대기 큐 — `_wait_q_t`

[kernel_structs.h:276-296](zephyr/include/zephyr/kernel_structs.h#L276-L296)

```c
#ifdef CONFIG_WAITQ_SCALABLE
typedef struct { struct _priq_rb waitq; } _wait_q_t;
#else
typedef struct { sys_dlist_t waitq; } _wait_q_t;
#endif
```

레디 큐와 달리 **멀티큐 옵션이 없습니다.** 대기 큐는 개별 IPC 객체마다 하나씩 존재하므로, 우선순위 개수만큼 리스트 헤드를 두는 방식은 RAM 비용이 감당되지 않기 때문입니다. (확신도: medium — 코드에 명시적 근거는 없고 `priority_q.h:48-58`에서 두 선택지만 제공된다는 사실로부터의 추론)

스레드가 어느 대기 큐에 있는지는 `_thread_base.pended_on`이 가리킵니다([thread.h:57](zephyr/include/zephyr/kernel/thread.h#L57)). 주석은 "트리에서만 필요하고 단순 리스트에는 불필요"하다고 설명하지만, 실제로는 `unpend_thread_no_timeout()`이 백엔드와 무관하게 이 포인터를 사용합니다([ksched.h:233-238](zephyr/kernel/include/ksched.h#L233-L238)).

## 7. 타임아웃 레코드 — `struct _timeout`

[kernel_structs.h:302-336](zephyr/include/zephyr/kernel_structs.h#L302-L336)

```c
struct _timeout {
#if defined(CONFIG_TIMEOUT_BACKEND_MINHEAP)
	int64_t abs_ticks;
	struct min_heap_handle heap_handle;
#else
	sys_dnode_t node;
#if defined(CONFIG_TIMEOUT_BACKEND_WHEEL)
	uint32_t flags;
#endif
	int64_t/int32_t dticks;
#endif
	_timeout_func_t fn;      /* 모든 백엔드 공통, 항상 마지막 */
};
```

설계 규칙이 주석으로 못박혀 있습니다: **핸들러 포인터 `fn`은 모든 백엔드 공통이며 항상 마지막 멤버**, 그 위는 선택된 백엔드가 소유합니다. 백엔드는 Kconfig choice `TIMEOUT_BACKEND`로 선택되며([kernel/Kconfig:755-806](zephyr/kernel/Kconfig#L755-L806)) 네 가지가 있습니다: dlist(델타 리스트), bucket, wheel(타이머 휠), minheap.

백엔드마다 "이 타임아웃이 큐에 없다"를 표현하는 방법이 다르며, 그 판정이 `z_is_inactive_timeout()`에 캡슐화되어 있습니다([timeout_q.h:35-75](zephyr/kernel/include/timeout_q.h#L35-L75)) — minheap은 `heap_handle.idx == 0`, 나머지는 `!sys_dnode_is_linked(&to->node)`.

스레드는 이 레코드를 `base.timeout`에 **값으로** 하나 품고 있습니다([thread.h:120-123](zephyr/include/zephyr/kernel/thread.h#L120-L123)). 즉 한 스레드는 동시에 두 개의 타임아웃을 가질 수 없으며, 슬립·펜드 타임아웃·조인 타임아웃이 모두 이 한 슬롯을 공유합니다. (확신도: high — 필드가 단일 인스턴스이고 `z_add_thread_timeout()`이 항상 `&thread->base.timeout`을 넘김, [timeout_q.h:118-121](zephyr/kernel/include/timeout_q.h#L118-L121))

## 8. 요약 — 자료구조 간 관계

```
_kernel (전역 1개)
 ├─ cpus[CONFIG_MP_MAX_NUM_CPUS] : struct _cpu
 │   ├─ current      ──────────────▶ struct k_thread  (실행 중)
 │   ├─ idle_thread  ──────────────▶ struct k_thread
 │   └─ ready_q                       (CPU_MASK_PIN_ONLY 일 때만)
 └─ ready_q : struct _ready_q         (기본: 전 CPU 공유)
     ├─ cache ────────────────────▶ struct k_thread  (UP 전용)
     └─ runq  ── dlist / rbtree / multiq 중 택1
                    │
                    └─ 노드는 k_thread.base.qnode_dlist | qnode_rb (유니온)

struct k_thread
 ├─ base : _thread_base
 │   ├─ preempt(= prio + sched_locked)   선점 판정
 │   ├─ thread_state                     8비트 상태
 │   ├─ pended_on ────────────────▶ _wait_q_t  (대기 중인 객체의 큐)
 │   └─ timeout : struct _timeout        스레드당 1개
 ├─ join_queue : _wait_q_t               join 대기자들
 └─ halt_queue : _wait_q_t               (SMP) suspend 완료 대기자들
```

## 9. 다음 문서

- 레디 큐 백엔드 3종의 알고리즘과 스레드 선택 로직: `02-스케줄러.md`
- 상태 비트가 실제로 전이되는 경로: `03-스레드-생명주기.md`
