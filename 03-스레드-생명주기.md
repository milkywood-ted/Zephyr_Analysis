# 스레드 생명주기

분석 대상: [zephyr/kernel/thread.c](zephyr/kernel/thread.c), [zephyr/kernel/sched.c](zephyr/kernel/sched.c), [zephyr/kernel/sleep.c](zephyr/kernel/sleep.c)

> **타깃 적용 (`qemu_cortex_a53` SMP)** — 전체 구성은 [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §4 참조
>
> | 본 문서 항목 | 이 타깃에서 |
> |---|---|
> | §1 `_THREAD_ABORTING` / `_THREAD_SUSPENDING` | **활성.** SMP 2코어이므로 실제로 도달하는 중간 상태 |
> | §6 `k_thread_suspend()` UP 빠른 경로 | **비활성.** `IS_ENABLED(CONFIG_SMP)` 조건에 막힘 → 항상 `z_thread_halt()` 경유 |
> | §7.2 다른 CPU 대상 halt (IPI + 스핀/블록) | **활성.** 이 타깃의 주 경로 |
> | §7.2 IPI 방식 | **directed.** `ARCH_HAS_DIRECTED_IPIS=y` ([sched.c:308](zephyr/kernel/sched.c#L308)) |
> | §7.3 `unpend_all(&thread->halt_queue)` | **활성.** `CONFIG_SMP=y` |
> | §7.3 더미화 시 `switch_handle = (void *)1` | **활성.** `USE_SWITCH=y` |
> | §8 `k_thread_join()`의 `z_sched_switch_spin()` | **활성.** SMP에서만 실제로 스핀 |
> | §9 지연 정리 | **비활성.** `THREAD_ABORT_NEED_CLEANUP=n` (`THREAD_STACK_MEM_MAPPED`가 기본 n) |
> | §5 펜딩 타임아웃 | `TIMEOUT_BACKEND_DLIST` + 64비트 `dticks` |
> | 각 API의 `z_vrfy_*` 검증 계층 | **비활성.** `USERSPACE=n` |
>
> 요약하면 이 타깃은 **abort/suspend의 크로스-CPU 경로(§7.2)가 주 경로**가 되며, 단일 코어 전용 단축 경로들은 타지 않습니다.

## 1. 상태 모델

스레드 상태는 `_thread_base.thread_state`의 비트 조합입니다(`01-커널-코어-자료구조.md` §4.3). **배타적 상태 머신이 아니라 플래그 집합**이므로, 예를 들어 "펜딩이면서 큐에 들어 있지 않은" 상태처럼 조합으로 표현됩니다.

주요 전이:

```
        k_thread_create()
              │
              ▼
      ┌───────────────┐  k_thread_start()/k_wakeup()   ┌──────────┐
      │  SLEEPING     │ ─────────────────────────────▶ │  READY   │
      │  (생성 직후)   │                                │ (+QUEUED)│
      └───────────────┘ ◀───────────────────────────── └──────────┘
              ▲              k_sleep() / 타임아웃 대기      │  ▲
              │                                            │  │
              │                                    스케줄됨 │  │ 선점/양보
              │                                            ▼  │
              │                                       ┌──────────┐
              │                                       │ RUNNING  │
              │                                       │(_current)│
              │                                       └──────────┘
              │                                         │  │   │
   k_sem_take 등 │  z_sched_wake()                       │  │   │
        ┌───────┴──────┐ ◀───────────────────────────────┘  │   │
        │   PENDING    │                                    │   │
        └──────────────┘                                    │   │
                                                            │   │
        ┌──────────────┐  k_thread_resume()                 │   │
        │  SUSPENDED   │ ◀──────────────────────────────────┘   │
        └──────────────┘  k_thread_suspend()                    │
                                                                │
        ┌──────────────┐  k_thread_abort() / 엔트리 함수 반환      │
        │     DEAD     │ ◀─────────────────────────────────────-─┘
        └──────────────┘
```

중간 상태 `_THREAD_ABORTING` / `_THREAD_SUSPENDING`은 SMP에서 "다른 CPU에서 실행 중인 스레드에게 멈추라고 요청했고 아직 이행되지 않은" 구간에만 존재합니다.

## 2. 생성

### 2.1 `k_thread_create()`

[thread.c:996-1012](zephyr/kernel/thread.c#L996-L1012)

```c
k_tid_t z_impl_k_thread_create(...)
{
	__ASSERT(!arch_is_in_isr(), "Threads may not be created in ISRs");
	z_setup_new_thread(new_thread, stack, stack_size, entry, p1, p2, p3,
			   prio, options, NULL);
	if (!K_TIMEOUT_EQ(delay, K_FOREVER)) {
		thread_schedule_new(new_thread, delay);
	}
	return new_thread;
}
```

`delay`의 의미:

| `delay` | 동작 |
|---|---|
| `K_NO_WAIT` | 즉시 `k_thread_start()` ([kthread.h:54-55](zephyr/kernel/include/kthread.h#L54-L55)) |
| 유한 시간 | `z_add_thread_timeout()`으로 타임아웃 등록 — 만료 시 자동 시작 |
| `K_FOREVER` | 아무것도 하지 않음. 명시적 `k_thread_start()` 필요 |

`K_FOREVER`는 `thread_schedule_new()` 안이 아니라 **호출자에서 걸러집니다.** 즉 `K_FOREVER`면 타임아웃 자체를 등록하지 않습니다.

### 2.2 `z_setup_new_thread()` — 초기화 순서

[thread.c:881-931](zephyr/kernel/thread.c#L881-L931)

```c
Z_ASSERT_VALID_PRIO(prio, entry);
thread_abort_cleanup_check_reuse(new_thread);      /* 1 */
init_thread_obj_core(new_thread);
init_thread_userspace(new_thread, stack, options);
z_waitq_init(&new_thread->join_queue);
z_init_thread_base(&new_thread->base, prio, _THREAD_SLEEPING, options);  /* 2 */
stack_ptr = setup_thread_stack(new_thread, stack, stack_size);
setup_shadow_stack(new_thread, stack);
assert_thread_coherence(new_thread, stack);
arch_new_thread(new_thread, stack, stack_ptr, entry, p1, p2, p3);        /* 3 */
new_thread->init_data = NULL;
...
new_thread->resource_pool = _current->resource_pool;                      /* 4 */
```

주목할 지점:

1. **재사용 방어** — `thread_abort_cleanup_check_reuse()`는 같은 `k_thread` 객체가 재사용될 때, 이전 인스턴스의 지연된 정리 작업이 남아 있으면 먼저 끝냅니다([thread.c:1540-1552](zephyr/kernel/thread.c#L1540-L1552)).
2. **초기 상태는 `_THREAD_SLEEPING`** — "생성됨"이라는 별도 상태가 없습니다. 그래서 시작이 곧 깨우기입니다(§3).
3. **`arch_new_thread()`** — 스택에 초기 컨텍스트를 심어, 첫 스위치 시 엔트리 함수로 진입하도록 만드는 아키텍처별 훅입니다.
4. **리소스 풀 상속** — 새 스레드는 생성자의 `resource_pool`을 물려받습니다. 단, `CONFIG_ARCH_HAS_CUSTOM_SWAP_TO_MAIN`이면서 `_current == NULL`인 부팅 초기에는 `NULL`을 넣고 조기 반환합니다([thread.c:915-920](zephyr/kernel/thread.c#L915-L920)).

### 2.3 `z_init_thread_base()`

[thread.c:1087-1120](zephyr/kernel/thread.c#L1087-L1120)

큐 노드는 초기화하지 않습니다 — 주석대로 "리스트에 처음 삽입될 때 초기화"됩니다. SMP에서 `cpu = 0`으로 두는 이유도 명시되어 있습니다: `_kernel.cpus[]` 배열 범위 초과 접근을 막기 위해 "마지막으로 CPU0에서 실행된 척"합니다([thread.c:1102-1106](zephyr/kernel/thread.c#L1102-L1106)).

## 3. 시작

`k_thread_start()`는 [kernel.h:1397-1400](zephyr/include/zephyr/kernel.h#L1397-L1400)에서 `k_wakeup()`을 호출하는 인라인 래퍼일 뿐입니다. 헤더 주석이 스스로를 "legacy API for compatibility"라 부르며, 현대 Zephyr 스레드는 sleeping 상태로 초기화되므로 특별한 시작 처리가 필요 없다고 밝힙니다.

```c
void z_impl_k_wakeup(k_tid_t thread)
{
	k_spinlock_key_t key = k_spin_lock(&_sched_spinlock);
	if (z_is_thread_sleeping(thread)) {
		(void)z_try_abort_thread_timeout(thread);
		z_mark_thread_as_not_sleeping(thread);
		z_sched_ready_locked(thread);
		z_reschedule(&_sched_spinlock, key);
	} else {
		k_spin_unlock(&_sched_spinlock, key);
	}
}
```
[thread.c:1692-1706](zephyr/kernel/thread.c#L1692-L1706)

**슬립 상태가 아니면 아무 일도 하지 않습니다.** 따라서 이미 실행 중인 스레드에 `k_thread_start()`를 호출하는 것은 무해한 no-op입니다.

## 4. 슬립과 깨우기

### `k_sleep()` → `z_tick_sleep()`

[sleep.c:48-89](zephyr/kernel/sleep.c#L48-L89)

```c
if (K_TIMEOUT_EQ(timeout, K_NO_WAIT)) { k_yield(); return 0; }   /* 1 */

k_spinlock_key_t key = k_spin_lock(&_sched_spinlock);
z_sched_unready_locked(_current);                                /* 2 */
expected_wakeup_ticks = z_add_thread_timeout(_current, timeout); /* 3 */
z_mark_thread_as_sleeping(_current);
(void)z_swap(&_sched_spinlock, key);                             /* 4 */

uint32_t left_ticks = expected_wakeup_ticks - sys_clock_tick_get_32();
int32_t signed_left = (int32_t)left_ticks;
return (signed_left > 0) ? signed_left : 0;                      /* 5 */
```

1. `K_NO_WAIT` 슬립은 **양보**로 해석됩니다.
2. 레디 큐에서 제거 (§`02-스케줄러.md` §4)
3. 깨어날 절대 틱을 받아 둡니다.
4. 컨텍스트 스위치. 여기서 실행이 멈춥니다.
5. 반환값 계산이 미묘합니다. 정상 만료면 남은 틱이 0 이하이고, **`k_wakeup()`으로 조기 기상한 경우에만 양수**입니다.

부호 처리에 대한 주석이 상세합니다([sleep.c:76-81](zephyr/kernel/sleep.c#L76-L81)): `k_ticks_t`가 `uint32_t`일 수 있으므로(`!CONFIG_TIMEOUT_64BIT`) 부호 없이 비교하면 음수 잔여가 거대한 양수로 오해석됩니다. 그래서 `int32_t`로 캐스팅한 뒤 비교합니다.

### 슬립 상태 스레드의 큐 상태

슬립 중인 스레드는 **어느 대기 큐에도 속하지 않습니다.** `pended_on`은 `NULL`이고, 오직 타임아웃 큐에만 등록됩니다. 이것이 `_THREAD_SLEEPING`과 `_THREAD_PENDING`을 나눈 이유입니다 — 전자는 "시간이 깨워줌", 후자는 "객체가 깨워줌"입니다.

## 5. 펜딩 (객체 대기)

세마포어·뮤텍스 등 IPC 객체 대기는 `z_pend_curr()`로 수렴합니다(`02-스케줄러.md` §8). 스레드 관점의 상태 변화:

```
add_to_waitq_locked():        unready_thread() → _THREAD_PENDING 설정 → wait_q에 삽입
                              pended_on = wait_q
z_add_thread_timeout():       타임아웃 등록 (K_FOREVER이면 무등록)
z_swap():                     컨텍스트 스위치
```
[sched.c:406-433](zephyr/kernel/sched.c#L406-L433)

깨어나는 경로는 두 가지이며 **경합합니다**:

| 경로 | 함수 | 결과 |
|---|---|---|
| 객체가 깨움 | `z_sched_wake()` | `swap_retval`에 성공값, `pended_on = NULL` |
| 타임아웃 만료 | `z_thread_timeout()` | `swap_retval`은 `do_swap()`이 설정한 `-EAGAIN` 유지 |

`do_swap()`이 스위치 전에 `old_thread->swap_retval = -EAGAIN`을 넣어 두므로([kswap.h:94](zephyr/kernel/include/kswap.h#L94)), **깨우는 쪽이 값을 덮어쓰지 않으면 자동으로 타임아웃 실패가 됩니다.** 별도의 "타임아웃이었는가" 플래그가 필요 없는 구조입니다.

`add_to_waitq_locked()` 첫 줄에 `__ASSERT_NO_MSG(thread->base.pended_on == NULL)`이 있습니다([sched.c:409](zephyr/kernel/sched.c#L409)) — 스레드는 동시에 두 대기 큐에 들어갈 수 없습니다.

## 6. 서스펜드와 리줌

### `k_thread_suspend()`

[thread.c:1627-1652](zephyr/kernel/thread.c#L1627-L1652)

```c
if (!IS_ENABLED(CONFIG_SMP) && (thread == _current) && !arch_is_in_isr()) {
	z_thread_suspend_current(thread);   /* 빠른 경로 */
	return;
}
k_spinlock_key_t key = k_spin_lock(&_sched_spinlock);
if (unlikely(z_is_thread_suspended(thread))) { k_spin_unlock(...); return; }
z_thread_halt(thread, key, false);
```

UP에서 자기 자신을 서스펜드하는 경우는 비동기 처리가 불필요하므로 [sched.c:941-950](zephyr/kernel/sched.c#L941-L950)의 짧은 경로로 갑니다. 그 외에는 abort와 **공용 경로** `z_thread_halt()`를 씁니다(`terminate = false`).

### `k_thread_resume()`

[thread.c:1663-1681](zephyr/kernel/thread.c#L1663-L1681). 서스펜드 상태가 아니면 no-op이고, 맞으면 비트를 지우고 레디로 만든 뒤 재스케줄합니다. **대칭적으로 단순**한 것은, 복잡성이 전부 suspend 쪽(다른 CPU에서 실행 중일 수 있음)에 있기 때문입니다.

## 7. 종료

### 7.1 `z_thread_abort()`

[thread.c:1717-1733](zephyr/kernel/thread.c#L1717-L1733)

```c
bool essential = z_is_thread_essential(thread);
k_spinlock_key_t key = k_spin_lock(&_sched_spinlock);
if (z_is_thread_dead(thread)) { k_spin_unlock(&_sched_spinlock, key); return; }
z_thread_halt(thread, key, true);
if (essential) {
	__ASSERT(!essential, "aborted essential thread %p", thread);
	k_panic();
}
```

`essential` 여부를 **halt 전에 미리 읽어 둡니다.** halt 이후에는 스레드 객체가 해제되었을 수 있기 때문입니다. (확신도: high — 동적 스레드의 스토리지 해제 가능성이 `sched.c:832-847` 주석에 명시)

### 7.2 `z_thread_halt()` — suspend/abort 공용 경로

[sched.c:284-351](zephyr/kernel/sched.c#L284-L351). 대상이 **다른 CPU에서 실행 중인지**로 갈립니다.

**대상이 다른 CPU에서 실행 중인 경우:**

```c
thread->base.thread_state |= (terminate ? _THREAD_ABORTING : _THREAD_SUSPENDING);
arch_sched_directed_ipi(...) 또는 arch_sched_broadcast_ipi();
if (arch_is_in_isr()) {
	thread_halt_spin(thread, key);     /* ISR이면 스핀 대기 */
} else {
	add_to_waitq_locked(_current, wq); /* 스레드면 halt_queue/join_queue에서 블록 */
	z_swap(&_sched_spinlock, key);
}
while (z_try_abort_thread_timeout(thread) == -EAGAIN) { }
```

플래그를 세우고 **동기 IPI**를 보냅니다. 지연 IPI가 아니어야 하는 이유는 여기서 스핀 대기할 수 있기 때문이라고 주석에 명시되어 있습니다([sched.c:294-298](zephyr/kernel/sched.c#L294-L298)). 대상 CPU는 인터럽트에서 `next_up()`에 진입할 때 자기 플래그를 보고 스스로 halt합니다([sched.c:58-68](zephyr/kernel/sched.c#L58-L68)).

마지막 `while` 루프가 중요합니다([sched.c:320-332](zephyr/kernel/sched.c#L320-L332) 주석). 대상의 self-halt 경로는 `halt_thread()`에 `NULL` 키를 넘겨 `-EAGAIN` 재시도를 할 수 없었으므로, **제3의 CPU에서 비행 중인 타임아웃 핸들러가 아직 실행되지 않았을 수 있습니다.**

> `-EAGAIN`이 반환되는 정확한 조건과 같은 CPU에서 abort가 일어난 경우의 처리는 [04-타임아웃과-시스템-클럭.md](1.zephyr분석/04-타임아웃과-시스템-클럭.md) §6.3~6.4를 참조하십시오. 요약하면, `-EAGAIN`은 **다른 CPU**에서 핸들러가 비행 중일 때만 나오며, 같은 CPU 케이스는 `-EINVAL` + superseded 표시로 처리됩니다. 호출자가 스레드 스토리지를 해제하기 전에, 락 밖에서 그 핸들러가 끝나기를 기다립니다. 이 루프가 끝나면 `thread->base`를 참조하는 코드가 더는 없음이 보장됩니다.

**대상이 스케줄되어 있지 않은 경우:** `halt_thread()`를 직접 호출해 정리합니다. 자기 자신을 abort하는 경우 essential이면 패닉시키고([sched.c:336-339](zephyr/kernel/sched.c#L336-L339)), 아니면 `z_swap()`으로 영영 떠납니다.

함수 끝의 경고가 이 API의 성격을 요약합니다([sched.c:347-350](zephyr/kernel/sched.c#L347-L350)):

> 스케줄러 락이 해제되었습니다. 여기에 로직을 넣지 마십시오. 락을 다시 잡더라도 레이스/데드락이 되기 쉽습니다.

### 7.3 `halt_thread()` — 실제 정리

[sched.c:814-939](zephyr/kernel/sched.c#L814-L939). `_THREAD_DEAD`인 경우 수행 순서:

1. 상태 비트 설정 및 레디 큐에서 제거
2. 대기 중이었다면 언펜드
3. **비행 중인 타임아웃 핸들러 완료 대기** — 재시도 루프에서 락을 놓았다 다시 잡습니다([sched.c:849-854](zephyr/kernel/sched.c#L849-L854))
4. `unpend_all(&thread->join_queue)` — join 대기자 전원 기상
5. (SMP) `unpend_all(&thread->halt_queue)`
6. 코프로세서 비활성화, 스레드 모니터 제거, 오브젝트 코어 해제
7. (USERSPACE) 메모리 도메인 탈퇴, 권한 소거, 커널 오브젝트 무효화
8. `k_thread_abort_cleanup()`
9. **더미화(dummify)** — 마지막 단계

3번의 이유가 주석에 명확합니다: 동적 스레드는 호출자가 스토리지를 해제할 수 있으므로, 비행 중 핸들러가 나중에 `thread->base`를 역참조하면 **use-after-free**가 됩니다.

9번 더미화는 엣지 케이스 처리입니다([sched.c:857-865](zephyr/kernel/sched.c#L857-L865), [:904-931](zephyr/kernel/sched.c#L904-L931)): ISR이 현재 스레드를 선점한 상태에서 그 스레드를 abort하면, 곧 있을 컨텍스트 스위치가 이미 해제된 메모리를 건드리게 됩니다. 그래서 `_current`를 전역 `_thread_dummy`로 갈아끼웁니다.

이 단계를 **가장 마지막에 두는 이유**도 명시되어 있습니다 — 위 단계들의 서브시스템이 `_current`가 바뀌지 않았다고 가정할 수 있어야 하기 때문입니다. 또한 join()에서 스핀 중인 스레드를 위해 `switch_handle`을 `NULL`이 아닌 값(`(void *)1`)으로 남겨야 합니다. 죽은 스레드이므로 절대 사용되지 않지만, `NULL`이면 join이 영원히 스핀하기 때문입니다.

## 8. `k_thread_join()`

[thread.c:1748-1779](zephyr/kernel/thread.c#L1748-L1779)

```c
if (z_is_thread_dead(thread)) {
	z_sched_switch_spin(thread);       /* 이미 죽음: 스위치 완료만 대기 */
	ret = 0;
} else if (K_TIMEOUT_EQ(timeout, K_NO_WAIT)) {
	ret = -EBUSY;
} else if ((thread == _current) ||
	   (thread->base.pended_on == &_current->join_queue)) {
	ret = -EDEADLK;                    /* 자기 자신 또는 상호 join */
} else {
	__ASSERT(!arch_is_in_isr(), "cannot join in ISR");
	z_sched_add_to_waitq_locked(_current, &thread->join_queue);
	z_add_thread_timeout(_current, timeout);
	ret = z_swap(&_sched_spinlock, key);
	return ret;
}
```

- 이미 죽은 스레드에 대해서도 `z_sched_switch_spin()`을 호출합니다. "죽었다"는 표시가 서는 시점과 실제 컨텍스트 스위치 완료 시점 사이에 간극이 있으므로(`02-스케줄러.md` §7.3), 스택 등 자원을 재사용하기 전에 완료를 확인해야 합니다.
- 데드락 검출은 **1단계만** 합니다: 자기 자신(A join A)과 직접 상호 join(A join B, B join A)입니다. 3자 이상의 순환은 검출하지 않습니다. 같은 한계가 `thread_halt_spin()` 주석에도 명시되어 있습니다 — "단순 데드락은 막지만 3개 이상 스레드의 순환은 막지 못한다"([sched.c:256-260](zephyr/kernel/sched.c#L256-L260)). (확신도: high — 조건문에 명시)

## 9. 지연 정리 — `CONFIG_THREAD_ABORT_NEED_CLEANUP`

[thread.c:1446-1554](zephyr/kernel/thread.c#L1446-L1554)

메모리 매핑된 스택 등, **자기 자신을 abort하는 스레드가 자기 스택을 해제할 수 없는** 문제를 다룹니다.

```c
if (thread == _current) {
	defer_thread_cleanup(thread);   /* 나중에 */
} else {
	do_thread_cleanup(thread);      /* 지금 */
}
```

지연된 정리는 **다음번 `k_thread_abort()` 호출 시** 또는 **같은 스레드 객체가 재사용될 때** 수행됩니다. 주석은 이 방식이 이상적이지 않음(스택이 한동안 매핑된 채 남음)을 인정하면서도, 대안 세 가지 — abort 중 스케줄 락 우회, 정리 전용 스레드 생성, 아키텍처 지원(예외) 요구 — 를 피할 수 있는 단순한 해법이라고 설명합니다([thread.c:1517-1529](zephyr/kernel/thread.c#L1517-L1529)).

`defer_thread_cleanup()`에는 향후 확장자를 위한 규칙이 달려 있습니다([thread.c:1460-1465](zephyr/kernel/thread.c#L1460-L1465)): **실제 정리 시점에는 스레드 객체가 이미 덮어써졌을 수 있으므로**(스택에 할당된 스레드 객체 등), 필요한 데이터는 지연 등록 시점에 따로 보관해야 합니다. 실제로 스택 주소와 크기를 정적 변수에 복사해 둡니다.

## 10. 더미 스레드

[thread.c:1556-1585](zephyr/kernel/thread.c#L1556-L1585)

스택도 엔트리 포인트도 없는 `k_thread`입니다. 두 곳에서 쓰입니다.

1. **부팅** — CPU에서 첫 스레드를 실행하려면 "빠져나올 컨텍스트"가 필요합니다. 저장된 상태는 그대로 버려집니다([kswap.h:232-240](zephyr/kernel/include/kswap.h#L232-L240)).
2. **ISR 안에서의 `_current` abort** — §7.3의 더미화.

전역 인스턴스 `_thread_dummy`는 `__incoherent`로 선언되어 있습니다([sched.c:40](zephyr/kernel/sched.c#L40)).

```c
#if defined(CONFIG_KERNEL_COHERENCE)
#define __incoherent __in_section_unique(cached)
#else
#define __incoherent
#endif
```
[section_tags.h:66-78](zephyr/include/zephyr/linker/section_tags.h#L66-L78)

`CONFIG_KERNEL_COHERENCE`가 켜지면 커널 공유 데이터는 코히런트(일반적으로 **비캐시**) 메모리에 배치되는데, `__incoherent`는 그 심볼만 **캐시 가능 섹션으로 되돌립니다.** 즉 "일관성 면제"가 아니라 "일관성 배치에서 제외"입니다. 더미 스레드의 저장 상태는 어차피 버려지므로 CPU 간 일관성이 필요 없고, 캐시에 두는 편이 빠릅니다. (확신도: high — 매크로 정의 직접 확인)

본 분석 타깃에서는 `CONFIG_KERNEL_COHERENCE=n`이므로 이 속성이 빈 매크로로 전개됩니다.

## 11. 요약 — 상태 전이별 진입점

| 전이 | 공개 API | 내부 경로 |
|---|---|---|
| 생성 → SLEEPING | `k_thread_create()` | `z_setup_new_thread()` |
| SLEEPING → READY | `k_thread_start()` | `k_wakeup()` → `z_sched_ready_locked()` |
| READY → RUNNING | — | `next_up()` → `arch_switch()` |
| RUNNING → PENDING | `k_sem_take()` 등 | `z_pend_curr()` → `add_to_waitq_locked()` |
| PENDING → READY | `k_sem_give()` 등 | `z_sched_wake()` |
| PENDING → READY (타임아웃) | — | `z_thread_timeout()` → `z_sched_wake_thread_locked()` |
| RUNNING → SLEEPING | `k_sleep()` | `z_tick_sleep()` |
| SLEEPING → READY | `k_wakeup()` / 타임아웃 | `z_sched_ready_locked()` |
| * → SUSPENDED | `k_thread_suspend()` | `z_thread_halt(terminate=false)` |
| SUSPENDED → READY | `k_thread_resume()` | `z_sched_ready_locked()` |
| * → DEAD | `k_thread_abort()` | `z_thread_halt(terminate=true)` → `halt_thread()` |
| DEAD 대기 | `k_thread_join()` | `join_queue` 펜드 |

## 12. 다음 문서

- 타임아웃 큐 자체의 구조와 4종 백엔드: `04-타임아웃과-시스템-클럭.md` (예정)
- `z_sched_wake()`를 사용하는 IPC 객체들: `05-동기화-객체.md` (예정)
