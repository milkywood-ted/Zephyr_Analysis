# 동기화 객체

분석 대상: [zephyr/kernel/sem.c](zephyr/kernel/sem.c), [zephyr/kernel/mutex.c](zephyr/kernel/mutex.c), [zephyr/kernel/condvar.c](zephyr/kernel/condvar.c), [zephyr/kernel/events.c](zephyr/kernel/events.c), [zephyr/kernel/poll.c](zephyr/kernel/poll.c)

> **타깃 적용 (`qemu_cortex_a53` SMP)** — 전체 구성은 [00-분석-개요.md](1.zephyr분석/00-분석-개요.md) §4 참조
>
> | 본 문서 항목 | 이 타깃에서 |
> |---|---|
> | §3 세마포어 / §4 뮤텍스 / §5 condvar | **활성.** 항상 빌드됨 |
> | §6 이벤트 (`CONFIG_EVENTS`) | **활성.** Kconfig 기본값은 없지만 `SCHED_IPI_SUPPORTED`가 `EVENTS`를 select ([smp/Kconfig:34](zephyr/kernel/smp/Kconfig#L34))하고 이 타깃은 SMP이므로 켜집니다 |
> | §7 poll (`CONFIG_POLL`) | **비활성 추정.** 기본값 없음 → 다른 서브시스템이 select하지 않으면 n. `sem->poll_events`도 함께 사라집니다. (확신도: medium — 빌드로 확인하지 못함) |
> | §4 우선순위 상속 | **활성.** `PRIORITY_CEILING` 기본 -128 < `K_LOWEST_THREAD_PRIO`(15) → `#if` 가드 통과 ([kernel/Kconfig:86](zephyr/kernel/Kconfig#L86)) |
> | 우선순위 상한 클램프 | **실질 무효.** 상한이 -128(가능한 최고 우선순위)이라 `z_get_new_prio_with_ceiling()`이 값을 바꾸지 않음 |
> | §6 이벤트의 2단계 깨우기 | **비활성.** `WAITQ_SCALABLE=n`이므로 walk 콜백에서 곧바로 깨우는 경로 |
> | 각 API의 `z_vrfy_*` 계층 | **비활성.** `USERSPACE=n` |

---

## 1. 공통 기반 — 원시 연산 두 개

모든 동기화 객체는 스케줄러가 제공하는 **두 개의 원시 연산** 위에 쌓입니다(`02-스케줄러.md` §8).

| 방향 | 함수 | 하는 일 |
|---|---|---|
| 대기 | `z_pend_curr(lock, key, wait_q, timeout)` | 호출자의 락을 놓으면서 현재 스레드를 wait_q에 pend하고 스위치 |
| 깨우기 | `z_sched_wake(wait_q, retval, data)` | 최우선 대기자 하나를 언펜드·레디 전환, 반환값 설정까지 원자적으로 |

각 객체가 하는 일은 결국 **"언제 pend하고 언제 wake할지"의 정책**과 **자기 상태 관리**뿐입니다.

| 객체 | 상태 | 대기 조건 | 깨우기 계기 |
|---|---|---|---|
| `k_sem` | `count`, `limit` | `count == 0` | `k_sem_give()` |
| `k_mutex` | `owner`, `lock_count`, `owner_orig_prio` | 다른 스레드가 소유 | `k_mutex_unlock()` |
| `k_condvar` | 없음 (순수 대기 큐) | 항상 대기 | `signal` / `broadcast` |
| `k_event` | `events` 비트 집합 | 원하는 비트 조합 불충족 | `post` / `set` / `clear` |
| `k_poll` | 객체별 `poll_events` 리스트 | 모든 이벤트가 not-ready | 감시 대상 객체의 상태 변화 |

---

## 2. 락 전략 — 전역이 기본, 이벤트만 예외

객체마다 락을 어디에 두었는지가 갈립니다. 그리고 **세 파일이 각자 자기 선택을 주석으로 변명합니다.**

| 파일 | 락 | 범위 |
|---|---|---|
| `sem.c` | `static struct k_spinlock sem_lock` | 시스템 전역 |
| `mutex.c` | `static struct k_spinlock mutex_lock` | 시스템 전역 |
| `condvar.c` | `static struct k_spinlock condvar_lock` | 시스템 전역 |
| `poll.c` | `static struct k_spinlock poll_lock` | 시스템 전역 (poll 객체 전용이 아니라 **다른 객체 안의 `poll_events` 리스트와 `k_work_poll`까지** 함께 보호) |
| **`events.c`** | `event->lock` | **객체별** |

### 세마포어 — RAM 대 성능

```c
/* We use a system-wide lock to synchronize semaphores, which has
 * unfortunate performance impact vs. using a per-object lock
 * (semaphores are *very* widely used).  But per-object locks require
 * significant extra RAM.  A properly spin-aware semaphore
 * implementation would spin on atomic access to the count variable,
 * and not a spinlock per se.  Useful optimization for the future...
 */
```
[sem.c:32-38](zephyr/kernel/sem.c#L32-L38)

세마포어가 **가장 널리 쓰이는** 객체인데도 전역 락을 쓴다는 점을 인정하면서, 이상적인 구현은 스핀락이 아니라 **`count`에 대한 원자 접근으로 스핀**하는 것이라고 방향까지 제시합니다.

### 뮤텍스 — 보호 대상이 객체 밖에 있음

```c
/* We use a global spinlock here because some of the synchronization
 * is protecting things like owner thread priorities which aren't
 * "part of" a single k_mutex. ...
 */
```
[mutex.c:45-49](zephyr/kernel/mutex.c#L45-L49)

이쪽은 이유가 다릅니다. 우선순위 상속이 건드리는 것은 **소유자 스레드의 우선순위**이고, 그것은 특정 뮤텍스에 속한 데이터가 아닙니다. 객체별 락으로는 보호 범위를 그릴 수 없습니다.

### poll — 락 구조 자체가 난해함

```c
/* Single subsystem lock.  Locking per-event would be better on highly
 * contended SMP systems, but the original locking scheme here is
 * subtle (it relies on releasing/reacquiring the lock in areas for
 * latency control and it's sometimes hard to see exactly what data is
 * "inside" a given critical section). ...
 */
```
[poll.c:28-34](zephyr/kernel/poll.c#L28-L34)

"어떤 데이터가 임계 구역 안에 있는지 파악하기 어렵다"는 표현이 눈에 띕니다. 락을 쪼개지 않은 이유가 성능 판단이 아니라 **기존 구조의 이해 난도**입니다.

`k_event`만 객체별 락을 쓰는 것은 §6의 대기 큐 순회와 관련이 있어 보입니다 — post 한 번에 대기 큐 전체를 훑기 때문에 임계 구역이 길고, 전역 락이면 무관한 이벤트끼리 서로를 막게 됩니다. (확신도: medium — 코드에 명시적 근거는 없는 추론)

---

## 3. 세마포어

### 3.1 give — 직접 전달

[sem.c:95-116](zephyr/kernel/sem.c#L95-L116)

```c
if (z_sched_wake(&sem->wait_q, 0, NULL)) {
	resched = true;
} else {
	sem->count += (sem->count != sem->limit) ? 1U : 0U;
	resched = sem_handle_poll_events(sem);
}
```

**대기자를 깨웠으면 `count`를 올리지 않습니다.** 세마포어 토큰이 카운터를 경유하지 않고 대기 스레드에게 **직접 전달**되는 구조입니다. 카운터를 올렸다가 깨어난 스레드가 다시 내리는 것과 결과는 같지만, 중간에 제3의 스레드가 끼어들어 토큰을 가로챌 여지가 없습니다.

`limit` 포화도 조용히 처리합니다 — `count == limit`이면 증가분이 0이 되어 give가 **무시**됩니다. 오류를 반환하지 않습니다.

### 3.2 take — 빠른 경로

[sem.c:127-159](zephyr/kernel/sem.c#L127-L159)

```c
if (likely(sem->count > 0U)) {
	sem->count--;
	k_spin_unlock(&sem_lock, key);
	ret = 0;
	goto out;
}
if (K_TIMEOUT_EQ(timeout, K_NO_WAIT)) { ... return -EBUSY; }
ret = z_pend_curr(&sem_lock, key, &sem->wait_q, timeout);
```

세 갈래가 전부입니다. 반환값은 `z_pend_curr()`가 그대로 전달하므로, **타임아웃이면 `-EAGAIN`**입니다 — `do_swap()`이 미리 심어둔 기본값입니다(`04-타임아웃과-시스템-클럭.md` §5, `03-스레드-생명주기.md` §5).

ISR에서의 호출은 `K_NO_WAIT`일 때만 허용되며 어서션으로 검사합니다([sem.c:131-132](zephyr/kernel/sem.c#L131-L132)). 실제 차단은 `z_pend_curr()`가 무조건 패닉시킵니다(`02-스케줄러.md` §8).

### 3.3 reset

[sem.c:161-180](zephyr/kernel/sem.c#L161-L180). 대기자 전원을 **`-EAGAIN`으로** 깨우고 `count`를 0으로 만듭니다. 대기자 입장에서는 타임아웃과 구분되지 않습니다.

---

## 4. 뮤텍스와 우선순위 상속

### 4.1 상태

```c
struct k_mutex {
	_wait_q_t wait_q;
	struct k_thread *owner;
	uint32_t lock_count;      /* 재귀 잠금 횟수 */
	int owner_orig_prio;      /* 상속 전 원래 우선순위 */
	...
};
```
[kernel.h:3572-3586](zephyr/include/zephyr/kernel.h#L3572-L3586)

`owner_orig_prio`가 **뮤텍스에 저장**된다는 점이 중요합니다. 스레드가 아니라 뮤텍스가 "이 소유자를 원래 어디로 되돌려야 하는가"를 기억합니다.

### 4.2 획득 시 상향

[mutex.c:108-219](zephyr/kernel/mutex.c#L108-L219)

경합 없는 경로(잠기지 않았거나 이미 내가 소유)는 `lock_count++`로 끝나며, 이때 **첫 획득이면** `owner_orig_prio`에 현재 우선순위를 기록합니다([mutex.c:125-127](zephyr/kernel/mutex.c#L125-L127)).

경합이 나면 pend **전에** 소유자 우선순위를 올립니다.

```c
new_prio = new_prio_for_inheritance(_current->base.prio, mutex->owner->base.prio);
if (z_is_prio_higher(new_prio, mutex->owner->base.prio)) {
	resched = adjust_owner_prio(mutex, new_prio);
}
int got_mutex = z_pend_curr(&mutex_lock, key, &mutex->wait_q, timeout);
```

`new_prio_for_inheritance()`는 둘 중 높은 쪽을 고른 뒤 상한을 적용합니다([mutex.c:84-91](zephyr/kernel/mutex.c#L84-L91)).

### 4.3 타임아웃 시 하향

대기가 타임아웃되면 **자신이 올려놓은 우선순위를 되돌려야** 합니다([mutex.c:187-209](zephyr/kernel/mutex.c#L187-L209)).

```c
if (likely(mutex->owner != NULL)) {
	struct k_thread *waiter = z_waitq_head(&mutex->wait_q);
	new_prio = (waiter != NULL) ?
		new_prio_for_inheritance(waiter->base.prio, mutex->owner_orig_prio) :
		mutex->owner_orig_prio;
	resched = adjust_owner_prio(mutex, new_prio) || resched;
}
```

**남은 대기자 중 최고 우선순위**를 기준으로 다시 계산합니다. 대기자가 없으면 원래 우선순위로 완전히 복귀합니다. `mutex->owner != NULL` 검사는 "언펜드된 뒤 해제까지 일어난" 경우를 걸러냅니다.

### 4.4 해제 — 인계의 원자성

[mutex.c:231-316](zephyr/kernel/mutex.c#L231-L316)

```c
adjust_owner_prio(mutex, mutex->owner_orig_prio);   /* ① 먼저 원복 */

LOCK_SCHED_SPINLOCK {                                /* ② 인계는 원자적으로 */
	new_owner = z_unpend_first_thread_locked(&mutex->wait_q);
	mutex->owner = new_owner;

	if (unlikely(new_owner != NULL)) {
		mutex->owner_orig_prio = new_owner->base.prio;
		arch_thread_return_value_set(new_owner, 0);
		z_sched_ready_locked(new_owner);
	} else {
		mutex->lock_count = 0U;
	}
}
```

**① 우선순위 원복이 인계보다 먼저**입니다. 상속받은 우선순위를 지닌 채 새 소유자를 고르면, 자신이 계속 높은 우선순위로 실행되어 버립니다.

**② 인계 전체가 `LOCK_SCHED_SPINLOCK` 하나 안에서** 일어납니다. 주석이 이유를 밝힙니다([mutex.c:278-281](zephyr/kernel/mutex.c#L278-L281)):

> 비행 중인 타임아웃 핸들러가 **반쯤 초기화된 wake를 관측하지 못하도록** 원자적으로 처리한다.

`z_sched_wake()`가 존재하는 이유와 정확히 같은 문제입니다(`04-타임아웃과-시스템-클럭.md` §6). 여기서는 소유권 이전까지 함께 해야 해서 래퍼 대신 하위 함수 `z_unpend_first_thread_locked()`를 직접 씁니다.

새 소유자의 우선순위를 조정하지 않는 이유도 주석에 있습니다([mutex.c:290-294](zephyr/kernel/mutex.c#L290-L294)): 대기 큐가 **우선순위 순**이므로 첫 대기자는 이미 나머지보다 높거나 같습니다.

### 4.5 중첩 규약 — 지키지 않으면 우선순위가 어긋납니다

파일 헤더 주석이 **API 사용자에게 요구하는 규약**을 명시합니다([mutex.c:17-27](zephyr/kernel/mutex.c#L17-L27)).

> 우선순위 상속에 기여하는 각 뮤텍스는 **획득의 역순으로 해제**되어야 한다. 또한 소유자의 우선순위를 올리는 데 기여하는 후속 뮤텍스는 **가장 최근의 우선순위 상향 이후 시점**에 획득되어야 한다.
>
> 이 중첩 모델을 따르지 않으면 스레드가 **예상치 못한 우선순위(너무 높거나 낮은)로 실행될 수 있다.**

`owner_orig_prio`가 뮤텍스마다 하나뿐인 스칼라이기 때문입니다. M1 획득 시 원래 우선순위를 저장하고, 상향된 뒤 M2를 획득하면 M2는 **이미 상향된 값**을 자기 `owner_orig_prio`로 저장합니다. 역순으로 해제하면 M2 → 상향값, M1 → 원래값 순으로 되돌아가 맞아떨어지지만, 순서가 뒤바뀌면 어긋납니다.

**커널이 강제하지 않고 문서로만 요구하는 제약**이라는 점이 중요합니다.

### 4.6 상속의 한계

코드상 우선순위 조정은 **`k_mutex_lock()` 시점과 해제·타임아웃 시점**에만 일어납니다. 즉:

- **전이적 상속이 없습니다.** A가 M1을 기다리고 그 소유자 B가 M2를 기다리는 경우, A의 우선순위는 B까지만 전파되고 M2의 소유자 C에게는 전파되지 않습니다. (확신도: high — `adjust_owner_prio()` 호출부가 세 곳뿐이고 어느 것도 소유자가 대기 중인 다른 뮤텍스를 따라가지 않음)
- 이미 대기 중인 상태에서 대기자의 우선순위가 바뀌어도 재계산되지 않습니다.

### 4.7 우선순위 상한

`CONFIG_PRIORITY_CEILING`은 상속으로 도달할 수 있는 **가장 높은 우선순위의 하한선**입니다([kernel/Kconfig:84-93](zephyr/kernel/Kconfig#L84-L93)). 기본값 -128은 가능한 최고 우선순위이므로 **클램프가 실질적으로 동작하지 않습니다.**

전체 상속 알고리즘 자체를 끄려면 상한을 아이들 우선순위 이하(숫자로는 이상)로 설정하면 됩니다 — `#if (CONFIG_PRIORITY_CEILING < K_LOWEST_THREAD_PRIO)` 가드가 관련 코드를 통째로 제거합니다([mutex.c:83](zephyr/kernel/mutex.c#L83)).

---

## 5. 조건 변수

[condvar.c](zephyr/kernel/condvar.c). 자체 상태가 없는 **순수 대기 큐**입니다.

### `k_condvar_wait()` — POSIX 의미 유지

[condvar.c:105-138](zephyr/kernel/condvar.c#L105-L138)

```c
key = k_spin_lock(&condvar_lock);
k_mutex_unlock(mutex);
ret = z_pend_curr(&condvar_lock, key, &condvar->wait_q, timeout);
k_mutex_lock(mutex, K_FOREVER);
```

**뮤텍스를 놓기 전에 `condvar_lock`을 먼저 잡습니다.** 그래야 unlock과 pend 사이에 signal이 끼어들어 유실되는 lost-wakeup을 막습니다.

반환 시 **타임아웃이든 아니든 뮤텍스를 반드시 다시 잡습니다.** 주석이 근거를 POSIX에 둡니다([condvar.c:128-132](zephyr/kernel/condvar.c#L128-L132)) — `pthread_cond_wait()`는 어떤 경로로 반환하든 호출 스레드가 뮤텍스를 쥐고 있어야 합니다. 재획득이 `K_FOREVER`인 것도 그래서입니다.

### `K_NO_WAIT` 특례

[condvar.c:113-121](zephyr/kernel/condvar.c#L113-L121)

```c
if (unlikely(K_TIMEOUT_EQ(timeout, K_NO_WAIT))) {
	/* No wait: per POSIX semantics, the mutex must remain locked when
	 * k_condvar_wait() returns. Returning -EAGAIN immediately without
	 * touching the mutex preserves the calling-thread-holds-mutex
	 * invariant.
	 */
	return ret;   /* -EAGAIN */
}
```

**뮤텍스를 아예 건드리지 않고** 즉시 반환합니다. 일반 경로를 그대로 탔다면 unlock → pend 실패 → lock 순서가 되어, 그 짧은 틈에 다른 스레드가 뮤텍스를 가져갈 수 있습니다. 함수가 실패했는데 부작용은 남는 셈입니다.

---

## 6. 이벤트

[events.c](zephyr/kernel/events.c). 32비트 **비트 집합**이며, 한 번의 post로 **여러 스레드를 동시에** 깨울 수 있다는 점이 다른 객체와 구별됩니다.

### 6.1 조건 판정

```c
static uint32_t are_wait_conditions_met(uint32_t desired, uint32_t current,
					unsigned int wait_condition)
{
	uint32_t match = current & desired;

	if ((wait_condition == K_EVENT_WAIT_ALL) && (match != desired)) {
		return 0;
	}
	return match;
}
```
[events.c:96-108](zephyr/kernel/events.c#L96-L108)

ANY는 교집합이 비지 않으면 성립, ALL은 교집합이 원하는 집합과 같아야 성립합니다. 반환값은 bool이 아니라 **일치한 비트들**이며, 이것이 깨어난 스레드의 반환값이 됩니다.

### 6.2 post — 대기 큐 전수 검사

일반적인 wake와 달리 **최우선 대기자 하나를 고르는 것이 아니라 전원을 검사**해야 합니다. 조건이 스레드마다 다르기 때문입니다.

```c
z_sched_waitq_walk(&event->wait_q, event_walk_op, EVENT_POST_WALK_OP_FN, &data);
event->events = data.events & ~data.clear_events;
z_reschedule(&event->lock, key);
```
[events.c:206-216](zephyr/kernel/events.c#L206-L216)

`z_sched_waitq_walk()`는 **순회 전체를 `_sched_spinlock` 아래에서** 수행하며, 헤더가 그 비용을 경고합니다([scheduler.h:66-68](zephyr/kernel/include/scheduler.h#L66-L68)):

> 대기 큐 길이는 불확정이므로 스케줄러가 불확정 시간 동안 잠깁니다. 시스템 성능에 영향을 줄 수 있습니다.

이벤트가 **객체별 락**을 쓰는 이유도 이와 무관하지 않아 보입니다(§2).

### 6.3 백엔드에 따라 갈리는 깨우기

[events.c:195-204](zephyr/kernel/events.c#L195-L204)의 주석이 두 경로를 설명합니다.

| 대기 큐 백엔드 | 방식 |
|---|---|
| `WAITQ_SIMPLE` (타깃) | walk 콜백에서 **곧바로** 깨움 ([events.c:162-169](zephyr/kernel/events.c#L162-L169)) |
| `WAITQ_SCALABLE` | ① walk하며 깨울 스레드의 **연결 리스트를 만들고** ② post-walk 콜백에서 순회하며 깨움. **둘 다 같은 `z_sched_waitq_walk()` 호출 안, `_sched_spinlock`을 쥔 채** 일어납니다([scheduler.h:111-118](zephyr/kernel/scheduler.c#L111-L118)) |

두 단계가 필요한 이유는 `k_thread` 구조체의 필드 주석에 있습니다([thread.h:277-288](zephyr/include/zephyr/kernel/thread.h#L277-L288)). 금지되는 것은 **순회 중 노드 제거**이지 필드 변경 일반이 아닙니다 — 실제로 scalable 경로도 순회 중 `thread->events` 등을 씁니다.

> 레드블랙 트리를 wait queue로 쓸 때만 필요하다. **rbtree waitq를 순회하는 도중에 변형하는 것이 금지**되어 있기 때문이다.

그래서 `next_event_link` 필드가 `CONFIG_WAITQ_SCALABLE`일 때만 존재합니다. `z_sched_waitq_walk()`의 경고도 같은 내용입니다([scheduler.h:71](zephyr/kernel/include/scheduler.h#L71)) — 콜백에서 스레드를 큐에서 제거해도 되는 것은 `WAITQ_SCALABLE=n`일 때뿐입니다.

### 6.4 `thread->events`의 이중 용도

구조체 주석이 명시합니다([thread.h:290](zephyr/include/zephyr/kernel/thread.h#L290)): `dual purpose - wait on and then received`.

- 대기 진입 시: **원하는** 이벤트 집합을 기록 ([events.c:331](zephyr/kernel/events.c#L331))
- 깨어날 때: 콜백이 **일치한** 집합으로 덮어씀 ([events.c:156](zephyr/kernel/events.c#L156))
- 반환 시: 그 값을 읽어 반환 ([events.c:339](zephyr/kernel/events.c#L339))

필드 하나를 요청과 응답에 재사용해 `k_thread` 크기를 아낍니다.

### 6.5 소비 옵션

`K_EVENT_OPTION_CLEAR`가 붙으면 일치한 비트가 **이벤트 객체에서 제거**됩니다([events.c:157-159](zephyr/kernel/events.c#L157-L159), [:214](zephyr/kernel/events.c#L214)). 이 옵션을 쓰는 API가 `*_safe` 계열입니다([events.c:390-427](zephyr/kernel/events.c#L390-L427)).

`clear_events`를 walk 도중이 아니라 **끝난 뒤 한꺼번에** 반영하는 것이 중요합니다. 순회 중에 지우면 뒤에 검사되는 스레드가 이미 소비된 이벤트를 보지 못해, 같은 post에 대해 앞뒤 스레드가 다르게 판정됩니다.

---

## 7. poll

> 본 타깃에서는 `CONFIG_POLL`이 꺼져 있을 것으로 보이므로, 이 절은 참고용입니다(문서 상단 타깃 표).

[poll.c](zephyr/kernel/poll.c). **여러 이벤트를 동시에 기다리는** 프레임워크입니다. 앞의 객체들과 방향이 반대입니다 — 객체가 대기 큐를 갖는 대신, **객체가 자기를 감시 중인 이벤트들의 리스트**를 갖습니다.

```c
struct k_sem {
	_wait_q_t wait_q;
	unsigned int count, limit;
	Z_DECL_POLL_EVENT        /* → sys_dlist_t poll_events */
	...
};
```
[kernel.h:3798-3806](zephyr/include/zephyr/kernel.h#L3798-L3806)

### 7.1 등록과 즉시 판정

[poll.c:228-257](zephyr/kernel/poll.c#L228-L257)

```c
for (int ii = 0; ii < num_events; ii++) {
	key = k_spin_lock(&poll_lock);
	if (is_condition_met(&events[ii], &state)) {
		set_event_ready(&events[ii], state);
		poller->is_polling = false;
	} else if (!just_check && poller->is_polling) {
		register_event(&events[ii], poller);
		events_registered += 1;
	}
	k_spin_unlock(&poll_lock, key);
}
```

**이벤트마다 락을 잡았다 놓습니다.** 지연 시간 제어를 위한 의도적 설계이며, §2에서 인용한 "락을 놓았다 다시 잡는 것에 의존하는 미묘한 구조"가 이것입니다.

`poller->is_polling`이 상태 기계 역할을 합니다. 등록 도중 조건이 하나라도 충족되면 `false`가 되고, 이후 이벤트는 등록하지 않습니다. 등록된 이벤트가 **다른 CPU/ISR에서 발화**해도 `signal_poll_event()`가 이 플래그를 내립니다([poll.c:458](zephyr/kernel/poll.c#L458)). 등록 루프가 끝난 뒤 플래그를 다시 확인하는 이유입니다([poll.c:308](zephyr/kernel/poll.c#L308)).

### 7.2 우선순위 순 등록

[poll.c:110-132](zephyr/kernel/poll.c#L110-L132)

`add_event()`는 이벤트를 객체의 리스트에 **poller 스레드의 우선순위 순**으로 끼워 넣습니다. 꼬리부터 비교해 대부분의 경우 append로 끝나는 최적화가 들어 있습니다.

객체가 발화하면 `sys_dlist_get()`으로 **머리 하나만** 꺼내므로([poll.c:474](zephyr/kernel/poll.c#L474)), 이 정렬이 곧 "여러 poller 중 누가 먼저 받는가"를 결정합니다.

### 7.3 공유 대기 큐

```c
static _wait_q_t wait_q = Z_WAIT_Q_INIT(&wait_q);
int swap_rc = z_pend_curr(&poll_lock, key, &wait_q, timeout);
```
[poll.c:327-329](zephyr/kernel/poll.c#L327-L329)

함수 내부 `static`이라 **모든 `k_poll()` 호출자가 같은 대기 큐 하나를 공유**합니다. 큐의 우선순위 순서는 의미가 없습니다 — 깨우기가 큐 순서를 따르지 않고 `signal_poller()`가 **특정 스레드를 지목해** 언펜드하기 때문입니다([poll.c:259-280](zephyr/kernel/poll.c#L259-L280)).

즉 이 wait_q는 "대기자를 고르는 자료구조"가 아니라 **단순한 주차 공간**입니다. 실제 선택은 §7.2의 객체별 리스트가 합니다.

### 7.4 두 가지 poller 모드

| 모드 | 깨우기 함수 | 용도 |
|---|---|---|
| `MODE_POLL` | `signal_poller()` | `k_poll()`로 블록된 스레드 |
| `MODE_TRIGGERED` | `signal_triggered_work()` | `k_work_poll` — 워크 큐에 작업 제출 |

`signal_poll_event()`가 모드를 보고 분기합니다([poll.c:443-467](zephyr/kernel/poll.c#L443-L467)). 같은 등록 메커니즘 위에 **동기 대기와 비동기 콜백**을 함께 얹은 구조입니다.

### 7.5 객체 쪽 연결점

세마포어가 poll에 관여하는 지점은 단 한 줄입니다.

```c
static inline bool sem_handle_poll_events(struct k_sem *sem)
{
	return z_handle_obj_poll_events(&sem->poll_events, K_POLL_STATE_SEM_AVAILABLE);
}
```
[sem.c:85-93](zephyr/kernel/sem.c#L85-L93)

그리고 이 함수는 **대기자가 없을 때만** 호출됩니다([sem.c:106](zephyr/kernel/sem.c#L106)). 대기자를 직접 깨웠다면 세마포어는 다시 비어 있으므로 poller에게 알릴 것이 없습니다.

---

## 8. 정리 — 계층 구조

```
  k_sem      k_mutex     k_condvar    k_event      k_poll
    │           │            │           │            │
    │           │            │           │            └─ 객체별 poll_events 리스트
    │           │            │           │               (역방향: 객체가 감시자를 가짐)
    │           │            │           └─ z_sched_waitq_walk()  전수 검사
    │           │            └─ mutex와 조합, POSIX 의미
    │           └─ 우선순위 상속 + LOCK_SCHED_SPINLOCK 원자 인계
    │
    └──────── 공통 기반 ────────┐
                                ▼
              z_pend_curr()  /  z_sched_wake()
                                ▼
                          _sched_spinlock
```

관찰된 원칙:

- **객체는 정책만 갖습니다.** 대기·깨우기의 메커니즘은 전부 스케줄러에 있고, 각 파일은 "언제"를 결정할 뿐입니다.
- **직접 전달이 카운터 경유보다 낫습니다.** 세마포어 give와 뮤텍스 unlock 모두 대기자에게 자원을 곧바로 넘깁니다.
- **원자성 경계가 반복해서 등장합니다.** 뮤텍스 인계가 `LOCK_SCHED_SPINLOCK`로 묶이는 이유는 `z_sched_wake()`가 존재하는 이유와 같습니다 — 비행 중인 타임아웃 핸들러에게 중간 상태를 보이지 않기 위해서입니다(`04-타임아웃과-시스템-클럭.md` §6).
- **자료구조 선택이 알고리즘을 바꿉니다.** rbtree wait queue는 순회 중 변형이 불가능해, 이벤트 post가 2단계로 갈라집니다.
- **일부 규약은 코드가 아니라 문서에 있습니다.** 뮤텍스 중첩 해제 순서가 대표적이며, 어기면 조용히 잘못된 우선순위로 실행됩니다.

## 9. 다음 문서

- 리셋 벡터에서 main 스레드까지: `07-부팅과-초기화.md` (예정)
